# aichat 项目优化计划

## Context

项目功能已全部完成，最终形态确认为 Electron 客户端。当前存在三个核心问题：

1. **客户端体积过大**：打包后 1.4 GB（解压）/ 557 MB（压缩），其中 `node_modules` 占 959 MB（68%），包含大量前端已编译的库和构建工具
2. **打包 exe 崩溃**：`aichat.exe` 启动即秒退，排查文档的结论不准确（详见下方"崩溃诊断"）
3. **代码/架构可优化**：App.tsx 889 行单体状态管理，chat.ts ~770 行双路由重复逻辑，CSS 双系统并存等

---

## Phase 0: 打包 exe 崩溃诊断与修复（最优先）

### 排查文档的问题

排查文档的结论"高度怀疑是 Windows 环境限制"是**误导性的**。文档中所有 5 个测试都用 `node` 命令运行（不是 `electron` 或打包后的 exe）。用普通 Node.js 调用 `require('electron')` 返回路径字符串、`app` 是 undefined —— 这些都是 npm `electron` 包的**正常预期行为**，真正的 Electron API 只有在 Electron 二进制内部才可用。因此这些测试结果不能用来诊断打包后的 exe 崩溃。

### 真正的崩溃原因（按概率排序）

**1. ESM import 模式问题（最高概率）**

git diff 显示，原始提交代码使用可靠的 `createRequire` 桥接模式：
```js
import { createRequire } from 'module';
const require = createRequire(import.meta.url);
const { app, BrowserWindow, ... } = require('electron');
```

当前未暂存的修改改成了 `import electron from 'electron'`（默认导出）：
```js
import electron from 'electron';
const { app, BrowserWindow, ... } = electron;
```

排查文档推荐的正确 ESM 写法是 `import * as electron from 'electron/main'`（命名空间导入），不是默认导入。如果默认导出返回的对象不包含 `{ app, BrowserWindow }` 等属性，解构后全是 `undefined`，调用 `app.whenReady()` 会立即抛 TypeError，进程崩溃且**没有任何输出**（崩溃在 Electron 日志初始化之前）。

同样的问题存在于 `pet-window.mjs` 和 `fullscreen-detector.mjs`。

**2. GPU 加速崩溃（中概率）**

Windows Server 或无独立 GPU 的机器，Chromium GPU 进程会崩溃退出，导致主进程跟着退出，完全无声。

**3. Windows Defender 拦截未签名 exe（低概率）**

当前 `signAndEditExecutable: false`，exe 未签名，SmartScreen 可能静默拦截。

### 修复方案

**Step 0.1：修正 Electron import 模式**

将 `electron/main.mjs`、`electron/pet-window.mjs`、`electron/fullscreen-detector.mjs` 的 import 方式统一改为排查文档推荐的模式：

```js
import * as electron from 'electron/main';
const { app, BrowserWindow, ... } = electron;
```

或者改回可靠的 `createRequire` 桥接模式。

**影响文件**：
- `electron/main.mjs`
- `electron/pet-window.mjs`
- `electron/fullscreen-detector.mjs`

**Step 0.2：添加错误处理和崩溃诊断**

在 `main.mjs` 最外层添加 try-catch，捕获初始化错误并输出到文件：
```js
try {
  // ... 全部主进程代码
} catch (err) {
  fs.writeFileSync('crash.log', `${new Date().toISOString()}\n${err.stack}\n`, 'utf-8');
  app.quit();
}
```

**Step 0.3：添加 GPU 加速兜底**

在 `app.whenReady()` 前添加：
```js
app.disableHardwareAcceleration();
```
如果这个修复有效，说明确实是 GPU 问题；后续可以按需恢复 GPU 加速。

**Step 0.4：验证方法**

- 先用 `npm run dev:electron` 测试修改后的代码是否正常（开发模式）
- 然后 `npm run build` 打包，在另一台 Windows 机器上测试打包后的 exe
- 如果开发模式也崩溃，说明 import 模式就是根因

---

## Phase 1: 客户端体积压缩（高优先级，预计节省 400-500 MB）

### 1.1 分离前后端依赖（最大收益 ~230 MB）

**核心问题**：`electron-builder.yml` 将整个 `node_modules`（959 MB）复制到 `extraResources`，但后端 `node.exe` 只需约 11 个运行时包。前端库（antd、lucide-react、react 等）已被 Vite 编译进 922 KB 的 bundle，完全不需要在运行时 node_modules 中。

**方案**：创建 `server/package.json`，仅包含后端运行时依赖，打包时使用该目录的 node_modules。

**具体步骤**：
- 创建 `server/package.json`，dependencies 仅包含：
  `express, cors, better-sqlite3, uuid, dotenv, multer, ai, @ai-sdk/openai-compatible, mupdf, mammoth`
- 将 `emoji-picker-element-data` 从根 dependencies 移除（源码未引用）
- 将 `country-flag-emoji-polyfill` 从根 dependencies 移除（源码未引用）
- 将 `react-router-dom` 从根 dependencies 移除（源码未引用）
- 将 `framer-motion` 从根 dependencies 移除（已被 `motion` 替代，源码只 import `motion/react`）
- 将 `@ant-design/icons` 从根 dependencies 移除（源码未直接引用）
- 将以下构建工具从根 dependencies 移到 devDependencies：`vite, @vitejs/plugin-react, @tailwindcss/vite, tw-animate-css`
- 修改 `electron-builder.yml` extraResources 指向 `server/node_modules`：
  ```yaml
  extraResources:
    - from: electron/bin/node.exe
      to: node.exe
    - from: server-dist
      to: server-dist
    - from: server/node_modules
      to: node_modules
  ```
- 修改 `scripts/build-package.js`：不再 `npm prune --production`，改为 `cd server && npm install --production`

**影响文件**：
- `package.json` — 依赖重组
- `server/package.json` — 新建
- `electron-builder.yml` — extraResources 改指向
- `scripts/build-package.js` — 重写构建逻辑

**预期节省**：~230 MB（移除 antd 53M + lucide-react 40M + @ant-design 31M + vite 13M + emoji-picker-data 30M + react-dom 7M + motion/framer-motion ~7M + 其他前端库 ~50M + 构建工具 ~20M）

### 1.2 修剪 Chromium 语言包（节省 ~45 MB）

`release/win-unpacked/locales/` 含 55 个 .pak 文件共 48 MB，仅需 zh-CN 和 en-US。

**步骤**：
- 在 `electron-builder.yml` 添加：
  ```yaml
  electronLanguages:
    - zh-CN
    - en-US
  ```

**影响文件**：`electron-builder.yml`

### 1.3 修剪 emoji-picker-element-data 语言包（节省 ~26 MB）

含 28 个语言包共 30 MB，仅需 en（2.9M）和 zh（1.6M）。

**步骤**：
- 在 `scripts/build-package.js` 添加清理步骤，删除除 en/zh 外的所有语言包目录

**影响文件**：`scripts/build-package.js`

### 1.4 清理 node_modules 非运行时文件（节省 ~30-50 MB）

每个 npm 包包含 .d.ts、.map、.md、test/ 等开发文件，运行时不需要。

**步骤**：
- 在 `scripts/build-package.js` 添加清理函数，遍历 `server/node_modules` 删除：
  `.d.ts, .map, .md, test/, tests/, __tests__/, docs/, .github/, LICENSE*, CHANGELOG*`

**影响文件**：`scripts/build-package.js`

### 1.5 修复构建脚本可靠性

当前 `build-package.js` 使用 `npm prune --production` + 构建 + `npm install`（finally）模式，如果中途崩溃会导致 node_modules 状态不一致。

**步骤**：
- 重写为「暂存区」模式：在 `server/` 目录独立安装生产依赖，不修改根 node_modules
- 修正 `package.json` 中 `electron` 版本从 `"^42.0.1"` 改为 `"42.0.1"`（精确版本，确保 prune 后版本一致）

**影响文件**：
- `scripts/build-package.js` — 重写
- `package.json` — electron 版本精确化

### Phase 1 预期结果
- 打包体积：1.4 GB → ~900 MB（解压），557 MB → ~380 MB（压缩）
- node_modules：959 MB → ~350 MB
- 构建过程不再有 node_modules 状态不一致风险

---

## Phase 2: 代码与架构优化（次优先级）

### 2.1 提取 SSE 流式逻辑为自定义 Hook（App.tsx 从 889 行降至 ~580 行）

**问题**：`handleSendMessage` 和 `handleRegenerate` 各含 ~150 行近乎相同的 SSE 回调代码（onToken, onParsed, onReasoning, onDone, onToolCall, onToolResult, onStepStart, onError）。

**方案**：
- 创建 `src/hooks/useStreamingChat.ts`
- 封装 SSE 回调连接、messagesCacheRef 模式、currentConversationIdRef 守卫、abortControllersRef 跟踪、reasoningSegments 状态管理
- Hook 导出 `startChat()` / `startRegenerate()` / `stopGeneration()`
- App.tsx 调用 Hook，删除两段重复 SSE 代码

**影响文件**：
- `src/hooks/useStreamingChat.ts` — 新建
- `src/App.tsx` — 重构

### 2.2 提取 chat.ts 共享逻辑为 ChatService（从 ~770 行降至 ~200 行）

**问题**：`/completions` 和 `/regenerate` 路由共享 ~300 行相同逻辑（加载上下文、构建 prompt、SSE 设置、工具构造、消息保存）。

**方案**：
- 创建 `server/src/services/ChatService.ts`，提取共享函数：
  `loadChatContext, buildSystemPrompt, setupSSE, buildTools, saveAssistantMessage, handleAbortSave`
- 各路由变成 ~50 行的薄编排层
- `/upload-file` 路由保持不变

**影响文件**：
- `server/src/services/ChatService.ts` — 新建
- `server/src/routes/chat.ts` — 重构

### 2.3 统一 CSS 变量系统

**问题**：`tokens.css`（旧 `--color-*` 系统）与 `design-tokens.css`（新 `--sidebar-bg/--accent` 系统）并存。旧系统仅被 3 处引用（StreamingMarkdown.tsx, global.css, animations.css），新系统被 18 个组件广泛使用。

**方案**：
- 将 StreamingMarkdown.tsx 中 3 个 `--color-*` 引用迁移到 `--*` 对应变量
- 将 global.css 和 animations.css 中的旧变量迁移到新系统
- 删除 tailwind.css 中"旧版"映射区块
- 从 global.css 移除 tokens.css 导入，删除 tokens.css

**影响文件**：
- `src/styles/tokens.css` — 删除
- `src/styles/global.css` — 修改导入
- `src/styles/tailwind.css` — 移除旧版区块
- `src/styles/animations.css` — 变量迁移
- `src/components/shared/StreamingMarkdown.tsx` — 变量迁移

### 2.4 替换内联 hover 事件为 CSS/Tailwind

**问题**：31 处 `onMouseEnter/onMouseLeave` + 内联 `style` 操作，绕过了 Tailwind 设计系统。

**方案**：
- 所有颜色变化 hover：用 `hover:text-accent`, `hover:bg-accent-dim` 等 Tailwind 类替代
- 所有背景变化 hover：用 `hover:bg-[var(--accent-dim)]` 替代
- 删除对应的 onMouseEnter/onMouseLeave 事件处理函数

**影响文件**：`src/components/ChatArea.tsx`, `src/components/Sidebar.tsx`, `src/components/AssistantModal.tsx`, `src/components/shared/ToolCallBlock.tsx`

### 2.5 Vite 代码分割（按需加载重型库）

**方案**：
- 在 `vite.config.ts` 添加 `build.rollupOptions.output.manualChunks`：
  ```ts
  manualChunks: {
    'antd': ['antd'],
    'markdown': ['react-markdown', 'remark-gfm'],
  }
  ```
- EmojiPicker 组件改为 `lazy()` 按需加载

**影响文件**：`vite.config.ts`

### 2.6 删除死代码 AppContext

**问题**：`src/store/AppContext.tsx` 定义了完整的 useReducer 状态管理，但从未被任何组件引用。

**方案**：删除文件

**影响文件**：`src/store/AppContext.tsx` — 删除

---

## Phase 3: 性能优化（远期，低优先级）

### 3.1 消息分页加载

将 `SELECT * FROM messages WHERE conversation_id = ?` 改为 `LIMIT ? OFFSET ?`，前端实现"向上滚动加载更多"。

### 3.2 记忆向量搜索优化

- 将嵌入存储从 JSON 字符串改为 Float32Array Buffer（减少解析开销）
- 添加嵌入缓存 Map，避免每次查询重复 JSON.parse
- 长期考虑 sqlite-vec 扩展替代 JS 侧余弦相似度计算

---

## 验证方式

### Phase 0 验证
1. `npm run dev:electron` 开发模式正常运行（import 模式正确）
2. `npm run build` 打包成功，exe 在另一台 Windows 机器上正常运行

### Phase 1 验证
1. `npm run build` 构建成功，检查 `release/win-unpacked/resources/` 体积是否降到 ~900 MB
2. 运行打包后的 exe，确认后端 node.exe 能正确加载 server/node_modules 中的依赖
3. 确认前端页面正常渲染（antd、lucide-react 等来自 Vite bundle）
4. 确认聊天、记忆、桌宠等核心功能正常工作

### Phase 2 验证
1. `npm run dev:electron` 开发模式正常运行
2. SSE 流式聊天和重新生成功能正常
3. 所有 hover 交互视觉效果一致
4. CSS 主题切换正常（dark/light）
5. `npm run build` 打包成功且功能正常

### Phase 3 验证
1. 长对话滚动加载正常
2. 记忆检索结果与优化前一致