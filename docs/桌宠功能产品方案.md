# 桌宠功能产品方案

> 基于 Codex Pet 社区标准，为 aichat 桌面客户端添加可导入社区素材的桌宠伴侣。

---

## 一、核心命题

AI 编程/对话工具正在从"纯文本终端"向"有温度的桌面伴侣"演进。Codex Pets（2026年5月上线）验证了这个方向——上线数周就积累了 750+ 社区宠物，并催生了 OpenPet、CodeWalkers、Copiwaifu 等衍生项目。

**我们的机会**：aichat 正在做 Electron 客户端重构（见产品方案 v2），这是集成桌宠的最佳时机。直接兼容 Codex Pet 格式，零成本接入社区素材生态。

---

## 二、核心设计决策

### 2.1 走 Codex 格式，不自己发明轮子

```
Codex 社区已沉淀 750+ 宠物 → 用户直接导入 → 零制作成本
自己发明格式 → 0 个宠物 → 用户需从头制作 → 冷启动死亡螺旋
```

**格式标准**：`pet.json` + `spritesheet.webp`

```
~/.aichat/pets/<pet-name>/
├── pet.json          # 动画元数据（行号→状态名、帧数、帧时长）
└── spritesheet.webp  # 8列×9行精灵图集，每格 192×208px
```

### 2.2 兼容的社区素材源

| 来源 | 类型 | 接入方式 |
|------|------|---------|
| `petdex.crafter.run` | 社区 Petdex 画廊 | URL 一键导入 |
| `codex-pets.net` | Codex 官方分享站 | URL 导入 |
| `awesome-codex-pet` | GitHub 精选仓库 | curl 脚本安装 |
| 本地文件 | 用户自制/下载的 .webp + .json | 拖拽导入 |

### 2.3 参考项目对标

| 项目 | 我们参考什么 | 我们差异 |
|------|------------|---------|
| **Codex Pets** | pet.json 格式标准、9 状态动画行 | 不绑定 Codex CLI，独立运行 |
| **OpenDesign** | 开源 host 实现、Web UI 集成方式 | 不依赖 Codex Skills 系统 |
| **CodeWalkers** | Tauri 穿透方案、气泡交互 | 我们用 Electron（与 v2 统一） |
| **masko-code** | 权限审批气泡交互 | 更轻量，不依赖外部状态机 |

---

## 三、产品功能设计

### 3.1 宠物生命周期

```
导入 → 唤醒 → 常驻桌面 → 状态联动 → 交互 → 收起/休眠
```

#### 导入（Import）

```
┌─────────────────────────────────────────┐
│  添加桌宠                                │
│                                         │
│  ┌─────────────────────────────────────┐│
│  │  拖拽 spritesheet.webp + pet.json  ││
│  │  或粘贴社区链接（petdex.crafter.run）││
│  └─────────────────────────────────────┘│
│                                         │
│  ── 或 ──                               │
│                                         │
│  [浏览社区画廊]  [从本地文件选择]         │
│                                         │
│  ┌─────────────────────────────────────┐│
│  │ 🐾 预览区域                         ││
│  │     (idle 动画循环播放)              ││
│  └─────────────────────────────────────┘│
│                                         │
│  宠物名称: [_______________]            │
│                                         │
│  [取消]  [导入]                          │
└─────────────────────────────────────────┘
```

**导入流程**：
1. 用户提供 pet.json + spritesheet.webp（两个文件），或粘贴社区 URL
2. 系统做基础校验：spritesheet 尺寸（1536×1872）、json 结构合法性、webp 格式
3. 预览区展示 idle 动画（8 帧循环播放）
4. 确认后复制到 `~/.aichat/pets/<name>/` 并注册

**从社区 URL 导入**：
- 输入 `https://petdex.crafter.run/pets/<slug>` 或 `https://codex-pets.net/share/<id>`
- 后端（或 Electron 主进程）下载两个文件，校验后存入本地
- 同样走预览→确认流程

#### 管理

```
设置 → 桌宠
├── 已安装宠物列表（卡片网格）
│   ├── 🐾 Dewey       [预览] [唤醒] [删除]
│   ├── 🔥 Fireball    [预览] [唤醒] [删除]
│   └── ...
├── [导入新宠物] 按钮
└── 全局设置
    ├── 默认宠物: [下拉选择]
    ├── 启动时自动唤醒: [开关]
    ├── 宠物缩放: [0.5x ~ 2x]
    └── 位置: [屏幕右下] [屏幕左下] [自定义]
```

### 3.2 状态机 & 动画映射

基于调研，定义最少 6 个核心状态 + 2 个可选状态：

```
                    ┌──────────┐
                    │   IDLE   │ ← 默认态：呼吸浮动、眨眼、眼神追踪鼠标
                    └────┬─────┘
         用户开始输入      │      任务完成/超时无交互
              ┌───────────┼───────────┐
              ▼           │           ▼
      ┌────────────┐      │    ┌──────────┐
      │ ATTENTION  │      │    │ SUCCESS  │ ← 完成：庆祝跳跃、撒花粒子
      └─────┬──────┘      │    └────┬─────┘
            │             │         │
      AI 开始推理         │         └────→ IDLE
            ▼             │
      ┌────────────┐      │
      │  THINKING  │ ← 思考中：皱眉、来回踱步、省略号气泡
      └─────┬──────┘
            │
    ┌───────┴───────┐
    ▼               ▼
┌──────────┐  ┌──────────┐
│TOOL_USE  │  │GENERATING│ ← 可选：工具调用 / 文本生成（共用 working 动画）
└────┬─────┘  └────┬─────┘
     │              │
     └──────┬───────┘
            ▼
     ┌──────────┐
     │  ERROR   │ ← 出错：沮丧冒烟、但保持助手姿态
     └──────────┘
```

**Codex 9 行动画状态 → 我们的状态映射**：

| Codex 动画行 | 我们映射的状态 | 说明 |
|-------------|-------------|------|
| `idle` (行1) | **IDLE** | 待机呼吸，默认循环 |
| `waving` (行2) | **ATTENTION** | 用户开始输入时打招呼 |
| `running-right` (行3) | — | 保留给后续漫游模式 |
| `running-left` (行4) | — | 保留给后续漫游模式 |
| `jumping` (行5) | **SUCCESS** | 任务完成时庆祝跳 |
| `waiting` (行6) | **WAITING** | 需要用户确认/权限审批 |
| `review` (行7) | **THINKING** | AI 推理/审查中 |
| `failed` (行8) | **ERROR** | 出错时播放 |
| (行9) | — | 扩展预留 |

### 3.3 交互设计

#### 桌面层交互

| 交互 | 行为 | 说明 |
|------|------|------|
| **鼠标悬停** | 宠物看向鼠标 / 轻微反应 | 像素级碰撞检测，仅非透明区域响应 |
| **单击宠物** | 弹出快捷菜单（切换会话/查看状态/收起） | 不打开主窗口 |
| **双击宠物** | 呼出 aichat 主窗口 | 快速回到对话 |
| **拖拽宠物** | 移动位置 | 吸附到屏幕边缘（磁吸 20px） |
| **点击穿透** | 透明区域点击直达下方应用 | 不干扰工作流 |
| **右键** | 上下文菜单（切换宠物/设置/休眠） | |
| **拖入文件** | 触发文件上传到当前会话 | 与 aichat 对话联动 |

#### 气泡交互（核心价值）

参考 Codex/OpenDesign 的气泡双向通信：

```
┌──────────────────────────┐
│  🐾                      │
│   ┌──────────────────┐   │
│   │ 任务完成！生成     │   │
│   │ 了 3 个设计稿，    │   │
│   │ 要查看吗？         │   │
│   └──────────────────┘   │
│   点击气泡查看 →         │
└──────────────────────────┘
```

- **状态气泡**：THINKING 时显示"思考中..."，SUCCESS 时显示摘要
- **可点击气泡**：点击直接展开对话 / 跳转到 aichat 主窗口对应会话
- **权限请求**：需要用户确认时，气泡显示操作按钮（批准/拒绝）
- **自动消失**：无操作 10 秒后气泡淡出，宠物回归 idle

#### 渐进式 Idle 行为

参考 Clawd-on-Desk 的设计：

| 时间 | 行为 |
|------|------|
| 0-30s | 正常 idle（呼吸 + 眨眼 + 眼追鼠标） |
| 30s-2min | 随机小动作（挠头、东张西望） |
| 2min-5min | 打哈欠、伸懒腰 |
| 5min+ | 打盹/睡觉（💤 气泡），点击唤醒 |

### 3.4 与 AI 对话联动的状态触发

```
后端 hook 事件 → IPC → 渲染进程 → 状态机 → 动画切换

事件映射:
  session:user-input-start   → ATTENTION
  session:ai-thinking-start  → THINKING
  session:tool-call-start    → WORKING (可选)
  session:tool-call-end      → THINKING (回到推理)
  session:response-start     → IDLE (流式输出中保持活跃)
  session:response-complete  → SUCCESS (3.5秒后回到 IDLE)
  session:error              → ERROR (3.5秒后回到 IDLE)
  session:permission-needed  → WAITING (直到用户响应)
  session:idle-timeout-60s   → SLEEP (进入休眠动画)
```

- 后端 Express 已有流式对话基础设施，在关键节点 emit 事件即可
- Electron 主进程转发 IPC 事件到渲染进程（pet overlay window）
- 状态机在渲染进程独立运行，不依赖后端持续连接

---

## 四、技术方案概览

### 4.1 架构

```
┌──────────────────────────────────────────────────────────┐
│                     Electron 主进程                        │
│                                                          │
│  ┌────────────┐  ┌────────────┐  ┌───────────────────┐  │
│  │ 窗口管理器   │  │ IPC 总线   │  │ 系统托盘           │  │
│  │            │  │            │  │ - 最小化到托盘      │  │
│  │ 生命周期    │  │ 事件转发   │  │ - 彻底退出         │  │
│  │ 全屏检测    │  │ 状态同步   │  │                    │  │
│  │ 屏幕跟随    │  │            │  │                    │  │
│  └─────┬──────┘  └─────┬──────┘  └─────────┬─────────┘  │
│        │               │                   │             │
│        ├───────────────┼───────────────────┤             │
│        │               │                   │             │
│  ┌─────▼──────┐  ┌─────▼──────┐           │             │
│  │ 主窗口      │  │ 宠物窗口    │           │             │
│  │ (aichat UI)│  │ (Overlay)  │           │             │
│  │            │  │            │           │             │
│  │ React App  │  │ PetRenderer│           │             │
│  │ 1200×800   │  │ 透明无边框  │           │             │
│  │ 可最大化    │  │ 192×208    │           │             │
│  │            │  │            │           │             │
│  │ 设置页含    │  │ z-order:   │           │             │
│  │ PetSettings │  │ "floating" │           │             │
│  └────────────┘  │ 穿透+检测   │           │             │
│                  └────────────┘           │             │
└──────────────────────────────────────────────────────────┘

层级示意：
  系统 UI 层    ████████████████████████  (开始菜单/通知/输入法)
  ───────────────── floating 天花 ─────────
  宠物层        ▓▓▓▓▓▓▓▓▓▓  ← 我们
  置顶工具层    ░░░░░░░░░░░░░░  (任务管理器/截图)
  普通窗口层    ░░░░░░░░░░░░░░  (aichat 主窗口/浏览器/IDE)
```

- 宠物使用**独立的透明 BrowserWindow**（不是主窗口内的 div），与主窗口**无父子关系**
- 宠物窗口尺寸 = spritesheet 单帧尺寸 × 缩放比（默认 192×208 × 1.0）
- z-order 使用 `"floating"` 级别——所有普通窗口之上，系统 UI 和输入法之下
- 点击穿透：Canvas 逐像素 Alpha 检测 → 透明像素处 `setIgnoreCursorEvents(true)`
- 全屏检测：主进程 500ms 轮询前台窗口尺寸和样式

### 4.2 悬浮窗管理策略

> 本节定义宠物 overlay 窗口在桌面环境中的行为规则，是桌宠功能**最容易出问题**的部分。核心原则：**宠物常驻可见，但不干扰用户工作流。**

#### 4.2.1 窗口层级设计

宠物窗口的 z-order 级别选择 **Electron `"floating"` 级别**（对应 Windows `WS_EX_TOPMOST` 中等层级）：

```
屏幕最顶层
  ↑ 系统级 UI（开始菜单、通知中心、UAC、锁屏、输入法候选窗）
  ↑ ─────────── floating 天花板 ───────────
  ↑ 宠物浮窗                                     ← 我们在这层
  ↑ 置顶窗口（任务管理器、截图工具等）
  ↑ 普通窗口（aichat 主窗口、浏览器、IDE、VS Code）
桌面最底层
```

| 层级 | 窗口类型 | 宠物应处位置 |
|------|---------|------------|
| 系统 UI | 开始菜单、通知中心、锁屏、UAC | **宠物在下**（天然保证，OS 级） |
| 输入法候选窗 | IME candidate list | **宠物在下**（`floating` 级别天然低于 IME 窗口） |
| 置顶工具 | 任务管理器、Snipaste 标注 | **宠物在下**（`floating` < `torn-off-menu`） |
| aichat 主窗口 | 普通/最大化 | **宠物在上** ✅ |
| 其他应用 | 浏览器、IDE、文件管理器 | **宠物在上** ✅ |

**为什么不用 `screen-saver` 级别？**
`screen-saver` 是 Electron 能设置的最高级别，会覆盖系统 UI 包括输入法候选窗，反而造成干扰。`floating` 恰好在"所有普通窗口之上，系统 UI 之下"，是桌宠的甜点层级。

#### 4.2.2 与 aichat 主窗口的生命周期绑定

aichat 客户端有三种运行状态，宠物行为不同：

| aichat 状态 | 触发方式 | 宠物行为 |
|------------|---------|---------|
| **正常运行** | 启动 / 从托盘恢复 | 宠物显示，跟随主窗口所在屏幕 |
| **最小化到托盘** | 点击关闭按钮（最小化到托盘） | 宠物**继续悬浮**，保持桌面常驻 |
| **彻底退出** | 托盘菜单 → 退出 | 宠物**跟随关闭**，清理窗口资源 |

```
用户操作流：

启动 aichat
  ├── 主窗口打开 → 宠物窗口创建并显示
  │
  ├── 用户点击主窗口 ✕（关闭到托盘）
  │   ├── 主窗口隐藏
  │   └── 宠物继续显示 ✅  ← 桌面常驻陪伴
  │
  ├── 用户双击托盘图标 → 主窗口恢复
  │
  └── 用户右键托盘 → 退出
      ├── 主窗口关闭
      └── 宠物窗口关闭  ← 彻底退出
```

**设计理由**：最小化到托盘是用户"暂时不用 aichat"，但 AI 可能在后台继续工作（处理长任务、等待工具调用结果等），宠物此时继续指示状态仍有价值。彻底退出才是用户明确表示"不使用了"。

#### 4.2.3 定位策略

**默认位置**：屏幕右下角，距边缘 20px 内边距。

```
┌──────────────────────────────────┐
│                                  │
│          屏幕 workarea           │
│                                  │
│                        ┌──┐     │
│                        │🐾│ ← 20px
│                        └──┘     │
│                         20px    │
└──────────────────────────────────┘
```

| 规则 | 说明 |
|------|------|
| **初始位置** | 跟随 aichat 主窗口所在屏幕，默认右下角，距右/下边缘各 20px |
| **位置记忆** | 用户拖拽后记住位置，存储到本地（`pet-position.json`），下次启动恢复 |
| **屏幕跟随** | 当 aichat 主窗口拖到另一个屏幕，宠物自动迁移到该屏幕（保持相对位置比例） |
| **磁吸** | 手动拖拽松手时，距屏幕边缘 ≤20px 自动吸附 |
| **边界约束** | 宠物始终在屏幕 workarea 内（排除任务栏区域），拖动时不可超越边界 |
| **坐标基准** | 使用 `screen.workArea` 而非 `screen.bounds`，避免遮挡任务栏/托盘 |

**跨屏幕迁移逻辑**：

```
主窗口从屏幕A移到屏幕B：
  1. 检测主窗口所在 screen.id 变化
  2. 计算宠物在主窗口屏幕中的相对位置百分比
     xRatio = pet.x / screenA.workArea.width
     yRatio = pet.y / screenA.workArea.height
  3. 映射到屏幕B对应位置
     pet.x = screenB.workArea.width * xRatio
     pet.y = screenB.workArea.height * yRatio
  4. 如果屏幕B尺寸不同导致越界，clamp 到 workarea 内
```

**显示器热插拔**：拔掉外接显示器时，如果宠物在已移除的屏幕上，立即迁移到主窗口当前所在屏幕的右下角。

#### 4.2.4 全屏检测与自动隐藏

这里的"全屏"指**独占全屏应用**（游戏、全屏视频播放器、全屏演示），不是 aichat 主窗口最大化。

**判定规则**（500ms 轮询）：

```
判定为全屏的条件（同时满足）：
  1. 前台窗口 != aichat 主窗口
  2. 前台窗口 != 宠物窗口
  3. 前台窗口尺寸 == 该显示器分辨率（宽高误差 ≤2px）
  4. 前台窗口无边框（WS_POPUP 样式）或无标题栏

特殊排除（以下不算全屏，不隐藏）：
  - aichat 自己的窗口（最大化时比宠物低，z-order 已保证）
  - 桌面本身（explorer.exe 的 Progman 窗口）
  - 任务管理器（小尺寸窗口）
```

**检测周期**：500ms 轮询，检测到变化时立即响应。

| 检测到的事件 | 宠物行为 |
|------------|---------|
| 前台窗口进入全屏 | 宠物 hide()，记录隐藏原因 |
| 前台窗口退出全屏 | 宠物 show()，恢复之前的位置和动画 |
| 宠物自己被判定为前台（误判保护） | 忽略，不触发隐藏 |
| aichat 主窗口被最小化 | 不隐藏宠物（最小化≠全屏） |
| 锁屏（Win+L） | 系统自动处理，宠物不可见 |
| UAC 安全桌面 | 系统自动隔离，宠物不可见 |

**性能优化**：宠物隐藏期间，停止 Canvas 渲染循环（`cancelAnimationFrame`），释放 GPU 资源。

#### 4.2.5 场景冲突处理矩阵

| 场景 | 宠物表现 | 处理机制 |
|------|---------|---------|
| **aichat 窗口最大化** | 宠物悬浮在主窗口之上，正常显示 | z-order `floating` 天然保证 |
| **aichat 最小化到托盘** | 宠物继续显示（桌面常驻） | 独立生命周期 |
| **aichat 彻底退出** | 宠物跟随关闭 | 主进程统一管理 |
| **浏览器全屏视频（F11/YouTube）** | 宠物自动隐藏 | 全屏检测命中 → hide |
| **IDE 全屏模式（VS Code F11）** | 宠物自动隐藏 | 全屏检测命中 → hide |
| **游戏独占全屏** | 宠物自动隐藏 | 全屏检测命中 → hide |
| **输入法候选窗弹出** | 宠物不遮挡（候选窗在宠物之上） | `floating` 级别天然低于 IME |
| **网页下拉菜单** | 偶尔遮挡极少部分 | 宠物尺寸小（192×208）、位置在右下角，一般碰不到 |
| **截图工具（微信/Snipaste）** | 宠物可能被截进图里 | 不影响截图工具本身功能；用户可手动隐藏 |
| **远程桌面 / 虚拟机** | 正常显示 | 远程桌面窗口不全屏时不触发隐藏 |
| **Windows 自动隐藏任务栏** | 宠物不挡任务栏弹出区域 | 使用 workarea 坐标，20px 底部边距 |
| **多显示器，主屏全屏，副屏正常** | 主屏宠物隐藏，但宠物已迁到副屏（跟随主窗口） | 屏随主窗口逻辑 |
| **投影/外接显示器演示** | 如果演示软件全屏则隐藏，否则正常 | 全屏检测覆盖 PPT 演示模式 |

---

### 4.3 核心依赖

项目已有 `motion`（原 framer-motion），可直接复用：
- 窗口级动画（宠物移动、弹跳）
- 气泡淡入淡出
- 撒花粒子效果

需新增：
- 精灵图渲染组件（轻量，自研 ~200 行，基于 Canvas 2D）
- pet.json 解析器（~50 行）

### 4.4 pet.json 标准结构

```json
{
  "name": "dewey",
  "displayName": "Dewey",
  "description": "A tidy duck for calm workspace days",
  "version": "1.0.0",
  "sprite": {
    "url": "spritesheet.webp",
    "width": 192,
    "height": 208,
    "columns": 8,
    "rows": 9
  },
  "animations": {
    "idle":        { "row": 0, "frames": 8, "fps": 6 },
    "waving":      { "row": 1, "frames": 8, "fps": 8 },
    "runningRight":{"row": 2, "frames": 8, "fps": 10 },
    "runningLeft": { "row": 3, "frames": 8, "fps": 10 },
    "jumping":     { "row": 4, "frames": 8, "fps": 8 },
    "waiting":     { "row": 5, "frames": 8, "fps": 6 },
    "review":      { "row": 6, "frames": 8, "fps": 6 },
    "failed":      { "row": 7, "frames": 8, "fps": 6 }
  }
}
```

> 注意：Codex 官方 spritesheet 有 9 行但第 9 行尚未定义动画。我们做解析时兼容 8 行和 9 行两种格式。

---

## 五、用户故事

### 故事 1：从社区导入第一个宠物

> 小李在 petdex.crafter.run 看到了一个可爱的猫娘宠物，复制了分享链接。打开 aichat 设置 → 桌宠 → 导入，粘贴链接，预览区立刻播放 idle 动画。点击导入，3 秒后宠物出现在桌面右下角。他开始输入问题，宠物立刻切换到 ATTENTION 状态看向他。

### 故事 2：等待回复时的陪伴

> 小王问了一个复杂问题，AI 开始推理。桌宠切换到 THINKING 状态——皱眉、来回踱步、头顶冒出省略号气泡。8 秒后 AI 调用了搜索工具，宠物切换到 WORKING 状态（翻书动画）。又过了 5 秒，回复开始流式输出。完成后宠物跳起来撒花（SUCCESS），气泡显示"搞定！"。小王双击气泡，主窗口展开显示完整回复。

### 故事 3：权限审批不切窗口

> AI 准备执行一个需要确认的操作。桌宠切换到 WAITING 状态——头顶冒出气泡："需要修改 3 个文件，确认吗？[批准] [拒绝]"。小王正在 VS Code 里看代码，直接点击气泡上的"批准"，没有切回 aichat 窗口。宠物高兴地跳了一下，回到 THINKING 状态。

### 故事 4：桌面陪伴的长尾价值

> 小张写代码写了 2 小时，中间没跟 AI 对话。桌宠在屏幕右下角安静陪伴：30 秒挠挠头，2 分钟打哈欠，5 分钟后蜷起来打盹（💤）。他偶尔瞥一眼，觉得有个小东西在桌面上挺治愈。

---

## 六、实施路线

### 阶段 0：先决条件
- Electron 客户端搭建完成（产品方案 v2 阶段 0）
- 主窗口 + IPC 通信 infrastructure 就绪

### 阶段 1：核心渲染 + 悬浮窗基础（宠物能显示、能管理）

**渲染引擎**：
- [ ] `PetLoader`：读取 pet.json，校验 spritesheet（尺寸、json 结构、webp 格式）
- [ ] `SpriteRenderer`：Canvas 2D 逐帧绘制，支持指定行号 + 循环/单次播放 + FPS 控制
- [ ] 缩放支持：0.5x ~ 2x 渲染缩放

**悬浮窗壳**：
- [ ] 独立透明 BrowserWindow（`transparent: true`, `frame: false`, `resizable: false`）
- [ ] z-order 设为 `"floating"` 级别
- [ ] 窗口尺寸 = spritesheet 单帧 × 缩放（默认 192×208）
- [ ] 像素级穿透：Canvas Alpha 检测 + `setIgnoreCursorEvents(true)`
- [ ] 默认位置：跟随主窗口所在屏幕右下角（workarea 基准，距边 20px）
- [ ] 拖拽移动 + 磁吸（20px 阈值吸附屏幕边缘）
- [ ] 位置记忆：存储到本地，下次启动恢复
- [ ] 屏幕跟随：主窗口跨屏时宠物自动迁移

**生命周期**：
- [ ] aichat 启动 → 宠物创建显示
- [ ] aichat 关闭到托盘 → 宠物继续显示
- [ ] aichat 托盘退出 → 宠物关闭
- [ ] 显示器热插拔 → 位置重新校准

**导入 & 管理**：
- [ ] 导入流程：本地文件选择 → 校验 → 复制到 `~/.aichat/pets/` → 注册
- [ ] 设置页：已安装列表（卡片网格 + 预览 + 删除）、默认宠物选择
- [ ] 缩放滑块（0.5x ~ 2x）

**产出**：宠物在桌面右下角显示，可在窗口间正常悬浮，与 aichat 生命周期联动

### 阶段 1.5：全屏检测与冲突规避（宠物不碍事）
- [ ] 主进程 500ms 轮询：前台窗口尺寸 + 样式检测
- [ ] 全屏判定逻辑（前台窗口=显示器分辨率 且 非 aichat/非桌面/非任务管理器）
- [ ] 全屏→hide() + 停止 Canvas 渲染循环
- [ ] 退出全屏→show() + 恢复渲染
- [ ] 边界测试：YouTube 全屏、游戏、PPT 演示、IDE F11 全屏

**产出**：全屏时自动隐藏，不干扰游戏/视频/演示

### 阶段 2：状态联动（宠物能反应）
- [ ] 状态机实现（idle → attention → thinking → success → error）
- [ ] 后端 hook 事件 → IPC → 渲染进程事件流
- [ ] 气泡组件（状态消息 + 操作按钮）
- [ ] 渐进式 idle 行为

**产出**：宠物能根据 AI 状态切换动画

### 阶段 3：交互（宠物能互动）
- [ ] 鼠标悬停/点击/双击/拖拽
- [ ] 像素级穿透检测
- [ ] 右键菜单
- [ ] 气泡点击 → 主窗口联动
- [ ] 拖拽文件到宠物 → 触发上传

**产出**：用户可与宠物互动

### 阶段 4：社区导入（生态打通）
- [ ] URL 导入（petdex.crafter.run + codex-pets.net）
- [ ] 社区画廊浏览（内嵌 iframe 或 API 拉取列表）
- [ ] 一键安装脚本兼容
- [ ] 缩放、位置、默认宠物等偏好设置

**产出**：零制作成本，直接享用社区 750+ 宠物

---

## 七、风险

| 风险 | 影响 | 应对 |
|------|------|------|
| Codex 格式未来变更 | 已导入宠物不可用 | 解析器做向后兼容，关注 openai/skills CHANGELOG |
| Electron 透明窗口性能 | 低配机卡顿 | Canvas 渲染本身就轻量；加 FPS 限制（30fps idle）；全屏隐藏时停止渲染 |
| 透明穿透在 Windows/Mac 表现不一致 | Mac 穿透失效 | CodeWalkers 已验证 `rgba(255,255,255,0.01)` 方案 |
| 社区素材质量参差不齐 | 导入后显示异常 | 导入时做严格校验（尺寸、json 结构、webp 格式） |
| 全屏检测误判 | 正常窗口被误判为全屏导致宠物意外隐藏 | 排除规则（aichat 自己、桌面、任务管理器）；加手动唤醒按钮 |
| `floating` 级别在不同 OS 表现差异 | Linux 上部分 DE 不遵循 z-order 层级 | 优先支持 Windows，macOS 和 Linux 作为渐进支持 |
| 多显示器 + 热插拔边缘情况 | 宠物跑出屏幕边界 | 每次显示器变更时重新 clamp 到最近屏幕 workarea 内 |
| 宠物窗口被安全软件误杀 | 功能不可用 | 不涉及系统文件/网络抓包，风险低 |

---

## 八、竞品差异化

| | Codex Pets | OpenDesign | CodeWalkers | **aichat Pet** |
|------|-----------|-----------|-----------|----------------|
| 格式 | Codex 原生 | 兼容 Codex | 自定义 | **兼容 Codex** |
| 社区素材 | 750+ | 复用 Codex | 无 | **750+ 直接导入** |
| AI 联动 | 仅 Codex | 多 agent | 多 CLI | **多模型+所有会话** |
| 穿透交互 | 原生 | Electron | Tauri | **Electron（与 v2 统一）** |
| 气泡审批 | ✅ | ✅ | ❌ | **✅ 气泡+按钮** |
| 开源 | ❌ | Apache 2.0 | 开源 | **待定** |

---

## 九、与 v2 客户端重构的配合

桌宠功能天然依赖 Electron 桌面环境（独立 BrowserWindow + 透明窗口 IPC），但核心逻辑应该与 UI 框架解耦：

- `PetRenderer`（Canvas 精灵图渲染）→ 纯 TypeScript，不依赖 React
- `PetStateMachine`（状态机）→ 纯逻辑，可单测
- `PetLoader`（格式解析校验）→ 纯逻辑，可单测
- `PetOverlay`（Electron 窗口壳）→ 仅 Electron 环境使用
- `PetSettings`（设置页 UI）→ React 组件，在主窗口渲染

这样即使将来不用 Electron，核心逻辑也可移植到 Tauri 或 Web。
