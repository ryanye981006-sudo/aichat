# 产品方案 v2 — 客户端重构

> 产品方向文档，非研发实现方案。执行前再写研发 plan。

## 一、产品形态变更

### 当前形态
- Web 浏览器应用，Vite dev server 在 localhost:3000 运行
- 依赖浏览器窗口，无法脱离浏览器使用

### 目标形态
- **桌面客户端**：Electron 打包，独立窗口运行
- **默认窗口模式**：启动即窗口模式，尺寸约 1200×800，可拖拽缩放
- **全屏切换**：提供全屏按钮，切换至全屏模式（F11 或窗口控件）
- **保留 dev 模式**：开发阶段仍可用 `npm run dev` 在浏览器中调试

### 技术选型建议
- Electron（与现有 Node.js 后端天然融合，学习成本最低）
- 窗口管理：Electron BrowserWindow API
- 打包：electron-builder

---

## 二、功能变更地图

```
功能模块              当前状态            v2 目标          处理方式
──────────────────────────────────────────────────────────────────
RAG 知识库           完整 CRUD + UI      删除              前端先干掉，后端逐步注释→测试→删代码
长期记忆             完整功能            保留              不变
个人信息             独立设置页          合并到"记忆设置"   简化为单输入框（500字限制）
全局记忆             独立设置页          合并到"记忆设置"   仅保留开关，提取内容不可见
模型设置             完整 CRUD + UI      保留              重设计 UI
助手管理             完整功能            保留              仅样式/交互调整
会话管理             完整功能            保留              仅样式/交互调整
对话界面             完整功能            微调              去掉知识库选择按钮
文档上传             依赖 RAG 能力        保留              底层 RAG 管线能力保留
```

---

## 三、各模块详细方案

### 3.1 RAG 知识库 — 删除

**范围**：
- 删除整个知识库管理页面（SettingsArea 中的 rag 标签页）
- 删除 ChatArea 输入框中的知识库选择下拉菜单
- 删除 AssistantModal 中的知识库关联标签页
- 删除 CitationBlock 引用展示组件

**后端保留**（长期记忆和文档上传依赖）：
- `EmbeddingService` — 记忆向量化需要
- `RerankerService` — 记忆检索精排需要
- `MultimodalEmbeddingService` — 文档上传 OCR 后向量化需要
- `KnowledgeService` 的文档加载/分块能力 — 文档上传需要
- `DeepSeekOcrService` — 扫描件识别需要

**后端删除**：
- `routes/knowledge.ts` 中纯粹的 CRUD API
- `KnowledgeService` 中知识库管理部分（保留文档处理管线）
- 知识库相关的数据库迁移（可保留表结构，仅不调用）

**策略**：前端直接删 → 后端逐步注释 + 测试 → 确认无影响后删代码

### 3.2 长期记忆 — 保留

模块完全保留，不做功能变更：
- 三层时间模型（L0/L1/L2）
- 话题切换触发 + 空闲超时触发
- 双通道检索 + RRF 融合 + 五信号加权
- 工具调用方式注入记忆上下文
- 会话隐私模式

### 3.3 个人信息 + 全局记忆 — 合并

**合并为一个设置区域**，暂定名"记忆设置"或"个人偏好"。

#### 个人信息（简化）
```
┌─────────────────────────────────────────┐
│  个人信息                               │
│  ┌─────────────────────────────────────┐│
│  │ 请介绍一下你自己（限制500字）        ││
│  │                                     ││
│  │                                     ││
│  │                                     ││
│  └─────────────────────────────────────┘│
│  已输入 0/500                           │
└─────────────────────────────────────────┘
```
- 单个 textarea，无键值对编辑
- 内容拼接进系统提示词（逻辑同现 `UserProfileService`）
- 后端接口从键值对改为单字段存储

#### 全局记忆（简化）
```
┌─────────────────────────────────────────┐
│  全局记忆                    [开关]     │
│  自动从对话中学习，跨会话复用           │
│  ─────────────────────────────────────  │
│  当前已提取 42 条记忆                   │
│  记忆内容仅在对话中由 AI 自动调用       │
│  不可手动查看或编辑                     │
└─────────────────────────────────────────┘
```
- 仅保留开关（控制 MemoryService 是否工作）
- **不展示**提取的记忆内容列表
- 可以显示记忆条数统计
- 去掉现有的记忆条目列表、搜索、编辑、删除 UI
- 后端保留记忆 CRUD API（调试用），前端不调用

### 3.4 模型设置 — UI 重设计

核心功能保留，重新设计交互：
- **提供商管理**：列表 + 添加/编辑/删除
- **模型管理**：每个提供商下的模型列表，手动添加 / 从 API 获取
- **API 密钥**：显示/隐藏切换
- **模型测试**：单个模型连通性测试
- 整体布局更紧凑，减少层级嵌套

### 3.5 对话界面 — 微调

- 去掉知识库选择按钮（输入框区域）
- 其余交互和功能不变：
  - 消息列表 + Markdown 渲染
  - 思考过程折叠展示
  - 文件附件（图片粘贴 / 拖拽上传）
  - 深度思考模式选择
  - 上下文轮数指示器
  - 发送/停止

### 3.6 助手 & 会话 — 样式交互调整

功能完全不变：
- 助手 CRUD、系统提示词编辑、模型选择、温度/上下文设置、记忆开关
- 会话管理、隐私模式切换

调整范围：
- 侧边栏样式：更紧凑，适配客户端窗口
- 模态框/面板动画：更流畅
- 整体视觉风格保持一致

---

## 四、设置页重构方案

### 当前结构（4 个标签页）
```
设置
├── 模型设置     ← 保留，UI 重设计
├── RAG 知识库   ← 删除
├── 全局记忆     ← 合并入"记忆设置"
└── 个人信息     ← 合并入"记忆设置"
```

### v2 结构（2 个标签页）
```
设置
├── 模型设置     ← 保留，UI 重设计
└── 记忆设置     ← 个人信息 + 全局记忆合并
```

---

## 五、行动步骤

### 第 0 步：Electron 客户端搭建
- 初始化 Electron 壳
- 配置主进程（窗口大小、全屏切换）
- Vite + Electron 开发环境串联
- 验证现有功能在 Electron 中正常运行
- **不涉及任何业务代码改动**

### 第 1 步：删除 RAG 知识库（前端）
- 删除 SettingsArea 中 rag 标签页
- 删除 ChatArea 中知识库选择下拉菜单
- 删除 AssistantModal 中知识库关联标签页
- 删除 CitationBlock 组件
- 删除 knowledgeApi 前端调用
- 删除 store/types 中知识库相关定义
- 验证应用无报错

### 第 2 步：合并个人信息 + 全局记忆
- 将 SettingsArea 的 profile 和 memory 标签页合并为一个
- 个人信息改为单 textarea（500 字限制）
- 全局记忆改为开关 + 统计数字
- 去掉记忆条目列表/搜索/编辑/删除 UI
- 调整后端 profile API（键值对 → 单字段）
- 保留后端 memory CRUD API（前端先不调）

### 第 3 步：模型设置 UI 重设计
- 重新设计模型设置页布局
- 提供商列表 + 模型列表更紧凑
- 保持所有现有功能

### 第 4 步：对话界面微调
- 移除知识库选择按钮
- 调整输入框按钮布局

### 第 5 步：助手 & 会话样式调整
- Sidebar 样式优化
- AssistantModal 样式优化
- 整体适配客户端窗口尺寸

### 第 6 步：后端 RAG 代码逐步清理
- 逐个注释 routes/knowledge.ts 中的端点
- 逐个注释 KnowledgeService 中纯知识库管理方法
- 每次注释后全量测试，确保记忆/文档上传不受影响
- 确认无影响后删除死代码
- 清理不用的依赖（如有）

### 第 7 步：整体测试 & 打包
- 全功能回归测试
- Electron 打包配置
- Windows 安装包生成

---

## 六、风险点

| 风险 | 影响 | 应对 |
|------|------|------|
| RAG 代码与记忆模块耦合 | 删 RAG 误伤记忆 | 第 6 步逐步注释+测试，不直接删 |
| Electron 打包体积过大 | 用户体验差 | 合理配置 electron-builder，排除不必要依赖 |
| 记忆内容不可见后排查困难 | 调试不便 | 后端保留 API，必要时 curl 查看 |
| 文档上传依赖 KnowledgeService 管线 | 删错导致功能缺失 | 先梳理依赖链，明确保留哪些方法 |
