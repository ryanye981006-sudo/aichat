# 研发实施方案

> 基于以上产品方案的技术落地 plan。实施前以此为准。

---

## 一、总览与模块划分

### 新增模块（server）

| 模块 | 文件 | 职责 |
|------|------|------|
| **ConversationChunkService** | `server/src/services/ConversationChunkService.ts` | L0/L1 层：turn 管理、语义切分 chunk、chunk 闭合检测、触发记忆提取 |
| **MemoryRetrievalService** | `server/src/services/MemoryRetrievalService.ts` | L2 层：双通道检索管线 + RRF + Reranker + 五信号加权 |
| **RerankerService** | `server/src/services/RerankerService.ts` | 封装已有硅基流动 rerank API（config.ts 中 siliconflowApiKey/RerankModel 已配置），对外暴露 `rerank(query, documents)` 方法，返回精排分数 |
| **ToolDefinitionBuilder** | `server/src/services/ToolDefinitionBuilder.ts` | 构建 search_memory / recall_context / recall_sources 三个工具的 Zod schema |
| **ToolExecutor** | `server/src/services/ToolExecutor.ts` | 工具调用执行器，分发到 MemoryRetrievalService / ConversationChunkService |
| **UserProfileService** | `server/src/services/UserProfileService.ts` | 用户个人信息 KV CRUD，拼入 system prompt |
| **ConversationIdleDetector** | `server/src/services/ConversationIdleDetector.ts` | 触发 B：定时扫描超 1 小时无新消息的会话 |
| **profile 路由** | `server/src/routes/profile.ts` | GET/PUT/DELETE /api/profile |

### 需修改的模块

| 模块 | 文件 | 变更要点 |
|------|------|---------|
| **chat.ts** | `server/src/routes/chat.ts` | 工具调用循环、隐私模式处理、消息快照字段、移除固定记忆注入 |
| **AiSdkService** | `server/src/services/AiSdkService.ts` | `chatStream` 支持 tools 参数、tool-call/tool-result 事件 |
| **MemoryService** | `server/src/services/MemoryService.ts` | 新增 importance 计算、source_links 写入、status/superseded_by、类型提取 |
| **assistants 路由** | `server/src/routes/assistants.ts` | 全局记忆开关（已有 enable_memory 字段） |
| **conversations 路由** | `server/src/routes/conversations.ts` | 新增 privacy_mode 更新端点 |
| **memory 路由** | `server/src/routes/memory.ts` | 新增编辑/删除/详情/来源端点 |
| **config.ts** | `server/src/config.ts` | 新增检索管线参数 |
| **前端 api.ts** | `src/services/api.ts` | 新增 tool_call/tool_result SSE 事件、profile/memory API |
| **前端 App.tsx** | `src/App.tsx` | toolCalls state、userProfile state |
| **AssistantModal** | `src/components/AssistantModal.tsx` | 全局记忆开关配置 |
| **ChatArea** | `src/components/ChatArea.tsx` | 渲染工具调用消息块 + 会话顶部隐私模式 toggle |
| **SettingsArea** | `src/components/SettingsArea.tsx` | 记忆管理增强 + 个人信息编辑表单 |
| **类型定义** | `src/types/index.ts`, `server/src/types/index.ts` | 新增 ToolCall、MemoryDetail、UserProfile 等 |

---

## 二、数据库迁移

### 迁移 011: 三层时间模型 + 双向链接 + 记忆增强

**messages 表新增字段：**
- `turn_index INTEGER NOT NULL DEFAULT 0` — 第几轮对话（user+assistant 合计算 1 轮）
- `memory_enabled INTEGER NOT NULL DEFAULT 0` — 消息入库时的记忆开关快照，默认不提取（产品方案 §不回溯源则）
- `privacy_mode INTEGER NOT NULL DEFAULT 0` — 消息入库时的隐私模式快照

**新建 conversation_chunks 表：**
```sql
CREATE TABLE conversation_chunks (
  id TEXT PRIMARY KEY,
  conversation_id TEXT NOT NULL REFERENCES conversations(id) ON DELETE CASCADE,
  start_turn_index INTEGER NOT NULL,
  end_turn_index INTEGER NOT NULL,
  content TEXT NOT NULL,               -- 拼接后的原文
  embedding TEXT NOT NULL DEFAULT '[]',
  status TEXT NOT NULL DEFAULT 'open' CHECK(status IN ('open','closed','extracting','extracted')),
  -- status 与产品方案映射: 'open'=正在增长 | 'closed'=已闭合待提取(产品方案"pending") | 'extracting'=提取中 | 'extracted'=已提取
  closed_at TEXT,
  extracted_at TEXT,
  created_at TEXT NOT NULL DEFAULT (datetime('now')),
  updated_at TEXT NOT NULL DEFAULT (datetime('now'))
);
CREATE INDEX idx_chunks_conv ON conversation_chunks(conversation_id);
CREATE INDEX idx_chunks_status ON conversation_chunks(conversation_id, status);
```

**memories 表新增字段：**
- `source_conversation_id TEXT` — 关联到会话，供直接查询"某次对话产生了哪些记忆"（产品方案 §记忆与消息/Chunk 的关联模型）
- `type TEXT NOT NULL DEFAULT 'fact'` — 暂存但不强制分类
- `topic TEXT`
- `importance REAL NOT NULL DEFAULT 0.5`
- `metadata TEXT` — JSON，对应产品方案 §记忆提取时的元数据写入: `{ proper_noun_count, number_count, total_word_count, extraction_trigger ("topic_shift"|"session_idle"), source_chunk_ids: ["chunk_012","chunk_013"], specificity, proper_nouns: ["PostgreSQL","UUID"] }`
- `status TEXT NOT NULL DEFAULT 'active' CHECK(status IN ('active','invalidated'))`
- `superseded_by TEXT REFERENCES memories(id)`
- `valid_from TEXT NOT NULL DEFAULT (datetime('now'))` — 事实生效时间（产品方案 §记忆生命周期状态）
- `valid_until TEXT` — 事实失效时间（NULL = 仍有效），供回溯查询：`WHERE valid_from <= '2026-02-01' AND (valid_until IS NULL OR valid_until > '2026-02-01')`
- `access_count INTEGER NOT NULL DEFAULT 0`
- `last_accessed TEXT`

**新建 memory_source_links 表（记忆 ↔ chunk 双向链接）：**
```sql
CREATE TABLE memory_source_links (
  id TEXT PRIMARY KEY,
  memory_id TEXT NOT NULL REFERENCES memories(id) ON DELETE CASCADE,
  chunk_id TEXT NOT NULL REFERENCES conversation_chunks(id) ON DELETE CASCADE,
  source_type TEXT NOT NULL DEFAULT 'chunk' CHECK(source_type IN ('chunk','message','attachment','web_retrieval')),
  source_id TEXT,
  relevance_score REAL,
  created_at TEXT NOT NULL DEFAULT (datetime('now'))
);
CREATE INDEX idx_msl_memory ON memory_source_links(memory_id);
CREATE INDEX idx_msl_chunk ON memory_source_links(chunk_id);
```

**新建 chunk_messages 表（chunk ↔ message 精确关联，产品方案 §记忆与消息/Chunk 的关联模型）：**
```sql
CREATE TABLE chunk_messages (
  id TEXT PRIMARY KEY,
  chunk_id TEXT NOT NULL REFERENCES conversation_chunks(id) ON DELETE CASCADE,
  message_id TEXT NOT NULL REFERENCES messages(id) ON DELETE CASCADE,
  turn_index INTEGER NOT NULL,
  role TEXT NOT NULL,
  created_at TEXT NOT NULL DEFAULT (datetime('now'))
);
CREATE INDEX idx_cm_chunk ON chunk_messages(chunk_id);
CREATE INDEX idx_cm_message ON chunk_messages(message_id);
```

**新建 message_attachments 表（消息附件，产品方案 §消息模型 attachments 字段）：**
```sql
CREATE TABLE message_attachments (
  id TEXT PRIMARY KEY,
  message_id TEXT NOT NULL REFERENCES messages(id) ON DELETE CASCADE,
  type TEXT NOT NULL CHECK(type IN ('image','file','audio','video')),
  name TEXT NOT NULL,
  path TEXT NOT NULL,
  created_at TEXT NOT NULL DEFAULT (datetime('now'))
);
CREATE INDEX idx_ma_message ON message_attachments(message_id);
```

**新建 message_web_retrievals 表（AI 网页检索结果，产品方案 §消息模型 web_retrievals 字段）：**
```sql
CREATE TABLE message_web_retrievals (
  id TEXT PRIMARY KEY,
  message_id TEXT NOT NULL REFERENCES messages(id) ON DELETE CASCADE,
  url TEXT NOT NULL,
  title TEXT,
  snippet TEXT,
  created_at TEXT NOT NULL DEFAULT (datetime('now'))
);
CREATE INDEX idx_mwr_message ON message_web_retrievals(message_id);
```

**新建 user_profile 表：**
```sql
CREATE TABLE user_profile (
  key TEXT PRIMARY KEY,
  value TEXT NOT NULL,
  updated_at TEXT NOT NULL DEFAULT (datetime('now'))
);
```

### 迁移 012: 会话隐私模式字段

**conversations 表新增（产品方案 §用户交互 — 会话级临时开关）：**
```sql
ALTER TABLE conversations ADD COLUMN privacy_mode INTEGER NOT NULL DEFAULT 0;
```

### 迁移 013: 索引优化

```sql
CREATE INDEX idx_messages_turn ON messages(conversation_id, turn_index);
CREATE INDEX idx_memories_status ON memories(status);
CREATE INDEX idx_memories_last_accessed ON memories(last_accessed);
```

---

## 三、后端服务设计

### 3.1 ConversationChunkService

**L0→L1 滑出规则（产品方案 §三层时间模型）：**
- 会话总 turn 数 ≤ L0_RETENTION_ROUNDS（10）→ 不做任何 chunking，原文保留在上下文中
- 当第 11 个 turn 产生时（首个超出 L0），自动创建第一个 chunk，该 turn 成为 chunk 起点
- 后续每个新 turn 滑入 L1，与上一个 turn（turn[N] vs turn[N-1]）做语义相似度比较

**相邻 turn 比较规则（产品方案 §触发 A）：**
- 取 turn[N-1]（上一轮 assistant 回复）和 turn[N]（当前 user 消息）的 content
- 分别调用 EmbeddingService.embed() → 计算 cosineSimilarity（复用现有服务）
- 相似度 ≥ 阈值（0.65）→ 视为同一话题，turn[N] 并入当前 open chunk，更新 end_turn_index
- 相似度 < 阈值 → 话题切换，当前 chunk 闭合，turn[N] 成为新 chunk 起点

```
class ConversationChunkService
  ├── assignTurnIndex(conversationId) → number
  │     查询当前会话最大 turn_index，COALESCE(MAX(turn_index), 0) + 1 返回（新会话首条为 1）。user+assistant 一轮合计为 1 个 turn
  ├── shouldEnterL1(conversationId) → boolean
  │     统计会话总 turn 数，只有 > L0_RETENTION_ROUNDS(10) 的 turn 才进入 L1 chunking
  ├── checkChunkBoundary(conversationId) → { closed, chunkId? }
  │     前提：turn > L0_RETENTION_ROUNDS（否则直接返回，不处理）
  │     无 open chunk 时（首个超出 L0 的 turn）→ 直接创建新 chunk，不做相似度比较
  │     有 open chunk 时：
  │       取最新消息 turn[N] 和上一条消息 turn[N-1] 的 content
  │       分别 embedding → cosineSimilarity
  │       相似度 ≥ 0.65 → 同一话题，更新 open chunk 的 end_turn_index
  │       相似度 < 0.65 → 话题切换，闭合当前 chunk，创建新 chunk
  │       （closeChunk 内部异步调用 extractFromChunk，产品方案 §触发 A：闭合 → 提取）
  ├── closeChunk(conversationId) → chunkId
  │     当前 open chunk：status → 'closed', end_turn_index → 最后一条消息的 turn_index, closed_at → now()
  │     调用 buildChunkContent 拼接最终 content → EmbeddingService.embed(content) 写入 chunk.embedding
  │     异步触发 extractFromChunk(chunkId)（产品方案 §触发 A：闭合的 chunk 触发记忆提取 Agent）
  ├── updateChunkEndTurn(chunkId, endTurnIndex)
  │     每次新 turn 归入同一 chunk 时更新 end_turn_index
  ├── addTurnToChunk(chunkId, messageId, turnIndex, role)
  │     将消息写入 chunk_messages 表；增量追加消息 content（含附件描述文本）到 chunk.content
  │     产品方案 §关键约束：附件描述文本参与 chunk embedding
  │     注意：增量追加是 open 阶段的性能优化，最终一致性由 closeChunk 的 buildChunkContent 保证
  ├── buildChunkContent(chunkId) → string
  │     遍历 chunk_messages 关联的所有消息，取 content 拼接；若消息有附件/网页检索，追加载体的描述文本
  │     全量重建，确保 chunk.content 与 chunk_messages 完全一致
  ├── getOpenChunk(conversationId) → ConversationChunk | null
  ├── extractFromChunk(chunkId) → 异步
  │     过滤：只处理 memory_enabled=1 AND privacy_mode=0 的消息（产品方案 §关键约束）
  │     步骤：
  │       0. chunk status → 'extracting'（防止并发提取，配合 processStaleConversations 的 30 分钟超时检测）
  │       1. buildChunkContent(chunkId) 拼接原文 + 附件描述文本
  │       2. MemoryService.extractFacts(content) → FactExtraction[]（带 topic 字段的输出）
  │       3. 按 action 分发：
  │          - ADD    → embeddings API → MemoryService.addMemoryWithSource(content, topic, chunkId)
  │          - UPDATE → embeddings API → MemoryService.addMemoryWithSource(...) + invalidateMemory(oldId, newId)
  │          - DELETE → MemoryService.invalidateMemory(id)（仅失效，不新增）
  │       4. 完成后：chunk status → 'extracted'，extracted_at → now()
  │     产出：一块或多块记忆，每块独立 embedding
  │     metadata: extraction_trigger("topic_shift"|"session_idle"), source_chunk_ids
  ├── processStaleConversations(idleThresholdMs) → 触发 B
  │     扫描规则：
  │       - 扫描 updated_at < now - 1h 且存在 status IN ('open', 'closed') chunk 的会话
  │       - 额外扫描 status='extracting' 且 updated_at < now - 30min 的 chunk（提取崩溃后卡住的异常状态）
  │     对 open chunk：强制闭合；对全部未提取 chunk：异步提取（extraction_trigger = "session_idle"）
  │     注意：extracting→重试前先将 status 重置为 'closed'，避免并发提取
  ├── getChunkSourceMessages(chunkId) → 供 recall_context 使用
  │     通过 chunk_messages 表查出所有关联消息，返回 role/content/turn_index/created_at
  └── getChunkAttachments(chunkId) → 供 recall_sources 使用
        通过 chunk → chunk_messages → message_attachments / message_web_retrievals 链路查出
```

### 3.2 MemoryRetrievalService

```
class MemoryRetrievalService
  ├── searchMemory(query, limit=5) → MemorySearchResult[]
  │     基础过滤：WHERE status = 'active'（产品方案 §记忆生命周期状态 — 只检索活跃记忆）
  │     Phase 1: EmbeddingService.embed(query)
  │     Phase 2: 通道 A (向量×time_decay) top-15 + 通道 B (纯向量) top-15
  │     Phase 3: RRF 融合 → top-20
  │     Phase 4: RerankerService.rerank() → top-10
  │     Phase 5: 时间修正 → CE_score × 0.5^(days/60)
  │     Phase 6: 五信号加权 → top-limit
  │     Phase 7: 更新 access_count / last_accessed
  ├── recallContext(memoryId) → { memory, turns, sources(名称+类型) }
  ├── recallSources(memoryId, sourceIds?) → { memory, fullSources }
  └── private: channelAVectorTimeDecay / channelBVectorOnly / rrfFusion / applyTimeCorrection / computeFinalScore
```

**五信号公式：**
```
semantic     = rerankScore（来自 RerankerService.rerank() 原始 CE 分数，未经 Phase 5 时间修正）
               Phase 5 时间修正(corrected = CE_score × time_decay)是独立的中间排序步骤，用于从 top-10 中筛出候选，
               不改变此处的 semantic 值，避免 time_decay 被双重计入
recency      = e^(-0.01 × hoursSinceLastAccess)
timeDecay    = 0.5^(daysSinceCreation / 60)
frequency    = 1 - e^(-0.1 × accessCount)
importance   = metadata.importance（提取时预计算）
finalScore   = 0.35×semantic + 0.20×recency + 0.20×timeDecay + 0.10×frequency + 0.15×importance
```

### 3.3 ToolDefinitionBuilder

```typescript
buildTools(memoryEnabled: boolean): Record<string, Tool> | undefined {
  if (!memoryEnabled) return undefined;
  return {
    search_memory: {
      description: '搜索用户的长期记忆，获取之前对话中记录的事实、偏好和上下文',
      parameters: z.object({
        query: z.string().describe('LLM 自行组织的检索词'),
        limit: z.number().optional().default(5).describe('返回条数'),
      }),
      // 返回: [{ memory_id, content, topic, type, created_at, importance, source_count }]
      // source_count = 关联的 turn/附件数量，供 LLM 判断是否调 recall_context 深入
    },
    recall_context: {
      description: '获取指定记忆的原始对话上下文，了解记忆的来源和背景',
      parameters: z.object({ memory_id: z.string() }),
      // 返回: { memory_id, topic, turns: [{ turn_index, role, content, created_at }],
      //         sources: [{ source_id, name, type }] }  ← 只标名称和类型，不含全文
    },
    recall_sources: {
      description: '获取指定记忆关联的附件全文和网页检索内容',
      parameters: z.object({
        memory_id: z.string(),
        source_ids: z.array(z.string()).optional().describe('可选，指定 source，不传返回全部'),
      }),
      // 返回: { memory_id, attachments: [{ source_id, name, type, content }],
      //         web_retrievals: [{ source_id, url, title, snippet }] }
    },
  };
}
```

### 3.4 ToolExecutor

依赖链：`chat.ts → ToolExecutor → MemoryRetrievalService → ConversationChunkService`

- `search_memory`：MemoryRetrievalService 内部完成检索，不依赖 ConversationChunkService
- `recall_context`：MemoryRetrievalService.recallContext 内部调用 ConversationChunkService.getChunkSourceMessages
- `recall_sources`：MemoryRetrievalService.recallSources 内部调用 ConversationChunkService.getChunkAttachments

```typescript
async execute(toolName: string, args: Record<string, any>) {
  switch (toolName) {
    case 'search_memory': return memoryRetrievalService.searchMemory(args.query, args.limit);
    case 'recall_context': return memoryRetrievalService.recallContext(args.memory_id);
    case 'recall_sources': return memoryRetrievalService.recallSources(args.memory_id, args.source_ids);
  }
}
```

### 3.5 UserProfileService

```
class UserProfileService
  ├── getAll() → Record<string, string>
  ├── get(key) → string | null
  ├── set(key, value)
  ├── delete(key)
  └── buildProfileSection() → "## 用户信息\n- 昵称: 张三\n..."
```

### 3.6 MemoryService 增量变更

新增方法：
- `addMemoryWithSource(content, topic, chunkId, messageIds?)` — 带 source_links 的存储，topic 来自 LLM 提取输出
- `computeImportance(content)` → `{ importance, specificity, properNouns, numbers, totalWords }` — `specificity` 与 `importance` 同值（产品方案 §重要度计算："specificity = 专有名词 + 数字密度，与 importance 同值"）
- `invalidateMemory(oldId, newId)` — 失效旧记忆，设置 superseded_by 链
- `recordAccess(memoryId)` — 更新 access_count + last_accessed
- `getMemoryDetail(memoryId)` — 含 source_links 的完整信息

变更方法：
- `extractFacts` prompt 增加 topic 字段输出（不强制 type 分类），接口更新为：
  ```typescript
  interface FactExtraction {
    fact: string;
    topic?: string;        // 新增：记忆主题，由 LLM 输出
    action: 'ADD' | 'UPDATE' | 'DELETE';
    existing_id: string | null;
  }
  ```
- `addMemory` 中调用 `computeImportance`，写入 metadata JSON + source_conversation_id
- `processFacts` 中 UPDATE 动作改为"新增 + 失效旧记忆"模式（产品方案 §记忆生命周期状态）：
  1. 调用 `addMemoryWithSource` 创建新记忆
  2. 调用 `invalidateMemory(oldId, newId)` 将旧记忆 status→'invalidated', superseded_by→newId, valid_until→now()
  3. 这样保留"Go → Rust"的完整变化链路，支持回溯查询
- `getMemoriesForChat` 不再被 chat 流程调用（改为工具检索）

### 3.7 ConversationIdleDetector

职责边界：
- **ConversationIdleDetector**：轻量定时器，server 启动时注册 `setInterval`，每 15 分钟调用一次 `ConversationChunkService.processStaleConversations()`
- **ConversationChunkService.processStaleConversations()**：实际扫描逻辑，查询、闭合、提取

processStaleConversations 详细逻辑：
- 查询 `updated_at < now - 1h` 且存在 `status IN ('open', 'closed')` chunk 的会话
- 额外扫描 `status='extracting' AND updated_at < now - 30min` 的 chunk（异常卡住，重置为 'closed' 后重试）
- "未提取"定义：status = 'open'（未闭合）或 'closed'（已闭合但尚未提取/提取失败，产品方案 §触发 B）
- 对 open chunk：强制闭合（status → 'closed', closed_at → now()）
- 对全部 closed 且未提取的 chunk：异步调用 extractFromChunk

### 3.8 config.ts 新增检索管线参数

对应产品方案 §关键参数，在 `config` 对象中新增：

```typescript
export const config = {
  // ... 现有参数

  // L0/L1 chunk 切分
  l0RetentionRounds: 10,               // L0 保留轮次，以内不做提取
  chunkSimilarityThreshold: 0.65,       // 相邻 turn 余弦相似度阈值，低于此值视为话题切换
  sessionIdleTimeoutMs: 3600000,        // 会话空闲超时（1 小时），触发 B

  // 时间衰减
  timeDecayHalfLifeDays: 60,            // 创建时间衰减半衰期（天）
  recencyDecayRate: 0.01,               // 近因衰减率 λ（每小时）
  frequencyGrowthRate: 0.1,             // 访问频率增长率 μ

  // RRF 融合
  rrfSmoothingFactor: 60,               // RRF 平滑因子 k，防止第 1 名碾压

  // 检索管线取 top-N
  channelTopN: 15,                      // 通道 A/B 各自取 top-N
  rrfTopN: 20,                          // RRF 融合后取 top-N
  rerankerTopN: 10,                     // Reranker 精排后取 top-N
  searchResultLimit: 5,                 // 最终返回 LLM 的条数

  // 五信号权重（总和 = 1.0）
  signalWeights: {
    semantic: 0.35,
    recency: 0.20,
    timeDecay: 0.20,
    frequency: 0.10,
    importance: 0.15,
  },
};
```

---

## 四、API 路由变更

### 新增路由

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/profile` | 获取用户个人信息 KV |
| PUT | `/api/profile` | 批量更新 `{ key: value, ... }` |
| DELETE | `/api/profile/:key` | 删除单条 |
| GET | `/api/memory/:id` | 获取单条记忆详情（含 source_links） |
| GET | `/api/memory/:id/sources` | 获取记忆关联的来源列表 |
| PUT | `/api/memory/:id` | 手动编辑记忆内容（重新 embedding） |
| DELETE | `/api/memory/:id` | 软删除单条记忆 |
| PATCH | `/api/conversations/:id` | 更新会话属性（隐私模式 toggle 等） |

### SSE 事件变更

| 事件类型 | 数据 | 说明 |
|----------|------|------|
| `tool_call` | `{ toolCallId, toolName, args }` | LLM 发起工具调用 |
| `tool_result` | `{ toolCallId, result }` | 工具执行完成 |

---

## 五、聊天流程变更（核心）

### 旧的流程

```
构建 system prompt → 固定注入记忆(getMemoriesForChat) → 注入 KB
→ streamText(messages) 单程流式 → SSE done
→ 异步提取记忆(最近20条)
```

### 新的流程

```
1. 构建 system prompt（产品方案 §发送机制）：
   system_prompt = 基础系统提示词
                 + "## 用户信息\n" + UserProfileService.buildProfileSection()
                 + "## 对话历史\n" + L0 热层消息最近 N 轮原文（固定轮次，不做提取）
                 + KB 检索结果（知识库）
   注意：L0 消息放在 system prompt 中，不作为 messages 数组的对话历史

2. 构建 messages = [system prompt] + L0 之后的消息 history
3. 构建 tools = ToolDefinitionBuilder.buildTools(enableMemory)

4. 第一轮 streamText(messages, { tools })
   → onToken/onReasoning: 正常 SSE
   → onToolCall: SSE tool_call → ToolExecutor.execute → SSE tool_result
   → onFinish: 如果 toolCalls 不为空，tool messages 加入 messages，回到 step 4（最多 3 轮）

5. 最终轮：保存消息到 DB
   → 每条消息写入快照字段：memory_enabled（助手配置值）、privacy_mode（会话当前值）
   → 产品方案 §关键约束：提取 Agent 只处理 memory_enabled=1 AND privacy_mode=0 的消息
   → 产品方案 §不回溯源则：开启前的历史消息 memory_enabled=0，不会被补提取

6. 异步：
   → assignTurnIndex + checkChunkBoundary
   → chunk 闭合 → extractFromChunk（只处理 privacy_mode=0 的消息）
```

### AiSdkService.chatStream 关键变更

```
streamText() 新增 tools 参数
fullStream 循环新增处理：
  case 'tool-call': 回调 onToolCall
  case 'tool-result': 回调 onToolResult
  
新增回调：onToolCall / onToolResult
新增参数：tools?: Record<string, Tool>
```

### 工具调用循环（chat.ts 内）

```
const MAX_TOOL_ROUNDS = 3;
let currentMessages = [...];
for (let round = 0; round <= MAX_TOOL_ROUNDS; round++) {
  await aiService.chatStream(currentMessages, ..., callbacks, signal, thinkingMode, startTime, tools);
  if (本轮无 toolCalls) break;
  执行每个 tool → 结果作为 tool message push 到 currentMessages;
}
```

---

## 六、前端变更

### 6.1 SSE 新增事件处理（api.ts）

```typescript
// chatSSE 新增回调
onToolCall?: (toolCallId: string, toolName: string, args: any) => void;
onToolResult?: (toolCallId: string, result: any) => void;

// Fetch 解析新增分支
case 'tool_call': callbacks.onToolCall?.(...); break;
case 'tool_result': callbacks.onToolResult?.(...); break;
```

### 6.2 App.tsx 变更

- 新增 `toolCallsByConv: Record<string, ToolCallEntry[]>` state
- handleSendMessage 中处理 onToolCall / onToolResult 回调
- 新增 userProfile state + 加载逻辑

### 6.3 ChatArea.tsx 变更

**会话顶部隐私模式 toggle（产品方案 §用户交互）：**
- 右侧会话顶部增加隐私模式开关，类似浏览器隐身窗口图标
- 开启时：会话 conversations.privacy_mode 置 1，后续消息快照 privacy_mode=1
- 关闭时：会话恢复正常记忆模式，后续消息快照 privacy_mode=0，可被提取
- 提示文字："隐私模式：开启时段对话内容不会被提取为记忆，但仍可检索已有记忆"

**工具调用块渲染：**
在助手消息气泡中渲染工具调用块：
```
┌─────────────────────────────┐
│ 🔧 search_memory           │
│ 参数: "用户用什么数据库"      │
│ ✓ 找到 3 条相关记忆          │
├─────────────────────────────┤
│ （正常 Markdown 回复文本）    │
│                             │
│ 📎 参考来源:                 │
│   数据库选型方案 · 2天前      │  ← 点击可跳转到原始对话对应位置（产品方案 §用户体验）
│   PostgreSQL 迁移讨论 · 上周  │
└─────────────────────────────┘
```

### 6.4 AssistantModal.tsx 变更

- 全局记忆开关 toggle（已有 enable_memory 字段）

### 6.5 SettingsArea.tsx 变更

**记忆 tab 增强：**
- 表格新增列：topic、importance、status、access_count
- 行操作：编辑、删除、"查看来源"按钮
- 来源弹窗：展示 chunk → messages 追溯链

**新增 "个人信息" tab：**
- Key-Value 表格，支持添加/编辑/删除
- 预设建议：昵称、位置、职业、技术栈、当前项目

### 6.6 类型定义

```typescript
// Message 新增
turn_index: number;
memory_enabled: number;      // 消息入库时的记忆开关快照
privacy_mode: number;         // 消息入库时的隐私模式快照
toolCalls?: { toolCallId, toolName, args, result?, status }[];

// Conversation 新增
privacy_mode: number;         // 会话级隐私模式（产品方案 §用户交互）

// 新增 UserProfile = Record<string, string>;
```

---

## 七、分阶段实施计划

### Phase 1: 数据库 + 核心模型（2 天）

1. 迁移 011/012/013：数据库 schema 变更
2. 更新类型定义（server + 前端）
3. 验证：迁移成功，现有功能无回归

### Phase 2: 三层时间模型 + 提取触发（3 天）

1. **ConversationChunkService** 全量实现
2. **修改 chat.ts**：消息快照字段写入、turn 管理、chunk 闭合检测
3. **修改 MemoryService**：importance 计算、source_links 写入、status/superseded_by 管理
4. **ConversationIdleDetector**：触发 B，server 启动时注册
5. 验证：多轮对话后 chunk 正确闭合和提取

### Phase 3: 工具调用 + 检索管线（4 天）

1. **RerankerService**：封装硅基流动 rerank API，暴露 `rerank(query, documents[])` 方法
2. **MemoryRetrievalService**：双通道检索 + RRF + RerankerService.rerank() + 五信号加权
3. **ToolDefinitionBuilder + ToolExecutor**
4. **修改 AiSdkService**：tools 参数 + tool-call/tool-result 事件
5. **修改 chat.ts**：工具调用循环（max 3 轮）
6. **UserProfileService + profile 路由**
7. 验证：端到端工具调用流程，五信号分数合理性

### Phase 4: 前端（3 天）

1. 后端路由补齐（memory 编辑/删除/详情/来源、profile CRUD、conversations privacy_mode 更新）
2. 前端 SSE + API 对接
3. ChatArea 工具调用渲染
4. ChatArea 会话顶部隐私模式 toggle
5. SettingsArea 记忆管理增强 + 个人信息编辑
6. 验证：完整前端交互流程

### Phase 5: 联调测试（2 天）

1. 端到端回归（关闭记忆 / 开启记忆 / 隐私模式）
2. 工具调用边界测试（3 轮上限、空结果）
3. 检索质量评测
4. 性能测试（双通道 + RRF + Reranker P99 延迟）

---

## 八、关键风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| **Vercel AI SDK tool-call 兼容性** | 工具调用无法实现 | Phase 3 前确认 SDK 版本，`ai` 包 v3.4+ 已稳定支持 |
| **工具调用轮次爆炸** | 延迟和费用剧增 | 硬限制 `MAX_TOOL_ROUNDS=3`，tool result 过长内容截断 |
| **Reranker 对短文本适配** | 精排效果不佳 | 构造富格式 `[类型] [主题] 内容...` 作为 reranker input；效果不佳则降级五信号排序 |
| **语义 chunk 切分误判** | 话题切分错误 | 阈值可配置；比较最近 3 轮滑动窗口平均相似度；后台日志观测 |
| **SQLite 向量检索性能** | 记忆量增长后延迟高 | Phase 3 用内存 LRU cache 缓存嵌入向量；中期考虑 sqlite-vss |
| **SSE 中 token 与 tool_call 穿插** | 前端渲染混乱 | tool_call 阶段不发 token SSE，只发 tool_call/tool_result |
| **embedding API 高频调用** | chunk 切分时 API 用量倍增 | 复用 EmbeddingCache（现有基础设施）；后续考虑 embedBatch |

---

## 涉及的关键文件汇总

| 文件 | 类型 | 说明 |
|------|------|------|
| `server/src/db/migrations/011_*.sql` | 新增 | 核心 schema：chunks、chunk_messages、message_attachments、message_web_retrievals、memory_source_links、user_profile、memories/messages 字段扩展 |
| `server/src/db/migrations/012_*.sql` | 新增 | conversations 隐私模式字段 |
| `server/src/db/migrations/013_*.sql` | 新增 | 索引优化 |
| `server/src/services/ConversationChunkService.ts` | 新增 | L0/L1 chunk 管理 + 语义切分 + 记忆提取触发 |
| `server/src/services/MemoryRetrievalService.ts` | 新增 | L2 检索管线：双通道 + RRF + Reranker + 五信号 |
| `server/src/services/RerankerService.ts` | 新增 | Cross-Encoder 精排，调用硅基流动 rerank API |
| `server/src/services/ToolDefinitionBuilder.ts` | 新增 | search_memory / recall_context / recall_sources 工具 schema |
| `server/src/services/ToolExecutor.ts` | 新增 | 工具执行分发 |
| `server/src/services/UserProfileService.ts` | 新增 | 用户信息 KV CRUD + system prompt 拼接 |
| `server/src/services/ConversationIdleDetector.ts` | 新增 | 触发 B 空闲超时检测 |
| `server/src/routes/profile.ts` | 新增 | GET/PUT/DELETE /api/profile |
| `server/src/routes/chat.ts` | 修改 | 工具调用循环(max 3轮) + 消息快照(memory_enabled/privacy_mode) + 移除固定记忆注入 |
| `server/src/services/AiSdkService.ts` | 修改 | streamText 支持 tools 参数 + tool-call/tool-result SSE 事件 |
| `server/src/services/MemoryService.ts` | 修改 | importance 计算 + source_links + status/superseded_by + valid_from/valid_until |
| `server/src/routes/memory.ts` | 修改 | GET/PUT/DELETE 记忆详情/编辑/软删除/来源查询 |
| `server/src/routes/assistants.ts` | 修改 | 全局记忆开关（已有 enable_memory 字段） |
| `server/src/routes/conversations.ts` | 修改 | PATCH privacy_mode 更新端点 |
| `server/src/config.ts` | 修改 | 检索管线参数（L0轮次、阈值、衰减率、信号权重、top-N 等 15 个参数） |
| `src/components/ChatArea.tsx` | 修改 | 工具调用块渲染 + 会话顶部隐私模式 toggle |
| `src/components/AssistantModal.tsx` | 修改 | 全局记忆开关配置 |
| `src/components/SettingsArea.tsx` | 修改 | 记忆管理增强(topic/importance/status/access_count) + 个人信息编辑 tab |
| `src/App.tsx` | 修改 | toolCalls state + userProfile state |
| `src/services/api.ts` | 修改 | SSE tool_call/tool_result 事件 + profile/memory API |
| `src/types/index.ts` | 修改 | ToolCall、MemoryDetail、UserProfile 等类型 |
| `server/src/types/index.ts` | 修改 | ConversationChunk、ChunkMessage、MessageAttachment、MessageWebRetrieval 等类型 |
