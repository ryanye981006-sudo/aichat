# 查询改写与网页搜索产品方案

> 基于行业调研（DeepSeek、腾讯元宝、通义千问）和开源项目分析，覆盖记忆搜索和网页搜索的统一查询改写方案。

---

## 一、核心命题

当前记忆搜索（`search_memory`）和未来网页搜索（`web_search`）共享同一个问题：**用户说的是人话，搜索引擎需要的却是关键词。** 查询改写是这中间的翻译层。

### 核心设计原则

```
开了深度思考 → 查询改写激活 → 改写过程体现在思考链中
没开深度思考 → 不查询改写 → 模型直接用原始 query 搜索
```

**为什么跟深度思考绑定？**

- 查询改写本质是"多步思考"——模型要理解用户意图、拆解问题、生成检索词。这正是深度思考模型擅长的事
- 不开深度思考时，模型直接输出 tool call，参数就是用户的原始提问，不需要额外的 LLM 改写调用（省延迟）
- 开了深度思考后，改写步骤融合在思考过程中，用户可以看见模型"在想什么"，透明度高

---

## 二、竞品调研总结

### 2.1 三家核心差异

| 维度 | DeepSeek | 腾讯元宝 | 通义千问 |
|------|----------|----------|----------|
| **架构** | RAG（查询转换+获取+融合） | Agentic RAG（规划-搜索-阅读-反思） | Search Tool + Reranker + 千寻平台 |
| **改写方式** | 意图识别+关键词提取+同义词扩展 | 两阶段 LLM 改写（SFT + RL） | Query2Doc 生成式重写 + PRO 对齐 |
| **多轮对话补全** | 有限 | 自动补全省略上下文 | 千寻平台支持多轮改写 |
| **改写过程可见性** | 不展示改写 query | 不直接展示 | 页面展示改写后的搜索关键字 |
| **搜索生态** | 弱（依赖第三方 API） | 强（微信+搜狗+腾讯生态） | 中（阿里云 OpenSearch） |
| **核心弱点** | 搜索精度不足 | 技术细节闭源 | 链接编造、内容幻觉 |

### 2.2 关键开源项目参考

| 项目 | 语言 | 查询改写方式 | 亮点 |
|------|------|-------------|------|
| **[deepsearcher](https://www.npmjs.com/package/deepsearcher)** | TypeScript | LLM 生成多个初始查询 → Reflection 节点分析知识缺口 → 迭代优化 | 基于 LangGraph，支持流式输出 |
| **[Perplexica](https://github.com/ItzCrazyKns/Perplexica)** | TypeScript | `webSearchRetrieverPrompt` 将后续问题改写为独立问题 | 上下文补全 prompt 设计优秀 |
| **[WebThinker](https://github.com/RUC-NLPIR/WebThinker)** | Python | Think-Search-Draft 策略，RL-trained（DPO） | NeurIPS 2025 录用，DeepSeek-R1 增强 |
| **[tool-trainer](https://pypi.org/project/tool-trainer/)** | Python | router → query_writer → Tavily搜索 → critic → answer | 训练 LLM 掌握"何时搜索+怎么写 query" |
| **[deeply-researched](https://github.com/JoyousJohn/deeply-researched)** | JS+Python | 递归式 prompt chaining，子任务分解+草拟+精炼+回溯 | 小模型也能产出高质量结果 |

### 2.3 行业趋势总结

1. **从单轮 RAG 到 Agentic RAG**：搜索→分析结果→发现缺口→再搜索，循环直到信息充分
2. **RL 驱动端到端优化**：用搜索满意度作 reward 优化 query 拆解和召回
3. **Interleaved Thinking**：OpenAI o3/o4-mini、Claude Sonnet 4/Opus 4 支持在推理过程中穿插工具调用
4. **MCP 协议崛起**：SerpApi、Tavily 等都推出了 MCP Server，标准化搜索接入

---

## 三、网页搜索：五大实现方式对比

### 3.1 方案总览

```
┌──────────────┬──────────────┬──────────────┬──────────────┬──────────────┬──────────┐
│              │  模型内置    │  Provider参数 │  Function    │  Workflow    │   MCP    │
│              │    搜索      │   控制搜索    │  Calling     │   强制搜索   │   协议   │
├──────────────┼──────────────┼──────────────┼──────────────┼──────────────┼──────────┤
│ 实现难度     │   极低       │    低        │    中        │    中高      │   中     │
│ 可控程度     │   极低       │    低        │    高        │    高        │   中高   │
│ 搜索源可定制 │    ❌        │    ❌        │    ✅         │    ✅         │   ✅     │
│ 引用源展示   │    ❌        │    ✅         │    ✅         │    ✅         │   ✅     │
│ 需要 FC 能力 │    否       │    否        │    是        │    否        │   是     │
│ 多轮深度搜索 │    ❌        │    ❌        │    ✅         │    ✅         │   ✅     │
│ Token 消耗   │   无额外     │   无额外     │   中等       │   较高       │   中等   │
│ 响应延迟     │   最低      │   低         │   中         │   中高       │   中     │
│ 多模型兼容   │   仅特定模型 │   部分模型    │   所有 FC 模型│  所有模型    │   MCP客户端│
└──────────────┴──────────────┴──────────────┴──────────────┴──────────────┴──────────┘
```

### 3.2 各方案详解

**方案 1：模型内置搜索（Provider-Level Built-in）**

Perplexity Sonar 系列、Jina DeepSearch、OpenRouter `:online` 后缀。切换模型名即开启，搜索过程在 provider 后端完成。

- 适合：快速验证、不需要定制的场景
- 不适合我们：aichat 需要支持多种模型提供商，不能绑定某一家

**方案 2：Provider 参数控制式**

Gemini Grounding（`google_search_tool`）、通义千问（`enable_search: true`）、OpenAI（Responses API `web_search` tool）、Claude（`web_search_20250305` tool）。各家参数不统一，适配工作量大。

- 适合：使用特定 model ecosystem 的产品
- 不适合我们：aichat 是多 provider 架构，适配成本高

**方案 3：Function Calling / Tool Calling（推荐）**

通过 Vercel AI SDK 的 tools 机制，定义 `web_search` 工具，模型自主决定何时调用。应用层对接搜索 API（Brave/Serper/SearXNG）。

- 适合：需要可控性、多 provider 兼容的产品
- **我们的选择**：与现有 memory 工具架构一致，同为 AI SDK tools

**方案 4：Workflow 强制搜索（无 FC 能力的补救）**

对不支持 function calling 的模型，在调用前先搜索或使用辅助模型。Token 消耗大，延迟高。

- 适合：DeepSeek R1 等不支持 FC 的推理模型
- 我们的态度：这些模型建议直接使用方案 3 的辅助模型方式，见下方 §3.3

**方案 5：MCP 协议**

基于 Anthropic MCP 的标准化搜索接入，SerpApi、Tavily 等服务商已推出 MCP Server。

- 适合：多模型、多工具 Agent 架构
- 我们的态度：关注但暂不引入。当前 3-4 个工具用 AI SDK tools 足够，引入 MCP 增加客户端复杂度。等工具数量 > 10 时再考虑

### 3.3 我们的选型

```
唯一方案：Function Calling（方案 3），不做降级
  ├── 与现有 search_memory/recall_context/recall_sources 架构一致
  ├── Vercel AI SDK tools + stepCountIs 已跑通
  ├── 搜索 API 由用户配置（Brave Search / Serper / SearXNG / Bing 等）
  ├── 不引入额外框架
  └── 不支持 FC 的模型 → 直接不注入搜索工具（前端提示该模型不支持联网搜索）

不考虑：
  ├── 模型内置搜索：多 provider 架构下绑定特定模型不合理
  ├── 辅助模型降级：增加延迟和复杂度，不值得
  └── MCP 协议：当前工具数量少，引入 MCP 过度设计
```

---

## 四、查询改写设计

### 4.1 两种模式行为对比

```
┌─────────────────────────────────────────────────────────────────┐
│                    不开深度思考                                  │
│                                                                 │
│  用户: "我去年在成都出差时吃的那个火锅叫什么来着"                  │
│                                                                 │
│  模型: 直接调用                                                    │
│    search_memory({ query: "成都出差 火锅 去年" })                │
│         ↓ 结果为空                                               │
│    search_memory({ query: "成都 火锅" })                        │
│         ↓ 找到了                                                 │
│  模型: "根据之前的记录，你提到的应该是蜀大侠火锅（春熙路店）"       │
│                                                                 │
│  特点：简单、直接、快，但用户看不见检索决策过程                    │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                    开了深度思考                                  │
│                                                                 │
│  用户: "我去年在成都出差时吃的那个火锅叫什么来着"                  │
│                                                                 │
│  思考过程（用户可见）：                                           │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ 用户想找的是之前去成都出差时吃的火锅。我需要：               │  │
│  │ 1. 先查记忆中有没有提到成都出差相关的火锅                    │  │
│  │ 2. 关键词：成都、出差、火锅、去年                           │  │
│  │ 3. 如果记忆中找不到，可能还需要搜一下成都知名火锅店对比      │  │
│  │                                                            │  │
│  │ → 改写后的检索词：                                          │  │
│  │   记忆搜索: "成都出差 火锅 餐厅"                             │  │
│  │   记忆搜索: "成都 火锅 推荐"                                 │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                                                 │
│  然后调用 search_memory → 找到结果 → 回复                         │
│                                                                 │
│  特点：思考过程透明，改写逻辑在 reasoning 中可见，                │
│        用户可以理解"AI 为什么搜这些词"                           │
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 查询改写流程（深度思考模式下）

```
用户发消息
    │
    ▼
┌──────────────────────┐
│  深度思考模型推理     │
│  (reasoning tokens)  │
│                      │
│  ├─ 理解用户意图      │
│  ├─ 判断需要查什么    │  ← 查询改写发生在推理过程中
│  │   - 需要查记忆？   │    不是独立的 LLM 调用
│  │   - 需要查网页？   │    而是模型推理的一部分
│  │   - 都需要？       │
│  ├─ 生成检索词        │
│  │   - 去口语化       │
│  │   - 提取核心实体    │
│  │   - 多版本并行搜索  │
│  └─ 搜索策略决策      │
│      - 先搜记忆再搜网页│
│      - 并行搜索两者    │
│      - 只搜一个        │
└──────┬───────────────┘
       │
       ▼
  模型发出 tool call(s)
  ├── search_memory({ query: "..." })
  ├── web_search({ query: "..." })
  └── ...
```

**关键设计决策：查询改写不由独立 LLM 调用完成，而是由深度思考模型在 reasoning 阶段自然完成。** 这样做的理由：

1. **零额外延迟**：不需要调用一个专门的改写 Agent，改写本身就是思考过程
2. **更好的结果**：推理模型经过 RL 训练，本身就擅长多步推理和决策规划
3. **可见性**：用户可以看见思考过程，理解模型为什么搜这些词
4. **与竞品一致**：元宝的 Query Planning、WebThinker 的 Think-Search-Draft 都是同一思路

### 4.3 不开深度思考时的行为

```
用户发消息
    │
    ▼
  模型直接推理 → 发出 tool call（参数为用户原始表述或简单提炼）
                    ↓
                 到搜索引擎执行
                    ↓
                 结果返回 → 模型整合回答
```

- 不做独立的查询改写调用
- 模型可能在 tool call 参数中做简单提炼（取决于模型自身能力），但不保证
- 首字延迟最小化

---

## 五、统一工具架构

### 5.1 工具全景

```
┌────────────────────────────────────────────────────────────┐
│                     LLM 可用工具                            │
│                                                            │
│  记忆工具（已有）            网页工具（新增）               │
│  ├── search_memory         ├── web_search                  │
│  ├── recall_context        └── web_fetch                   │
│  └── recall_sources                                        │
│                                                            │
│  开启记忆开关 → 记忆工具注入                                 │
│  开启联网搜索 → 网页工具注入                                 │
│  两个独立开关，互不依赖                                     │
└────────────────────────────────────────────────────────────┘
```

### 5.2 新增工具定义

**web_search — 搜索引擎检索**

```
web_search
  ├── 描述: "搜索互联网，获取最新信息、事实、数据。当你不确定某个事实、需要最新信息、或用户明确要求上网查时使用。"
  ├── 参数:
  │     ├── query: string        ← 检索关键词（模型根据用户问题提炼）
  │     ├── max_results: number  ← 返回条数，默认 5，最大 10
  │     └── time_range: string   ← 时间范围（可选）: NoLimit/OneDay/OneWeek/OneMonth/OneYear
  ├── 实现: 阿里云 IQS /search/unified, engineType=LiteAdvanced
  └── 返回: [{
          title, url, snippet, summary,
          published_time, rerank_score,
          hostname, hostLogo          ← IQS API 实测返回，网站图标 URL
        }]
```

**web_fetch — 网页内容抓取**

```
web_fetch
  ├── 描述: "读取指定网页的完整内容。当你需要从搜索结果中获取详细信息时使用。注意：此操作可能返回大量文本。"
  ├── 参数:
  │     ├── url: string          ← 要读取的网页 URL
  │     └── mode: 'basic' | 'scrape'  ← 默认 basic（静态解析），scrape 用于 JS 渲染页面
  ├── 实现: 阿里云 IQS /readpage/basic（默认）或 /readpage/scrape
  └── 返回: {
          url, title,
          content,                ← 网页正文（HTML 清洗后提取纯文本）
          fetched_at
        }
```

### 5.3 与记忆工具的关系

```
用户问题 → 模型推理
              │
              ├── 涉及历史/个人信息 → search_memory
              ├── 涉及最新/事实信息 → web_search
              ├── 两种都涉及 → 并行调用两者
              │
              ▼
           结果融合 → 模型整合回答
```

### 5.4 工具调用轮次上限

**每个工具独立上限（可配置变量），各自独立计数，互不影响。不做全局轮次限制。**

```
search_memory 最多 3 次
recall_context 最多 3 次
recall_sources 最多 3 次
web_search 最多 3 次
web_fetch 最多 3 次
总轮次 = 工具数 × 单工具上限（5 × 3 = 15），由 AI SDK stepCountIs 自动控制
```

**为什么每个工具独立计数？**

记忆搜索是三层渐进式工具（search → recall_context → recall_sources），web_search 是渐进式（search → web_fetch）。同类别共享上限会导致"搜完记忆就不能深入读网页"——分开后 LLM 可以在每层充分展开，例如对 3 条不同的记忆分别 `recall_context` 深入查看。

**AI SDK 实现：**

```typescript
// chat.ts 中每次请求初始化 per-tool 计数器
const toolCallCounts = new Map<string, number>();
// maxSteps = Object.keys(tools).length * config.maxToolCallPerTool
// 各 tool execute 内部检查自己的调用次数
```

```
示例流程（每个工具独立计数，互不影响）：
  第 1 轮: search_memory("火锅") + web_search("成都火锅")
           → search_memory: 1/3, web_search: 1/3
           ↓
  第 2 轮: recall_context(mem_A) + web_fetch(url_1)
           → recall_context: 1/3, web_fetch: 1/3
           ↓
  第 3 轮: recall_context(mem_B) + recall_sources(mem_A)
           → recall_context: 2/3, recall_sources: 1/3（对另一条记忆深入查看）
           ↓
  第 4 轮: web_search("蜀大侠火锅 春熙路") + web_fetch(url_2)
           → web_search: 2/3, web_fetch: 2/3
           ↓
  ... 各工具继续各自计数，直到各自的 3 次上限
```

---

## 六、工具调用记录到消息（已确认）

### 6.1 决策

**所有工具调用必须写入 messages 表，与消息绑定，随消息持久化。**

### 6.2 存储方案

```sql
-- messages 表新增字段（迁移 014）
tool_calls TEXT   -- JSON: [
                  --   {
                  --     toolCallId: "call_xxx",
                  --     toolName: "search_memory",
                  --     args: { query: "成都火锅", limit: 5 },
                  --     result: { status: "done", output: [...], count: 3 },
                  --     started_at: "2026-05-10T14:30:00Z",
                  --     completed_at: "2026-05-10T14:30:02Z"
                  --   }
                  -- ]
                  -- NULL 表示该消息无工具调用
```

### 6.3 写入规则

| 规则 | 说明 |
|------|------|
| **写入时机** | assistant 消息最终保存时，本轮所有 tool call/result 一并写入 `tool_calls` 字段 |
| **存储位置** | 本轮 assistant 回复所在的 messages 行（与 content、thought_process 同级） |
| **不参与 chunk** | chunk 提取只取 `content` 字段，`tool_calls` JSON 不参与 embedding/拼接 |
| **不参与轮次计数** | 中间工具调用轮次不增加 `turn_index`，仅在 user→最终 assistant 回复完成时递增 |
| **不过滤隐私** | tool_calls 与消息绑定，不受 `privacy_mode` 影响（用户切换助手/会话后仍可见） |
| **不发给记忆提取 Agent** | 延续：深度思考内容 + tool_calls 均不参与 L1 chunk 记忆提取 |

### 6.4 三层记忆工具的 tool_calls 记录示例

```
消息 1（assistant，turn_index=5）:
  content: "根据之前的记录，你在成都出差时去的应该是蜀大侠火锅..."
  tool_calls: [
    {
      toolCallId: "call_001",
      toolName: "search_memory",
      args: { query: "成都出差 火锅", limit: 5 },
      result: {
        status: "done",
        output: [
          { memory_id: "mem_123", content: "用户曾在成都出差...", topic: "出差", ... }
        ],
        count: 3
      }
    },
    {
      toolCallId: "call_002",
      toolName: "recall_context",
      args: { memory_id: "mem_123" },
      result: {
        status: "done",
        output: {
          memory_id: "mem_123",
          topic: "出差",
          turns: [{ turn_index: 3, role: "user", content: "去成都出差..." }, ...],
          sources: [{ source_id: "src_1", name: "出差行程表.xlsx", type: "file" }]
        }
      }
    },
    {
      toolCallId: "call_003",
      toolName: "recall_sources",
      args: { memory_id: "mem_123", source_ids: ["src_1"] },
      result: {
        status: "done",
        output: {
          memory_id: "mem_123",
          attachments: [{ source_id: "src_1", name: "出差行程表.xlsx", type: "file", content: "..." }],
          web_retrievals: []
        }
      }
    }
  ]
```

---

## 七、工具调用的前端展示

### 7.1 核心要求

1. **不同工具不同图标和颜色**，一眼区分记忆搜索 vs 网页搜索
2. **开深度思考时**：工具调用在 reasoning stream 之后渲染，附带参数和结果摘要
3. **不开深度思考时**：在回复开头显示工具调用摘要（如 "🔍 搜索 3 条记忆"、"🌐 搜索 5 条网页"）
4. **生成回复后固定显示**：tool_call 块与消息绑定，切换助手/会话后重新打开仍可见（因为已写入 messages 表）
5. **结果摘要随 tool_result 实时更新**：流式过程中显示"搜索中..."→ 完成后显示"找到 X 条结果"

### 7.2 各工具的 Icon 和颜色

| 工具 | 图标 | 颜色 | 执行中文本 | 完成文本 |
|------|------|------|-----------|---------|
| `search_memory` | 🧠 (brain) | 紫色 #7c3aed | 搜索记忆中... | 搜索到 X 条相关记忆 |
| `recall_context` | 💬 (chat) | 蓝色 #3b82f6 | 读取原始对话... | 读取了 X 轮原始对话 |
| `recall_sources` | 📎 (clip) | 靛蓝 #6366f1 | 读取附件/网页... | 读取了 X 个来源 |
| `web_search` | 🌐 (globe) | 绿色 #16a34a | 搜索网页中... | 搜索到 X 条网页结果 |
| `web_fetch` | 📄 (page) | 翠绿 #059669 | 读取网页中... | 读取了 1 个网页全文 |

### 7.3 渲染位置

```
┌─────────────────────────────────────────────┐
│  👤 用户消息                                 │
│  "我去年在成都出差时吃的火锅叫什么来着？"       │
├─────────────────────────────────────────────┤
│  🤖 助手回复                                 │
│  ┌─────────────────────────────────────┐    │
│  │ 深度思考（可折叠）                    │    │
│  │ 用户想找之前去成都出差时吃的火锅...    │    │
│  │ → 需要搜索记忆: 成都 出差 火锅        │    │
│  └─────────────────────────────────────┘    │
│  ┌─────────────────────────────────────┐    │
│  │ 🧠 搜索记忆: "成都 出差 火锅"         │ ← 工具调用块│
│  │ ⏱ 0.8s · 找到 3 条相关记忆          │    │
│  └─────────────────────────────────────┘    │
│  ┌─────────────────────────────────────┐    │
│  │ 💬 读取记忆来源: mem_123             │ ← 工具调用块│
│  │ ⏱ 0.3s · 读取了 5 轮原始对话         │    │
│  └─────────────────────────────────────┘    │
│  （正常 Markdown 回复内容...）              │
│  "根据之前的记录，你在成都出差时去的是         │
│   蜀大侠火锅（春熙路店）"                    │
└─────────────────────────────────────────────┘
```

### 7.4 不开深度思考时的显示

```
┌─────────────────────────────────────────────┐
│  👤 用户消息                                 │
│  "今天成都有什么新闻？"                       │
├─────────────────────────────────────────────┤
│  🤖 助手回复                                 │
│  ┌─────────────────────────────────────┐    │
│  │ 🌐 网页搜索: "成都 今日 新闻 2026"    │ ← 搜索进度（轮询中）
│  │ ⏱ 搜索中...                          │    │
│  └─────────────────────────────────────┘    │
│  （几秒后变为）                             │
│  ┌─────────────────────────────────────┐    │
│  │ 🌐 网页搜索: "成都 今日 新闻 2026"    │ ← 搜索完成
│  │ ⏱ 2.1s · 搜索到 5 条网页结果         │    │
│  └─────────────────────────────────────┘    │
│  （正常 Markdown 回复内容...）              │
│  "根据最新消息，今天成都..."                  │
└─────────────────────────────────────────────┘
```

**关键行为：** 工具调用块在回复内容生成前显示，markdown 回复出来后两者一起固定。历史消息中始终可见。

### 7.5 多层次工具调用的展示

三层记忆工具（search → recall_context → recall_sources）用缩进嵌套展示层级关系：

```
🧠 搜索记忆: "成都 火锅"           · 3 条结果
  ├─ 💬 读取来源: mem_123         · 5 轮对话
  │   └─ 📎 读取文件: 出差行程表.xlsx  · 12KB
  └─ 💬 读取来源: mem_456         · 2 轮对话

🌐 网页搜索: "成都火锅推荐"        · 5 条结果
  └─ 📄 读取网页: dianping.com/...   · 3.2KB
```

### 7.6 消息持久化实现

```
前端 App state: toolCallsByConv[convId][msgId] → 实时流中维护
消息入库后: messages.tool_calls JSON 持久化
历史消息加载: GET /api/messages 返回 messages（含 tool_calls 字段）
前端渲染: 检查 msg.tool_calls，有则渲染对应工具调用块
切换助手/会话后回来: 从 DB 加载消息 → tool_calls 仍然在 → 工具调用块完整显示
```

---

## 八、搜索 API：阿里云 IQS（信息查询服务）

### 8.1 为什么选它

| 维度 | 阿里云 IQS | Brave Search | SearXNG 自建 |
|------|-----------|-------------|-------------|
| 延迟 | **500ms**（LiteAdvanced） | 低 | 800-2000ms |
| 中文搜索质量 | **高**（国内引擎聚合） | 中 | 取决于配置 |
| 免费额度 | **1000/5000 次**（15天试用） | 2000次/月 | 无限 |
| 付费价格 | 12元/千次 | 免费额度后需付费 | VPS 30-50元/月 |
| 维护成本 | **零维护** | 零维护 | 需运维 |
| 网页解析 | ✅ 内置 readpage | ❌ 需额外接入 | ❌ 需额外爬虫 |

### 8.2 API 端点

| 工具 | 端点 | 说明 |
|------|------|------|
| `web_search` | `POST https://cloud-iqs.aliyuncs.com/search/unified` | engineType: `LiteAdvanced`（极速版，500ms） |
| `web_fetch` (basic) | `POST https://cloud-iqs.aliyuncs.com/readpage/basic` | 静态 HTML 解析，2元/千次 |
| `web_fetch` (scrape) | `POST https://cloud-iqs.aliyuncs.com/readpage/scrape` | 浏览器沙箱渲染，10元/千次 |

**认证：** `Authorization: Bearer <IQS_API_KEY>`

### 8.3 已验证通过

```
✅ 中文搜索: "杭州美食推荐" → 3 条结果, 572ms
✅ 英文搜索: "Hangzhou food" → 3 条结果, 545ms
✅ 网页解析: readpage basic → 返回完整 HTML
   Key: -wxwal7NSAto_d-gpA553UCAEWfBm602YTAwMTU3Yg
```

### 8.4 计费

| 项目 | 单价 | 日均100次月费 | 日均1000次月费 |
|------|------|------------|-------------|
| 联网搜索（LiteAdvanced） | 12元/千次 | ~36元 | ~360元 |
| 网页解析（basic） | 2元/千次 | ~6元 | ~60元 |
| 增值摘要包（可选） | 2.8元/千次 | ~8.4元 | ~84元 |

> 增强摘要（summary 字段）目前实测已包含在 LiteAdvanced 返回中，暂不确认是否独立计费。按最保守估算，个人用日均 100 次每月约 36 元。

### 8.5 扩展设计

架构上搜索服务做接口抽象，后续可切换/新增搜索引擎：

```typescript
interface WebSearchProvider {
  search(query: string, maxResults: number, timeRange?: string): Promise<SearchResult[]>;
  fetchPage(url: string, mode: 'basic' | 'scrape'): Promise<PageContent>;
}

class IqsSearchProvider implements WebSearchProvider { /* 阿里云 IQS */ }
class BraveSearchProvider implements WebSearchProvider { /* 后续扩展 */ }
class SearxngProvider implements WebSearchProvider { /* 后续扩展 */ }
```

前端设置页中用户配置 API Key，全局生效，不按助手重复填写。

---

## 九、模型能力检测

### 9.1 不做预检测，走自然报错

不新增独立的 FC 能力检测服务。理由：检测出来也是报错让用户换模型，检测不出来也是一样的结果——预检测只增加延迟和复杂度。

**处理方式：**

1. 记忆/联网开关按助手配置正常注入 tools，不预先判断模型是否支持 FC
2. 如果 API 不支持 FC → `streamText` 直接报错（无文本产出）
3. chat.ts 的 `onError` 捕获 → `translateError()` 翻译为中文："该模型不支持工具调用（记忆搜索/联网搜索），请关闭记忆/联网开关后重发，或切换到 GPT-4o、Claude 等支持 Function Calling 的模型"
4. 用户看到提示 → 关掉开关 → 重发 → 正常回复
5. 如果 API 忽略 tools 但正常产出文本（部分兼容模型）→ 透明通过，无额外处理

### 9.2 深度思考能力

同理不做预检测。如果模型不支持 reasoning → AI SDK 不会产生 `reasoning-delta` 事件 → 前端深度思考区域为空 → 用户自然知道。前端深度思考按钮不因模型能力而禁用，始终可点击。

### 9.3 前端按钮的 FC 相关行为

不做提前禁用。记忆和联网按钮始终可点击（继承助手设置），只有在实际报错时用户才需要切换模型。这样主流程不受影响，不增加额外请求。

---

## 十、对话框按钮交互设计

### 10.1 按钮布局

```
┌──────────────────────────────────────────────────────────┐
│  输入框区域                                               │
│                                                          │
│  上下文轮数: 5/10  [🧠 记忆] [🌐 联网] [💭 深度思考]     │
│  ┌────────────────────────────────────────────────┐      │
│  │  输入你的消息...                                 │      │
│  └────────────────────────────────────────────────┘      │
│                                    [发送]                │
└──────────────────────────────────────────────────────────┘
```

**三个开关按钮，放在输入框上方，从左到右排列：**

| 按钮 | 图标 | 功能 | 状态 | 样式 |
|------|------|------|------|------|
| 记忆 | 🧠 | 开启/关闭本会话的记忆工具注入 | 开/关 | 开关色 |
| 联网 | 🌐 | 开启/关闭本会话的网页搜索工具注入 | 开/关 | 开关色 |
| 深度思考 | 💭 | 强制开启/强制关闭深度思考 | 强制开/强制关/自动（默认） | 三态 |

### 10.2 深度思考按钮（改为三态）

**旧行为：** 切换开/关
**新行为：** 三种状态循环，或下拉选择

```
状态流转（点击切换）：
  自动（默认）→ 强制开启 → 强制关闭 → 自动（默认）→ ...

  ┌────────────┐    ┌────────────┐    ┌────────────┐
  │ 💭 自动    │ →  │ 💭 深度思考 │ →  │ 💭 不思考   │ → 循环
  │ (默认状态) │    │ (强制开启) │    │ (强制关闭) │
  └────────────┘    └────────────┘    └────────────┘
  跟随助手配置      忽略助手配置      忽略助手配置
                   传 thinking:      传 thinking:
                   enabled           disabled
```

**各状态样式：**

| 状态 | 图标 | 按钮样式 | 含义 |
|------|------|---------|------|
| 自动 | 💭 | 灰色/默认 | 跟随助手设置中的 thinking 配置 |
| 强制开启 | 💭 | 高亮/蓝色/激活 | 忽略助手设置，强制启用深度思考 |
| 强制关闭 | 💭⃝ | 灰色/划线 | 忽略助手设置，强制关闭深度思考 |

### 10.3 联网搜索按钮

```
点击开启 → 🌐 高亮（绿色激活）→ 注入 web_search + web_fetch 工具
再次点击 → 🌐 灰色/默认 → 不注入网页搜索工具

开启状态：按钮高亮，工具注入，模型可调用 web_search/web_fetch
关闭状态：按钮灰色，工具不注入，如同没有联网功能
```

**与助手设置的关系：**
- 助手设置中可配置"默认开启联网搜索"（`enable_web_search INTEGER DEFAULT 0`）
- 新建会话时，继承助手设置的默认值
- 会话内点击按钮覆盖默认值（仅影响当前会话）

### 10.4 三个按钮的联锁逻辑

| 场景 | 记忆按钮 | 联网按钮 | 深度思考按钮 |
|------|---------|---------|------------|
| 刚进入会话 | 继承助手设置 | 继承助手设置 | 自动（继承助手设置） |
| 手动开启深度思考 | 不变 | 不变 | 强制开启 |
| 关闭深度思考 | 不变 | 不变 | 强制关闭 |
| 开启联网搜索 | 不变 | **开启** | 不变 |
| 模型不支持 FC（运行时） | 按钮正常开启，调用时报错提示换模型 | 按钮正常开启，调用时报错提示换模型 | 不变 |
| 模型不支持深度思考（运行时） | 不变 | 不变 | 按钮正常，reasoning 区域为空 |

### 10.5 状态管理

```typescript
// App.tsx state（按会话维度）
interface ConversationUIState {
  deepThinkingMode: 'auto' | 'enabled' | 'disabled'  // 深度思考三态
  webSearchEnabled: boolean                            // 联网搜索开关
  memoryEnabled: boolean                               // 记忆开关（已有）
}
// 不持久化到 DB，跟随会话生命周期（切换会话重置为助手默认值）
// 或者：持久化到 localStorage，记住用户偏好
```

---

## 十一、前端体验

### 11.1 深度思考中展示改写过程

开启深度思考时，模型在 reasoning 中的检索决策自然展示给用户（reasoning tokens 实时流式到前端）。不额外添加 UI 元素。

### 11.2 工具调用块渲染（见 §7）

参见第七章"工具调用的前端展示"。

### 11.3 搜索结果引用

网页搜索结果在回答中以 `[citation:X]` 格式引用，来源链接可点击。与现有知识库引用格式保持一致。

### 11.4 搜索引擎选择

**当前阶段：** 固定使用阿里云 IQS（信息查询服务），API Key 写入环境变量 `IQS_API_KEY`，前端设置页可修改（全局生效）。暂不做多引擎切换。

**后续扩展（架构已预留）：** `WebSearchProvider` 接口抽象允许后续新增 Brave Search / Serper.dev / SearXNG / Bing Search 等引擎实现。届时助手配置中新增搜索引擎选择（下拉），各引擎 API Key 在设置页统一管理。

---

## 十二、分阶段实施

### Phase 1：数据库 + 消息持久化（0.5 天）
1. 迁移 014：messages 表新增 `tool_calls TEXT` 字段
2. chat.ts 中写入 tool_calls 到 messages 表
3. 前端类型定义更新

### Phase 2：网页搜索工具（1.5 天）
1. 抽象 `WebSearchProvider` 接口 + `IqsSearchProvider` 实现
2. 对接阿里云 IQS `/search/unified` + `/readpage/basic`
3. ToolDefinitionBuilder 新增 `web_search` + `web_fetch` 工具
4. ToolExecutor 分发
5. 工具调用上限改为按工具独立计数

### Phase 3：按钮交互 + 工具调用前端渲染（2 天）
1. ChatArea 输入框区：深度思考三态按钮 + 联网搜索开关按钮
2. 工具调用块渲染（5 种 icon/颜色/动画，已定义在 §7.2）
3. 不开深度思考时的搜索进度提示
4. tool_calls 持久化后的历史消息渲染

### Phase 4：联调测试（1 天）
1. 端到端：记忆搜索 + 网页搜索并行 → 三层渐进式调用 → 最终回复
2. 历史消息加载：切换助手/会话后 tool_calls 完整显示
3. 边界测试：不支持 FC 的模型、工具上限、空结果

---

## 十三、关键参数预设

| 参数 | 预设值 | 说明 |
|------|--------|------|
| 深度思考时查询改写 | 自动（reasoning 内） | 不额外调用 LLM |
| 不开深度思考时查询改写 | 跳过 | 模型直接用原始 query 作为 tool call 参数 |
| 每个记忆工具最大调用次数 | 3 | search_memory/recall_context/recall_sources 各自独立计数 |
| 每个网页工具最大调用次数 | 3 | web_search/web_fetch 各自独立计数 |
| 全局总轮次 | 工具数 × 单工具上限（5×3=15） | maxSteps 由 tools 数量动态计算，不设固定全局上限 |
| search_memory 默认返回数 | 5 条 | 最大 10 条 |
| web_search 默认返回数 | 5 条 | 最大 10 条 |
| web_fetch 默认返回长度 | 5000 字符 | 防止上下文爆炸 |
| 搜索引擎 | 阿里云 IQS（LiteAdvanced） | 可扩展架构支持切换/新增 |
| web_fetch 模式 | basic（默认） | scrape 用于 JS 渲染页面 |
| 工具调用存储 | messages.tool_calls JSON | 随消息持久化，不参与 chunk 提取 |
| 深度思考按钮 | 三态：自动/强制开/强制关 | 默认跟随助手设置 |
| 联网搜索按钮 | 二态：开/关 | 默认继承助手设置 |
| 模型不支持 FC | 运行时 API 报错 → translateError 中文提示 | 不做预检测，用户看到提示后关开关重发 |
| 模型不支持深度思考 | 深度思考区域为空 | 不做预检测，按钮始终可点击 |
