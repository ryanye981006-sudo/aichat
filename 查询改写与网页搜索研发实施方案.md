# 查询改写与网页搜索 — 研发实施方案

> 基于[查询改写与网页搜索产品方案](./查询改写与网页搜索产品方案.md)的技术落地 plan。

---

## 一、总览与模块划分

### 新增模块（server）

| 模块 | 文件 | 职责 |
|------|------|------|
| **WebSearchProvider** | `server/src/services/webSearch/WebSearchProvider.ts` | 搜索引擎接口抽象：`search()` + `fetchPage()` |
| **IqsSearchProvider** | `server/src/services/webSearch/IqsSearchProvider.ts` | 阿里云 IQS 实现：`/search/unified` + `/readpage/basic` |
| **WebSearchService** | `server/src/services/WebSearchService.ts` | 搜索协调层：管理 provider 实例、结果格式化、引用标注生成 |

### 需修改的模块

| 模块 | 文件 | 变更要点 |
|------|------|---------|
| **ToolDefinitionBuilder** | `server/src/services/ToolDefinitionBuilder.ts` | 新增 `web_search` + `web_fetch` 工具定义；签名改为 `{ memoryEnabled, webSearchEnabled }` |
| **ToolExecutor** | `server/src/services/ToolExecutor.ts` | 分发 `web_search` / `web_fetch` |
| **chat.ts** | `server/src/routes/chat.ts` | tool_calls 写入消息表；tools 注入逻辑改为按开关动态组合；per-tool 计数器初始化；/regenerate 路由同步 |
| **AiSdkService** | `server/src/services/AiSdkService.ts` | translateError 新增 FC 不支持的中文错误翻译 |
| **config.ts** | `server/src/config.ts` | 新增 IQS 参数 + maxToolCallPerTool + maxTotalToolRounds |
| **assistants 路由** | `server/src/routes/assistants.ts` | `enable_web_search` 字段 CRUD |
| **ChatArea** | `src/components/ChatArea.tsx` | 深度思考三态按钮 + 联网搜索开关 + 工具调用块 icon/颜色渲染 |
| **AssistantModal** | `src/components/AssistantModal.tsx` | 联网搜索默认开关 |
| **SettingsArea** | `src/components/SettingsArea.tsx` | 搜索引擎 API Key 全局配置 |
| **api.ts** | `src/services/api.ts` | 新增 settings API（IQS Key 读写） |
| **App.tsx** | `src/App.tsx` | webSearchEnabled / deepThinkingMode state |
| **类型定义** | `src/types/index.ts`, `server/src/types/index.ts` | WebSearchResult、PageContent、ConversationUIState 等 |

---

## 二、数据库迁移

### 迁移 014：工具调用持久化 + 联网搜索开关

**messages 表新增字段：**
```sql
ALTER TABLE messages ADD COLUMN tool_calls TEXT;
-- JSON 数组，格式: [{ toolCallId, toolName, args, result, started_at, completed_at }]
-- NULL 表示该消息无工具调用
```

**assistants 表新增字段：**
```sql
ALTER TABLE assistants ADD COLUMN enable_web_search INTEGER NOT NULL DEFAULT 0;
```

**全局设置表新增 IQS API Key：**
```sql
-- 复用 user_profile 表或新增 settings 表
INSERT INTO user_profile (key, value) VALUES ('iqs_api_key', '') 
  ON CONFLICT(key) DO NOTHING;
```

### 迁移 015：索引优化

```sql
-- 工具调用查询（调试/审计场景）
-- 不建专用索引，工具调用通过 messages.id 联查，已有一级索引
```

---

## 三、后端服务设计

### 3.1 WebSearchProvider 接口

```typescript
// server/src/services/webSearch/WebSearchProvider.ts

export interface SearchParams {
  query: string;
  maxResults?: number;      // 默认 5，最大 10
  timeRange?: 'NoLimit' | 'OneDay' | 'OneWeek' | 'OneMonth' | 'OneYear';
}

export interface SearchResult {
  title: string;
  url: string;
  snippet: string;
  summary?: string;
  publishedTime?: string;
  rerankScore?: number;
  hostname?: string;
  hostLogo?: string;          // IQS 返回的网站图标 URL
}

export interface PageContent {
  url: string;
  title: string;
  content: string;           // 清洗后的纯文本
  fetchedAt: string;
}

export interface WebSearchProvider {
  /** 搜索互联网 */
  search(params: SearchParams): Promise<SearchResult[]>;
  /** 抓取网页全文 */
  fetchPage(url: string, mode: 'basic' | 'scrape'): Promise<PageContent>;
}
```

### 3.2 IqsSearchProvider 实现

```typescript
// server/src/services/webSearch/IqsSearchProvider.ts

class IqsSearchProvider implements WebSearchProvider {
  private apiKey: string;
  private baseUrl = 'https://cloud-iqs.aliyuncs.com';

  constructor(apiKey: string) {
    this.apiKey = apiKey;
  }

  async search(params: SearchParams): Promise<SearchResult[]> {
    const resp = await fetch(`${this.baseUrl}/search/unified`, {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${this.apiKey}`,
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({
        query: params.query,
        engineType: 'LiteAdvanced',
        timeRange: params.timeRange || 'NoLimit',
        contents: {
          mainText: false,
          markdownText: false,
          summary: true,
          rerankScore: true,
        },
        advancedParams: { numResults: params.maxResults || 5 },
      }),
    });

    const data = await resp.json();
    return (data.pageItems || []).map((item: any) => ({
      title: item.title,
      url: item.link,
      snippet: item.snippet || '',
      summary: item.summary || null,
      publishedTime: item.publishedTime || null,
      rerankScore: item.rerankScore || null,
      hostname: item.hostname || null,
      hostLogo: item.hostLogo || null,   // IQS API 实测返回网站图标 URL
    }));
  }

  async fetchPage(url: string, mode: 'basic' | 'scrape'): Promise<PageContent> {
    const endpoint = mode === 'scrape' ? '/readpage/scrape' : '/readpage/basic';
    const resp = await fetch(`${this.baseUrl}${endpoint}`, {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${this.apiKey}`,
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({ url, maxAge: 0 }),
    });

    const data = await resp.json();
    // readpage 返回 HTML，需要提取纯文本
    const text = this.extractText(data.data?.html || '');
    return {
      url,
      title: this.extractTitle(data.data?.html || '') || url,
      content: text.slice(0, 5000),  // 截断
      fetchedAt: new Date().toISOString(),
    };
  }

  // 从 HTML 提取纯文本（简单实现，后续可换用 cheerio/html-to-text）
  private extractText(html: string): string { /* ... */ }
  private extractTitle(html: string): string { /* ... */ }
}
```

### 3.3 WebSearchService

```typescript
// server/src/services/WebSearchService.ts

class WebSearchService {
  private provider: WebSearchProvider | null = null;

  setProvider(provider: WebSearchProvider) { this.provider = provider; }
  getProvider(): WebSearchProvider | null { return this.provider; }

  async search(params: SearchParams): Promise<SearchResult[]> {
    if (!this.provider) throw new Error('搜索引擎未配置');
    return this.provider.search(params);
  }

  async fetchPage(url: string, mode: 'basic' | 'scrape'): Promise<PageContent> {
    if (!this.provider) throw new Error('搜索引擎未配置');
    return this.provider.fetchPage(url, mode);
  }

  /** 将搜索结果格式化为引用提示词 */
  formatCitations(results: SearchResult[]): string {
    // 格式: "## 引用材料\n[1] title (url)\ncontent\n..."
  }
}

export const webSearchService = new WebSearchService();
```

**Provider 初始化时机：** 服务启动时从 `user_profile` 读 `iqs_api_key`，创建 `IqsSearchProvider` 并注入 `webSearchService`。用户在前端修改 Key 时通过 API 触发重建。

### 3.4 ToolDefinitionBuilder 变更

```typescript
// 在现有基础上新增, tools 动态组合
buildTools(opts: {
  memoryEnabled: boolean;
  webSearchEnabled: boolean;
}): Record<string, any> | undefined {

  const tools: Record<string, any> = {};

  if (opts.memoryEnabled) {
    tools.search_memory = { /* 已有 */ };
    tools.recall_context = { /* 已有 */ };
    tools.recall_sources = { /* 已有 */ };
  }

  if (opts.webSearchEnabled) {
    tools.web_search = {
      description: '搜索互联网，获取最新事实信息。当需要最新数据或用户明确要求搜索时使用。',
      inputSchema: z.object({
        query: z.string().describe('检索关键词'),
        max_results: z.number().optional().default(5).describe('返回条数'),
        time_range: z.enum(['NoLimit','OneDay','OneWeek','OneMonth','OneYear']).optional().default('NoLimit'),
      }),
      execute: async ({ query, max_results, time_range }) => {
        return webSearchService.search({ query, maxResults: max_results, timeRange: time_range });
      },
    };

    tools.web_fetch = {
      description: '读取指定网页的完整内容。需要从搜索结果中获取详细信息时使用。',
      inputSchema: z.object({
        url: z.string().describe('网页 URL'),
        mode: z.enum(['basic','scrape']).optional().default('basic'),
      }),
      execute: async ({ url, mode }) => {
        return webSearchService.fetchPage(url, mode);
      },
    };
  }

  return Object.keys(tools).length > 0 ? tools : undefined;
}
```

### 3.5 ToolExecutor 变更

```typescript
// 新增两个 case
case 'web_search':
  return webSearchService.search({ query: args.query, maxResults: args.max_results, timeRange: args.time_range });
case 'web_fetch':
  return webSearchService.fetchPage(args.url, args.mode || 'basic');
```

### 3.6 per-tool 调用上限

每个工具独立计数器，互不影响。采用闭包变量在 chat.ts 的 `/completions` 和 `/regenerate` 路由中初始化，传入各 `tool.execute()` 内部。

```typescript
// server/src/config.ts 新增
iqsApiKey: process.env.IQS_API_KEY || '',
maxToolCallPerTool: 3,        // 每个工具独立上限（search_memory/recall_context/recall_sources/web_search/web_fetch 各 3 次）
// 全局轮次不设固定上限，由 maxSteps = Object.keys(tools).length × maxToolCallPerTool 动态计算

// chat.ts 中每次请求初始化计数器：
const toolCallCounts = new Map<string, number>(); // key: toolName, value: 已调用次数

// 每个 tool.execute() 内部：
// const count = toolCallCounts.get('web_search') || 0;
// if (count >= maxToolCallPerTool) return { error: '该工具调用次数已达上限，请基于现有结果回答' };
// toolCallCounts.set('web_search', count + 1);

// AiSdkService.ts: stopWhen 用 stepCountIs(maxTotalToolRounds + 1)
```

---

## 四、API 路由变更

### 新增路由

| 方法 | 路径 | 说明 |
|------|------|------|
| PUT | `/api/settings/iqs-key` | 更新 IQS API Key，触发 provider 重建 |
| GET | `/api/settings/iqs-key` | 获取 IQS Key 是否已配置（不返回完整值） |

实现位置：在 `server/src/routes/chat.ts` 中新增两个路由（与聊天共享 server 实例），或新建 `server/src/routes/settings.ts` 并挂载到 server。

### SSE 事件（已有，无需变更）

`tool_call` / `tool_result` 事件已在 chat.ts 中实现，新工具复用同一事件流。

### assistants 路由变更

```typescript
// PATCH /api/assistants/:id
// 新增可更新字段: enable_web_search
```

---

## 五、聊天流程变更

### 5.1 当前流程

```
发送消息 → 构建 system prompt → 构建 messages → 注入 tools（仅记忆开关）→ streamText → SSE done
```

### 5.2 新流程

```
1. 读取开关状态:
   memoryEnabled = assistant.enable_memory && 会话 memoryEnabled
   webSearchEnabled = assistant.enable_web_search && 会话 webSearchEnabled
   deepThinkingMode = 用户选择（auto / enabled / disabled）

2. 构建 tools = ToolDefinitionBuilder.buildTools({ memoryEnabled, webSearchEnabled })

3. 构建 providerOptions:
   if deepThinkingMode === 'enabled'  → thinking: { type: 'enabled' }
   if deepThinkingMode === 'disabled' → thinking: { type: 'disabled' }
   if deepThinkingMode === 'auto'     → 不传 providerOptions，跟随助手默认配置

4. 调用 chatStream(messages, ..., tools, maxSteps: Object.keys(tools).length * config.maxToolCallPerTool)
   — chat.ts 当前硬编码 maxSteps=3（第338行+第632行），两处均改为按工具数动态计算
   — /regenerate 路由（第362行）也需同步修改：tools 动态组合 + maxSteps
   ├── onToken/onReasoning → SSE
   ├── onToolCall → SSE tool_call → ToolExecutor.execute → SSE tool_result
   └── onFinish

5. 保存 assistant 消息到 DB:
   — 新增字段 tool_calls = JSON.stringify(本轮所有 tool call/result)
   — INSERT 语句增加 tool_calls 字段: ...tool_calls, turn_index, memory_enabled, privacy_mode) VALUES (..., ?, ?, ?, ?)
   — /regenerate 路由保存时同样写入 tool_calls

6. 异步: chunk 边界检测 + 记忆提取（与现有流程一致）
```

### 5.3 计数器初始化（chat.ts 中实现）

```typescript
// chat.ts 的 /completions 和 /regenerate 路由中，每次请求初始化：
const toolCallCounts = new Map<string, number>();

// 构建 tools 时，每个 tool 的 execute 包装计数逻辑：
function wrapWithLimit(toolName: string, execute: (args: any) => Promise<any>) {
  return async (args: any) => {
    const count = toolCallCounts.get(toolName) || 0;
    if (count >= config.maxToolCallPerTool) {
      return { error: `${toolName} 调用次数已达上限（${config.maxToolCallPerTool}次），请基于现有结果回答` };
    }
    toolCallCounts.set(toolName, count + 1);
    return execute(args);
  };
}

// 使用示例：
tools['web_search'] = {
  ...def,
  execute: wrapWithLimit('web_search', (args) => webSearchService.search(...)),
};
```

// AiSdkService.ts 已有 stopWhen: stepCountIs((maxSteps ?? 3) + 1)（第157行）
// chat.ts 传入 maxSteps = Object.keys(tools).length * config.maxToolCallPerTool

---

## 六、前端变更

### 6.1 输入框按钮区（ChatArea.tsx）

在输入框上方新增三个按钮，从左到右排列：

```
上下文轮数: 5/10   [🧠 记忆]  [🌐 联网]  [💭 自动▼]
```

**按钮组件设计：**

| 按钮 | 组件 | 状态 | 交互 |
|------|------|------|------|
| 🧠 记忆 | `MemoryToggleButton` | 开(紫)/关(灰) | 切换开关，继承助手 enable_memory |
| 🌐 联网 | `WebSearchToggleButton` | 开(绿)/关(灰) | 切换开关，继承助手 enable_web_search |
| 💭 深度思考 | `DeepThinkingButton` | 自动(灰)/开(蓝高亮)/关(灰划线) | 三态循环: auto→enabled→disabled→auto |

**状态管理（App.tsx）：**

```typescript
// 按会话维度（切换会话重置为助手默认值）
interface ConversationUIState {
  deepThinkingMode: 'auto' | 'enabled' | 'disabled';
  webSearchEnabled: boolean;
}
// state: Record<string, ConversationUIState>  — keyed by conversationId
```

### 6.2 工具调用块渲染（ChatArea.tsx）

在 assistant 消息气泡中，content 之前渲染 tool_calls 块：

```tsx
// ToolCallBlock 组件
function ToolCallBlock({ toolCall }: { toolCall: ToolCallEntry }) {
  const config = TOOL_DISPLAY_CONFIG[toolCall.toolName];

  return (
    <div className={`tool-call-block ${config.colorClass}`}>
      <span className="tool-icon">{config.icon}</span>
      <span className="tool-label">
        {toolCall.status === 'running'
          ? config.runningText
          : config.doneText(toolCall.result)}
      </span>
      {toolCall.args.query && (
        <span className="tool-query">: "{toolCall.args.query}"</span>
      )}
      <span className="tool-duration">{computeDuration(toolCall.startedAt, toolCall.completedAt)}ms</span>
    </div>
  );
}

// 配置常量
const TOOL_DISPLAY_CONFIG = {
  search_memory: {
    icon: '🧠', colorClass: 'tool-purple',
    runningText: '搜索记忆中...',
    doneText: (r: any[]) => `搜索到 ${r?.length || 0} 条相关记忆`,
  },
  recall_context: {
    icon: '💬', colorClass: 'tool-blue',
    runningText: '读取原始对话...',
    doneText: (r: any) => `读取了 ${r?.turns?.length || 0} 轮原始对话`,
  },
  recall_sources: {
    icon: '📎', colorClass: 'tool-indigo',
    runningText: '读取附件/网页...',
    doneText: (r: any) => `读取了 ${(r?.attachments?.length || 0) + (r?.webRetrievals?.length || 0)} 个来源`,
  },
  web_search: {
    icon: '🌐', colorClass: 'tool-green',
    runningText: '搜索网页中...',
    doneText: (r: any[]) => `搜索到 ${r?.length || 0} 条网页结果`,
  },
  web_fetch: {
    icon: '📄', colorClass: 'tool-teal',
    runningText: '读取网页中...',
    doneText: (r: any) => `读取了 ${r?.title || '网页全文'}`,
  },
};
```

**渲染位置（两种模式）：**

- **开深度思考**：reasoning 块 → tool_call 块(们) → Markdown content
- **不开深度思考**：tool_call 块(们) → Markdown content

### 6.3 嵌套展示（三层记忆工具）

```
🧠 搜索记忆: "成都 火锅"          · 3 条结果
  ├─ 💬 读取来源: mem_123        · 5 轮对话
  │   └─ 📎 读取文件: 行程表.xlsx · 12KB
  └─ 💬 读取来源: mem_456        · 2 轮对话

🌐 网页搜索: "成都火锅推荐"       · 5 条结果
  └─ 📄 读取网页: dianping.com    · 3.2KB
```

用缩进 + 树形连线（`├─` `└─`）展示层级关系。前端根据 tool_call 的时间顺序和 toolName 自动判断嵌套关系。

### 6.4 历史消息持久化渲染

```
加载历史消息 → 检查 msg.tool_calls
  ├── 有 → 渲染 ToolCallBlock（状态固定为 done，duration 从 started_at/completed_at 计算）
  └── 无 → 跳过，正常渲染 content
```

切换助手/会话后回来：tool_calls 从 DB 加载 → 完整显示，不会消失。

### 6.5 设置页新增（SettingsArea.tsx）

在系统设置中新增 "联网搜索" 区域：

```
┌─────────────────────────────────────┐
│  联网搜索                           │
│                                     │
│  API Key: [____________________]    │
│  引擎: 阿里云 IQS 信息查询服务       │
│  状态: ✅ 已配置 / ❌ 未配置         │
│                                     │
│  说明: API Key 在 IQS 控制台创建    │
│        https://iqs.console.aliyun.com│
└─────────────────────────────────────┘
```

### 6.6 助手设置新增（AssistantModal.tsx）

在记忆开关下方新增：

```
联网搜索: [✓] 默认启用联网搜索
          新建会话时该助手默认开启联网搜索
```

### 6.7 结果引用展示

网页搜索结果在 Markdown 回复中以 `[citation:X]` 格式引用，来源链接可点击。复用现有知识库引用渲染方式。

---

## 七、config.ts 变更

```typescript
// 新增
iqsApiKey: process.env.IQS_API_KEY || '',
iqsBaseUrl: 'https://cloud-iqs.aliyuncs.com',

// 工具调用轮次（每个工具独立计数）
maxToolCallPerTool: 3,       // 每个工具独立上限（5 个工具各 3 次）
// maxSteps 动态计算: Object.keys(tools).length × maxToolCallPerTool，不设固定全局上限
```

---

## 八、类型定义变更

### server/src/types/index.ts — 已有类型无需新增，仅追加

`ToolCallEntry` 已在记忆长期模块中定义（[server/src/types/index.ts:210](server/src/types/index.ts#L210)），直接复用。本次仅新增 web search 相关类型：

```typescript
// 新增（与 §3.1 WebSearchProvider 接口中的类型对应）
export interface SearchParams {
  query: string;
  maxResults?: number;
  timeRange?: 'NoLimit' | 'OneDay' | 'OneWeek' | 'OneMonth' | 'OneYear';
}

export interface SearchResult {
  title: string;
  url: string;
  snippet: string;
  summary?: string;
  publishedTime?: string;
  rerankScore?: number;
  hostname?: string;
  hostLogo?: string;
}

export interface PageContent {
  url: string;
  title: string;
  content: string;
  fetchedAt: string;
}
```

### src/types/index.ts — 已有类型补充，新增仅 2 个接口

`ToolCallEntry` 已在记忆长期模块中定义（[src/types/index.ts:160](src/types/index.ts#L160)），`Message.tool_calls` 字段已存在（[src/types/index.ts:73](src/types/index.ts#L73)）。本次仅新增：

```typescript
// 新增：前端展示用的工具调用块（含 startedAt/completedAt 用于显示耗时）
export interface ToolCallBlock {
  toolCallId: string;
  toolName: string;
  args: Record<string, any>;
  result?: any;
  status: 'running' | 'done' | 'error';
  startedAt: string;
  completedAt?: string;
}

// 新增：会话级 UI 状态（不持久化到 DB，跟随会话生命周期）
export interface ConversationUIState {
  deepThinkingMode: 'auto' | 'enabled' | 'disabled';
  webSearchEnabled: boolean;
}
```

---

## 九、文件清单

### 新建文件

| 文件 | 行数估算 |
|------|---------|
| `server/src/services/webSearch/WebSearchProvider.ts` | ~30 |
| `server/src/services/webSearch/IqsSearchProvider.ts` | ~120 |
| `server/src/services/WebSearchService.ts` | ~80 |
| `server/src/db/migrations/014_tool_calls.sql` | ~10 |
| `src/components/ToolCallBlock.tsx` | ~80 |
| `src/components/MemoryToggleButton.tsx` | ~40 |
| `src/components/WebSearchToggleButton.tsx` | ~50 |
| `src/components/DeepThinkingButton.tsx` | ~60 |

### 修改文件

| 文件 | 变更量 | 要点 |
|------|--------|------|
| `server/src/services/ToolDefinitionBuilder.ts` | +60 | 新增 web_search + web_fetch |
| `server/src/services/ToolExecutor.ts` | +10 | 分发 web_search/web_fetch |
| `server/src/routes/chat.ts` | +50 | tool_calls 写入、tools 动态组合、计数器初始化、/regenerate 同步 |
| `server/src/services/AiSdkService.ts` | +10 | translateError 新增 FC 错误翻译 |
| `server/src/config.ts` | +8 | IQS 参数 + maxToolCallPerTool + maxTotalToolRounds |
| `server/src/routes/assistants.ts` | +5 | enable_web_search |
| `server/src/types/index.ts` | +30 | 新类型 |
| `src/components/ChatArea.tsx` | +100 | 按钮区 + ToolCallBlock 渲染 |
| `src/components/AssistantModal.tsx` | +15 | 联网开关 |
| `src/components/SettingsArea.tsx` | +40 | IQS Key 配置 |
| `src/App.tsx` | +25 | state 管理 |
| `src/services/api.ts` | +10 | 新 API |
| `src/types/index.ts` | +25 | 新类型 |

---

## 十、关键风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| **IQS API 延迟波动** | 搜索步骤慢于预期 | LiteAdvanced 极速版已测 500-570ms；超时 10s fallback |
| **IQS readpage HTML 清洗质量差** | 网页正文提取不干净 | 用 `html-to-text` 库清洗；后续可选 jina.ai Reader 作为兜底 |
| **per-tool 计数在 SSE 流中并发** | 计数器状态不一致 | 每个 chatStream 调用用闭包计数器，不存在并发 |
| **前端 tool_call 块与 Markdown 流式渲染冲突** | UI 闪烁或顺序错乱 | tool_call 和 text 不在同一时刻出现（先 tool→后 text），天然隔离 |
| **深度思考三态按钮用户理解门槛** | 用户困惑三种状态的区别 | tooltip 说明文字；默认 auto 状态不影响现有体验 |
| **IQS Key 在前端传输安全性** | Key 泄露风险 | Key 只存后端，前端仅显示"已配置/未配置"，不返回完整值 |
| **不支持 FC 的模型注入 tools 后报错** | 用户看到英文错误，不知道原因 | translateError() 新增 FC 相关错误中文翻译，提示用户切换至支持 FC 的模型 |

---

## 十一、分阶段实施

### Phase 1：数据库 + 接口抽象（0.5 天）
1. 迁移 014（messages.tool_calls + assistants.enable_web_search）
2. `WebSearchProvider` 接口 + `IqsSearchProvider` 实现
3. `WebSearchService` 协调层
4. config.ts 参数新增
5. 类型定义更新（server + 前端）

### Phase 2：工具定义 + chat 流程（1 天）
1. ToolDefinitionBuilder 新增 `web_search` + `web_fetch`
2. ToolExecutor 分发
3. chat.ts：tools 动态组合 + tool_calls 写入 + per-tool 计数器 + /regenerate 同步
4. AiSdkService：translateError 新增 FC 错误中文翻译
5. assistants 路由：enable_web_search
6. 设置页 IQS Key API

### Phase 3：前端按钮 + 工具调用渲染（1.5 天）
1. ChatArea 按钮区：MemoryToggle + WebSearchToggle + DeepThinkingButton
2. ToolCallBlock 组件（5 种 icon/颜色/动画）
3. 历史消息 tool_calls 持久化渲染
4. SettingsArea IQS Key 配置
5. AssistantModal 联网默认开关

### Phase 4：联调测试（1 天）
1. 端到端：记忆搜索 + 网页搜索并行 → web_fetch → 最终回复
2. 深度思考三态切换：auto/enabled/disabled 各自验证
3. 历史消息：切换助手/会话后 tool_calls 完整显示
4. 边界：不支持 FC 的模型报错提示、工具上限触发
5. 计费验证：IQS 控制台查看消耗

---

## 十二、与已有记忆系统的关系

```
本次变更与已有的长期记忆系统（对话长期记忆研发实施方案）是并行关系：

已有:
  ├── search_memory / recall_context / recall_sources（三层记忆工具）
  ├── ConversationChunkService（L0/L1 chunk 管理）
  ├── MemoryRetrievalService（L2 双通道检索）
  ├── MemoryService（importance/source_links/状态管理）
  └── conversation_chunks / memory_source_links 表

本次新增:
  ├── web_search / web_fetch（两层网页工具）
  ├── WebSearchProvider / IqsSearchProvider / WebSearchService
  ├── messages.tool_calls 字段（所有工具的调用记录持久化）
  ├── per-tool 调用上限（替代全局 MAX_TOOL_ROUNDS）
  ├── 前端按钮区（深度思考三态 + 联网搜索开关）
  └── ToolCallBlock 渲染（统一展示记忆和网页工具调用）

共享:
  ├── AiSdkService tools 机制（已跑通）
  ├── ToolDefinitionBuilder 统一出口
  ├── ToolExecutor 统一分发
  ├── chat.ts 工具调用循环（已有 maxSteps）
  └── SSE tool_call/tool_result 事件（已有）
```

---

## 涉及的关键文件汇总

| 文件 | 类型 | 说明 |
|------|------|------|
| `server/src/db/migrations/014_tool_calls.sql` | 新建 | tool_calls 字段 + enable_web_search |
| `server/src/services/webSearch/WebSearchProvider.ts` | 新建 | 搜索接口抽象 |
| `server/src/services/webSearch/IqsSearchProvider.ts` | 新建 | 阿里云 IQS 实现 |
| `server/src/services/WebSearchService.ts` | 新建 | 搜索协调层 |
| `server/src/services/ToolDefinitionBuilder.ts` | 修改 | +web_search +web_fetch |
| `server/src/services/ToolExecutor.ts` | 修改 | +2 case |
| `server/src/routes/chat.ts` | 修改 | tools 动态组合 + tool_calls 入库 + 计数器初始化 + /regenerate 同步 |
| `server/src/services/AiSdkService.ts` | 修改 | translateError 新增 FC 错误中文翻译 |
| `server/src/config.ts` | 修改 | IQS 参数 + maxToolCallPerTool + maxTotalToolRounds |
| `server/src/routes/assistants.ts` | 修改 | enable_web_search |
| `server/src/routes/settings.ts` | 新建/修改 | GET/PUT /api/settings/iqs-key |
| `server/src/types/index.ts` | 修改 | SearchParams + SearchResult + PageContent |
| `src/components/ChatArea.tsx` | 修改 | 按钮区 + 渲染 |
| `src/components/ToolCallBlock.tsx` | 新建 | 工具调用块组件 |
| `src/components/MemoryToggleButton.tsx` | 新建 | 记忆开关按钮 |
| `src/components/WebSearchToggleButton.tsx` | 新建 | 联网开关按钮 |
| `src/components/DeepThinkingButton.tsx` | 新建 | 深度思考三态按钮 |
| `src/components/AssistantModal.tsx` | 修改 | 联网默认开关 |
| `src/components/SettingsArea.tsx` | 修改 | IQS Key 配置 |
| `src/App.tsx` | 修改 | UI state |
| `src/services/api.ts` | 修改 | 新 API |
| `src/types/index.ts` | 修改 | 新类型 |
