# 对话长期记忆 + 联网搜索 — 全量功能测试文档

> 基于产品方案 v2 和研发实施方案，覆盖前后端全链路功能验证。

---

## 一、测试环境准备

### 1.1 前置条件

| 条件 | 说明 |
|------|------|
| 后端服务 | `npx tsx server/src/index.ts` 启动在 localhost:3001 |
| 前端应用 | `npx vite` 启动在 localhost:5173 |
| 数据库 | SQLite，路径 `data/aichat.db`，迁移已执行到 014 |
| 模型 | 需配置至少一个支持 Function Calling 的模型（如 qwen-plus、GPT-4o） |
| Embedding | 需配置 embedding 提供商和模型 |
| Reranker | 需配置硅基流动 Rerank API Key |
| 网络 | 联网搜索测试需要有效的 IQS API Key |

### 1.2 测试数据准备

```bash
# 1. 创建测试助手（开启记忆）
curl -s http://localhost:3001/api/assistants -X POST \
  -H "Content-Type: application/json" \
  -d '{
    "name": "记忆测试助手",
    "system_prompt": "你是一个友好的助手，拥有长期记忆能力。",
    "model_id": "<model_id>",
    "provider_id": "<provider_id>",
    "enable_memory": 1,
    "context_rounds": 10
  }'

# 2. 确认记忆设置已配置
curl -s http://localhost:3001/api/memory/settings

# 3. 配置用户个人信息（用于验证 system prompt 注入）
curl -s http://localhost:3001/api/profile -X PUT \
  -H "Content-Type: application/json" \
  -d '{"昵称":"测试用户","职业":"QA工程师","位置":"杭州"}'
```

---

## 二、核心功能测试用例

### 模块 A：三层时间模型 (L0/L1/L2)

#### A-1: L0 热层 — 限定轮次内不提取

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-A-1-001 |
| **测试目标** | 验证 L0 轮次内（≤ context_rounds）的消息不被 chunk 化 |
| **前置条件** | 助手 context_rounds=10，enable_memory=1 |
| **操作步骤** | 1. 创建新对话<br>2. 发送 5 轮简短对话（如 "你好" → 回复 → "今天天气怎么样" → 回复...）<br>3. 查询 conversation_chunks 表 |
| **预期结果** | conversation_chunks 表中无极对话的 chunk 记录 |
| **验证 SQL** | `SELECT * FROM conversation_chunks WHERE conversation_id = '<conv_id>'` |

#### A-2: L1 温层 — 超出轮次后首条创建 chunk

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-A-2-001 |
| **测试目标** | 第 11 个 turn 超出 L0 后自动创建第一个 chunk |
| **前置条件** | 助手 context_rounds=10，已有恰好 10 轮对话 |
| **操作步骤** | 发送第 11 条消息 |
| **预期结果** | conversation_chunks 表中出现 1 条 status='open' 的记录，start_turn_index=11 |
| **验证 SQL** | `SELECT * FROM conversation_chunks WHERE conversation_id = '<conv_id>' AND status = 'open'` |

#### A-3: 语义切分 — 同话题续接

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-A-3-001 |
| **测试目标** | 连续同一话题的 turn 归入同一 chunk |
| **前置条件** | L1 已有 open chunk，当前话题为"Python 性能优化" |
| **操作步骤** | 1. 发送 "CPython 和 PyPy 的 JIT 有什么区别？"<br>2. 发送 "Numba 的 LLVM 后端怎么工作的？" |
| **预期结果** | 两条消息归入同一个 open chunk，end_turn_index 递增 |
| **验证 SQL** | `SELECT cc.id, cc.start_turn_index, cc.end_turn_index FROM conversation_chunks cc WHERE cc.conversation_id = '<conv_id>' ORDER BY cc.created_at` |

#### A-4: 语义切分 — 话题切换闭合

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-A-4-001 |
| **测试目标** | 话题骤变时 chunk 闭合，新 chunk 创建 |
| **前置条件** | L1 已有 open chunk 讨论"技术话题" |
| **操作步骤** | 1. 发送 "我们决定用 Rust 重写网关层，预计 QPS 要到 10 万"<br>2. 发送 "周末去西湖划船怎么样？"（话题骤变） |
| **预期结果** | 1. 第一条消息归入当前 open chunk<br>2. 第二条触发 chunk 闭合（status→'closed'），并创建新的 open chunk<br>3. 闭合的 chunk 异步触发记忆提取 |
| **验证方式** | 1. 查 conversation_chunks 状态变化<br>2. 几分钟后查 memories 表是否有新记忆 |

#### A-5: L2 冷层 — 记忆检索

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-A-5-001 |
| **测试目标** | 已提取的记忆可通过 search_memory 工具检索 |
| **前置条件** | memories 表中有已提取的活跃记忆 |
| **操作步骤** | 在新对话中发送："我之前聊过我的技术偏好吗？" |
| **预期结果** | 1. SSE 事件流中出现 `tool_call`（search_memory）<br>2. 出现 `tool_result` 返回记忆列表<br>3. LLM 回复中引用记忆内容 |
| **验证方式** | 检查 SSE 事件和 LLM 回复内容 |

---

### 模块 B：记忆提取触发

#### B-1: 触发 A — 话题切换触发

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-B-1-001 |
| **测试目标** | chunk 闭合后自动触发记忆提取 |
| **前置条件** | L1 中有闭合的 chunk（status='closed'），包含用户个人信息相关对话 |
| **操作步骤** | 1. 完成话题切换（TC-A-4）<br>2. 等待 30-60 秒（异步提取）<br>3. 检查 memories 表和 memory_source_links 表 |
| **预期结果** | 1. memories 表有新记录，`extraction_trigger='topic_shift'`<br>2. memory_source_links 表有对应的 source_link 记录<br>3. chunk status 变为 'extracted' |
| **验证 SQL** | `SELECT * FROM memories WHERE json_extract(metadata, '$.extraction_trigger') = 'topic_shift' ORDER BY created_at DESC` |

#### B-2: 触发 B — 空闲超时触发

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-B-2-001 |
| **测试目标** | 会话空闲超过 1 小时后强制提取 |
| **前置条件** | 助手 context_rounds 较小（如 3），已有超出 L0 的 open chunk，会话最后活动时间 > 1 小时前 |
| **操作步骤** | 1. 修改 sessionIdleTimeoutMs 配置为短时间（如 60000 = 1 分钟）用于测试<br>2. 等待空闲检测器扫描（15 分钟间隔，测试时可手动触发）<br>3. 检查 chunk 状态和记忆 |
| **预期结果** | open chunk 被强制闭合，status → 'closed' → 'extracted'，`extraction_trigger='session_idle'` |
| **验证 SQL** | `SELECT * FROM memories WHERE json_extract(metadata, '$.extraction_trigger') = 'session_idle'` |

#### B-3: 隐私模式消息不提取

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-B-3-001 |
| **测试目标** | privacy_mode=1 的消息不被记忆提取 |
| **前置条件** | 会话 privacy_mode=1 |
| **操作步骤** | 1. 开启隐私模式<br>2. 发送包含个人信息的消息<br>3. 手动触发 chunk 提取 |
| **预期结果** | 提取时过滤掉 privacy_mode=1 的消息，不产生新记忆 |
| **验证方式** | 检查 memories 表无新增相关记录 |

---

### 模块 C：三层记忆检索工具

#### C-1: search_memory — 第一层检索

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-C-1-001 |
| **测试目标** | LLM 通过 search_memory 工具搜索记忆 |
| **前置条件** | 助手 enable_memory=1，memories 表有记忆数据 |
| **操作步骤** | 发送需要查记忆的查询："还记得我之前聊过什么吗？" |
| **预期结果** | 1. SSE 出现 `tool_call` 事件，toolName='search_memory'<br>2. `tool_result` 返回记忆摘要列表（含 memory_id, content, topic, importance, source_count）<br>3. 返回条数 ≤ 5 |
| **验证指标** | tool_result 的 args.query 为 LLM 自主生成的检索词 |

#### C-2: recall_context — 第二层深入

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-C-2-001 |
| **测试目标** | LLM 通过 recall_context 获取记忆的原始对话上下文 |
| **前置条件** | LLM 已通过 search_memory 获取到 memory_id |
| **操作步骤** | 发送需要详细上下文的问题："上次讨论数据库选型时，具体说了哪些方案的优缺点？" |
| **预期结果** | 1. 可能先调 search_memory<br>2. 再调 recall_context（如果 LLM 判断需要）<br>3. recall_context 返回 turn 原文 + sources（仅名称和类型，不含全文） |
| **验证方式** | SSE 事件中出现 toolName='recall_context' 的 tool_call |

#### C-3: recall_sources — 第三层附件全文

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-C-3-001 |
| **测试目标** | LLM 通过 recall_sources 获取附件和网页全文 |
| **前置条件** | 记忆关联了文件附件 |
| **操作步骤** | 发送需要文件内容的问题："上次上传的那个 PDF 里 QPS 数据是多少？" |
| **预期结果** | 1. recall_sources 返回附件真实内容（最多 50000 字符）<br>2. 图片文件返回描述文本而非二进制 |
| **验证方式** | tool_result 的 attachments[].content 包含文件内容而非占位符 |

#### C-4: 工具调用上限

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-C-4-001 |
| **测试目标** | 单个工具调用次数不超过 maxToolCallPerTool(3) |
| **前置条件** | 助手 enable_memory=1 |
| **操作步骤** | 构造需要反复检索的复杂查询 |
| **预期结果** | 每个工具最多调用 3 次，超出时返回错误提示 |
| **验证方式** | 统计 SSE 事件中各 toolName 的出现次数 |

---

### 模块 D：检索管线

#### D-1: 双通道检索

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-D-1-001 |
| **测试目标** | 验证通道 A（时间感知）和通道 B（语义兜底）各自返回 top-15 |
| **前置条件** | memories 表有 ≥ 20 条活跃记忆 |
| **操作步骤** | 发送需要查记忆的查询 |
| **预期结果** | 内部日志显示：通道 A 取 15 条 + 通道 B 取 15 条 → RRF 融合 → top-20 |
| **验证方式** | 检查服务端日志 MemoryRetrievalService 输出 |

#### D-2: 五信号加权

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-D-2-001 |
| **测试目标** | 最终排序使用五信号加权：sem(0.35) + rec(0.20) + timeDecay(0.20) + freq(0.10) + imp(0.15) |
| **前置条件** | 有多次记忆检索历史（access_count > 0 的记录） |
| **操作步骤** | 1. 多次查询同一主题的记忆<br>2. 观察 access_count 和 last_accessed 更新 |
| **预期结果** | 1. 被命中的记忆 access_count 递增，last_accessed 更新<br>2. 高频记忆在后续检索中排名更靠前 |
| **验证 SQL** | `SELECT id, topic, access_count, last_accessed FROM memories WHERE status = 'active' ORDER BY access_count DESC` |

#### D-3: Reranker 精排

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-D-3-001 |
| **测试目标** | Reranker（硅基流动 Cross-Encoder）在 RRF 融合后精排 |
| **前置条件** | Reranker API Key 已配置 |
| **操作步骤** | 发送记忆搜索查询 |
| **预期结果** | 1. 日志显示 Reranker 调用成功<br>2. 返回的 top-5 按 rerank 分数排序 |
| **验证方式** | 检查 RerankerService 日志 |

---

### 模块 E：全局记忆开关与隐私模式

#### E-1: 全局记忆开关 — 关闭

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-E-1-001 |
| **测试目标** | enable_memory=0 时不注入记忆工具，不提取记忆 |
| **前置条件** | 创建 enable_memory=0 的助手 |
| **操作步骤** | 1. 使用该助手发送消息<br>2. 检查 SSE 事件和 messages 表 |
| **预期结果** | 1. 无 tool_call 事件<br>2. messages.memory_enabled=0 |
| **验证方式** | SSE 事件流和数据库 |

#### E-2: 全局记忆开关 — 开启（正常模式）

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-E-2-001 |
| **测试目标** | enable_memory=1 时正常注入工具和提取 |
| **前置条件** | 助手 enable_memory=1 |
| **操作步骤** | 发送消息 |
| **预期结果** | 1. messages.memory_enabled=1, privacy_mode=0<br>2. tools 中包含 search_memory/recall_context/recall_sources |
| **验证方式** | 数据库和 SSE |

#### E-3: 隐私模式 — 开启

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-E-3-001 |
| **测试目标** | 会话级隐私模式阻止消息被提取 |
| **前置条件** | 助手 enable_memory=1 |
| **操作步骤** | 1. 开启会话隐私模式（PATCH conversations/:id privacy_mode=1）<br>2. 发送消息<br>3. 检查数据库 |
| **预期结果** | 1. conversations.privacy_mode=1<br>2. messages.privacy_mode=1<br>3. 提取 Agent 跳过该消息 |
| **验证 SQL** | `SELECT id, privacy_mode FROM messages WHERE conversation_id = '<conv_id>' ORDER BY created_at DESC` |

#### E-4: 隐私模式 — 不阻断检索

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-E-4-001 |
| **测试目标** | 隐私模式下 LLM 仍能通过 search_memory 检索已有记忆 |
| **前置条件** | 隐私模式开启，memories 表有数据 |
| **操作步骤** | 发送需要查记忆的查询 |
| **预期结果** | tool_call 正常触发 search_memory，tool_result 返回记忆 |

---

### 模块 F：联网搜索

#### F-1: 联网搜索开关

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-F-1-001 |
| **测试目标** | enable_web_search=0 时不注入 web_search 工具 |
| **前置条件** | 助手 enable_web_search=0 |
| **操作步骤** | 发送需要搜索的查询 |
| **预期结果** | tools 中不包含 web_search 和 web_fetch |

#### F-2: web_search 工具调用

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-F-2-001 |
| **测试目标** | LLM 通过 web_search 搜索互联网 |
| **前置条件** | 助手 enable_web_search=1，IQS API Key 已配置 |
| **操作步骤** | 发送 "搜索最新的 React 19 新特性" |
| **预期结果** | 1. SSE 出现 toolName='web_search' 的 tool_call<br>2. tool_result 返回搜索结果数组<br>3. 结果含 hostLogo 字段 |
| **验证方式** | SSE 事件流 |

#### F-3: web_fetch 工具调用

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-F-3-001 |
| **测试目标** | LLM 通过 web_fetch 读取网页内容 |
| **前置条件** | web_search 已返回 URL |
| **操作步骤** | 发送 "帮我打开 React 19 的官方博客看看具体内容" |
| **预期结果** | 1. SSE 出现 toolName='web_fetch' 的 tool_call<br>2. tool_result 返回页面标题和正文 |
| **验证方式** | SSE 事件流 |

#### F-4: IQS Key 管理

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-F-4-001 |
| **测试目标** | IQS API Key 配置、查询、更新 |
| **前置条件** | 无 |
| **操作步骤** | 1. GET /api/settings/iqs-key → 确认 { configured: false }<br>2. PUT /api/settings/iqs-key { apiKey: "test-key" }<br>3. GET /api/settings/iqs-key → 确认 { configured: true, masked: "test***-key" } |
| **预期结果** | 配置成功，搜索引擎实例重新初始化 |
| **后端日志** | `[WebSearch] IQS 搜索引擎已就绪` |

---

### 模块 G：前端交互

#### G-1: 工具调用块渲染

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-G-1-001 |
| **测试目标** | ChatArea 正确渲染工具调用块 |
| **前置条件** | 对话中有工具调用（如 search_memory） |
| **操作步骤** | 1. 发送需要查记忆的查询<br>2. 观察 ChatArea 消息气泡 |
| **预期结果** | 1. 消息气泡中显示 ToolCallBlock 组件<br>2. search_memory 显示大脑图标 + "正在搜索记忆..."<br>3. 结果返回后显示 "找到 N 条相关记忆"<br>4. 每个工具有独立的图标和颜色 |

#### G-2: 深度思考按钮

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-G-2-001 |
| **测试目标** | DeepThinkingButton 三态切换 |
| **前置条件** | 在 ChatArea 中可见 |
| **操作步骤** | 1. 默认状态：auto（跟随助手）<br>2. 点击切换为 enabled（蓝色高亮）<br>3. 再次点击切换为 disabled（关闭思考）<br>4. 再次点击回到 auto |
| **预期结果** | 1. 三态循环切换<br>2. 选中态蓝色高亮<br>3. 状态按对话独立保存 |

#### G-3: 联网搜索按钮

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-G-3-001 |
| **测试目标** | WebSearchToggleButton 二态切换 |
| **前置条件** | 助手 enable_web_search=1 |
| **操作步骤** | 点击联网按钮切换 on/off |
| **预期结果** | 1. ON 状态绿色 + Globe 图标<br>2. OFF 状态灰色<br>3. 状态按对话独立保存 |

#### G-4: 助手设置 — 联网搜索开关

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-G-4-001 |
| **测试目标** | AssistantModal 中联网搜索开关 |
| **前置条件** | 打开助手编辑弹窗 |
| **操作步骤** | 切换"联网搜索"开关，保存 |
| **预期结果** | assistants 表 enable_web_search 字段更新 |

#### G-5: 设置页 — 记忆管理增强

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-G-5-001 |
| **测试目标** | SettingsArea 记忆 tab 的增��列和操作 |
| **前置条件** | memories 表有数据 |
| **操作步骤** | 1. 打开设置 → 记忆 tab<br>2. 查看表格列<br>3. 点击某条记忆的"查看来源"<br>4. 点击"跳转" |
| **预期结果** | 1. 表格显示 topic, importance, status, access_count 列<br>2. 弹窗显示记忆详情 + 关联 chunk<br>3. 有 conversation_id 的 chunk 可点击跳转 |

#### G-6: 设置页 — 个人信息编辑

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-G-6-001 |
| **测试目标** | SettingsArea 个人信息 tab 的 CRUD |
| **前置条件** | 打开设置 → 个人信息 tab |
| **操作步骤** | 1. 添加字段（key=昵称, value=测试）<br>2. 编辑字段<br>3. 删除字段 |
| **预期结果** | user_profile 表正确增删改 |

#### G-7: 设置页 — IQS Key 配置

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-G-7-001 |
| **测试目标** | SettingsArea 模型 tab 中 IQS Key 配置区域 |
| **前置条件** | 打开设置 → 模型 tab |
| **操作步骤** | 1. 查看 "IQS 搜索 Key" 状态<br>2. 输入 Key 并保存<br>3. 移除 Key |
| **预期结果** | 1. 未配置时显示"未配置" + 输入框<br>2. 配置后显示 masked key + "移除"按钮<br>3. 与后端 API 同步 |

---

### 模块 H：边界与异常

#### H-1: FC 错误处理

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-H-1-001 |
| **测试目标** | 不支持 FC 的模型使用记忆/联网时返回友好错误 |
| **前置条件** | 助手使用不支持 function calling 的模型 |
| **操作步骤** | 发送消息 |
| **预期结果** | 返回提示："该模型不支持工具调用（记忆搜索/联网搜索），请关闭记忆/联网开关后重发..." |
| **验证方式** | AiSdkService 中正则匹配 /function\s*call\|tool\s*call\|tools\|invalid.*tool/i |

#### H-2: 工具调用轮次超限

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-H-2-001 |
| **测试目标** | 总工具调用轮次不超过 maxTotalToolRounds(6) |
| **前置条件** | 复杂查询可能触发多轮工具调用 |
| **操作步骤** | 构造需要反复检索的查询 |
| **预期结果** | 最多 6 轮后停止，不会无限循环 |

#### H-3: 空记忆检索结果

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-H-3-001 |
| **测试目标** | 无匹配记忆时正常返回 |
| **前置条件** | 查询与任何记忆不相关 |
| **操作步骤** | 发送 "搜索关于 'xyzzy_nonexistent_123' 的记忆" |
| **预期结果** | tool_result 返回空数组，不报错 |

#### H-4: 客户端断连中断

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-H-4-001 |
| **测试目标** | 客户端断开时中止 LLM 调用 |
| **前置条件** | 正在流式生成中 |
| **操作步骤** | 关闭浏览器 tab |
| **预期结果** | LLM 调用被 AbortController 中止，不崩溃 |

#### H-5: embedding API 失败降级

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-H-5-001 |
| **测试目标** | embedding 不可用时 chunk 切分不影响聊天 |
| **前置条件** | 模拟 embedding API 不可达 |
| **操作步骤** | 发送消息 |
| **预期结果** | 聊天正常进行，chunk 切分日志报错但不阻塞消息 |

#### H-6: 附件文件不存在

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-H-6-001 |
| **测试目标** | recall_sources 处理文件路径无效的情况 |
| **前置条件** | memory_source_links 关联的文件已被物理删除 |
| **操作步骤** | 触发 recall_sources |
| **预期结果** | 返回 `[无法读取文件: <name>]` 而非崩溃 |

#### H-7: 并发提取保护

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-H-7-001 |
| **测试目标** | 多个空闲检测周期不能并发提取同一 chunk |
| **前置条件** | chunk status='extracting' |
| **操作步骤** | 手动模拟并发提取 |
| **预期结果** | extracting 状态阻止重复提取；30 分钟超时后自动重置 |

---

## 三、端到端流程测试

### 流程 1：完整记忆生命周期

```
用户创建助手（开启记忆）
  → 用户发起对话（阐述个人信息、偏好）
  → 多轮对话积累（同一话题 3-5 轮）
  → 话题切换（chunk 闭合 + 记忆提取）
  → 新对话中查询记忆（search_memory 工具调用）
  → 深入查看原文（recall_context）
  → 查看来源跳转到原始对话
```

### 流程 2：隐私模式完整链路

```
开启对话的隐私模式
  → 发送敏感信息
  → 恢复普通模式
  → 验证敏感信息未被提取为记忆
  → 验证隐私模式期间仍可检索已有记忆
```

### 流程 3：联网搜索完整链路

```
配置 IQS Key
  → 创建/编辑助手开启联网搜索
  → 对话中发送需要实时信息的问题
  → LLM 调用 web_search 工具
  → 返回搜索结果
  → LLM 可选调 web_fetch 获取详细内容
  → 最终回复引用搜索结果
```

---

## 四、性能基准

| 指标 | 目标值 | 测量方式 |
|------|--------|---------|
| TTFT（含 KB 检索） | < 8s | metrics.ttftMs |
| 记忆检索（含双通道+RRF+Reranker） | < 3s | 服务端日志计时 |
| SSE tool_call → tool_result 延迟 | < 2s | 时间戳差值 |
| 记忆提取（单 chunk） | < 30s | 异步后台，不阻塞 |
| 前端工具调用渲染延迟 | < 100ms | React DevTools |

---

## 五、测试检查清单

### 接口覆盖

- [x] GET /api/assistants — 助手列表（含 enable_memory, enable_web_search）
- [x] POST/PUT/DELETE /api/assistants — 助手 CRUD
- [x] GET/POST/PATCH/DELETE /api/conversations — 对话 CRUD + 隐私模式
- [x] GET/POST /api/chat/completions — 聊天 SSE（含工具调用）
- [x] POST /api/chat/regenerate — 重新生成 SSE（含工具调用）
- [x] GET/PUT/DELETE /api/profile — 个人信息 CRUD
- [x] GET/PUT /api/settings/iqs-key — IQS Key 管理
- [x] GET /api/memory — 记忆列表
- [x] GET /api/memory/settings — 记忆设置
- [x] PUT /api/memory/settings — 更新记忆设置
- [x] GET /api/memory/:id — 记忆详情（含 source_links + conversation_id）
- [x] GET /api/memory/:id/sources — 记忆来源
- [x] PUT /api/memory/:id — 编辑记忆
- [x] DELETE /api/memory/:id — 删除记忆
- [x] POST /api/memory/search — 记忆语义搜索
- [x] GET /api/memory/history — 记忆变更历史

### SSE 事件覆盖

- [x] `meta` — 对话 ID + citations
- [x] `status` — 状态提示（如"正在检索知识库..."）
- [x] `token` — 内容 token
- [x] `reasoning` — 思考 token
- [x] `parsed` — 思考/内容分离
- [x] `done` — 完成（含 metrics, timing）
- [x] `error` — 错误
- [x] `tool_call` — 工具调用发起
- [x] `tool_result` — 工具调用结果

### 数据库表覆盖

- [x] assistants（enable_memory, enable_web_search）
- [x] conversations（privacy_mode）
- [x] messages（turn_index, memory_enabled, privacy_mode, tool_calls）
- [x] conversation_chunks（status, start/end_turn_index, embedding）
- [x] chunk_messages（chunk_id, message_id, turn_index, role）
- [x] memory_source_links（memory_id, chunk_id, source_type）
- [x] message_attachments（message_id, type, name, path）
- [x] message_web_retrievals（message_id, url, title, snippet）
- [x] memories（topic, importance, metadata, status, superseded_by, valid_from, valid_until, access_count, last_accessed）
- [x] user_profile（key, value）

---

## 六、已知合理偏离

| 序号 | 偏离项 | 原因 |
|------|--------|------|
| 1 | L0 消息放入 messages 数组而非 system prompt | Vercel AI SDK 标准做法，context_rounds 控制范围 |
| 2 | maxTotalToolRounds=6 而非方案中的 3 | 双层限流更灵活（6 全局 + 3 单工具） |
| 3 | maxTurnsPerChunk=8 强制闭合 | 安全防护，防止永不闭合 |
| 4 | FactExtraction 多出 importance 字段 | LLM 优先的增强，回退到启发式 |
| 5 | 联网搜索工具 (web_search/web_fetch) | 方案外独立功能，与记忆系统并行 |
