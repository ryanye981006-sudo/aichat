# 查询改写与网页搜索 — 功能测试文档

> 基于[查询改写与网页搜索产品方案](./查询改写与网页搜索产品方案.md)和[查询改写与网页搜索研发实施方案](./查询改写与网页搜索研发实施方案.md)，覆盖前后端全链路功能验证。

---

## 一、测试环境准备

### 1.1 前置条件

| 条件 | 说明 |
|------|------|
| 后端服务 | `npx tsx server/src/index.ts` 启动在 localhost:3001 |
| 前端应用 | `npx vite` 启动在 localhost:5173 |
| 数据库 | SQLite，路径 `data/aichat.db`，迁移已执行到 014 |
| 模型 | 需配置至少一个支持 Function Calling 的模型（如 qwen-plus、GPT-4o） |
| 搜索引擎 | 需配置 IQS API Key（.env 或 Settings 页面） |

### 1.2 测试数据准备

```bash
# 1. 创建测试助手（开启联网搜索 + 记忆）
curl -s http://localhost:3001/api/assistants -X POST \
  -H "Content-Type: application/json" \
  -d '{
    "name": "联网搜索测试助手",
    "system_prompt": "你是一个友好的助手，支持联网搜索和长期记忆。",
    "model_id": "<model_id>",
    "provider_id": "<provider_id>",
    "enable_memory": 1,
    "enable_web_search": 1,
    "context_rounds": 10
  }'

# 2. 确认 IQS Key 已配置
curl -s http://localhost:3001/api/settings/iqs-key
# 预期: { "configured": true, "masked": "-wxw***Yg", "source": "env" }

# 3. 如果未配置，通过 API 写入
curl -s http://localhost:3001/api/settings/iqs-key -X PUT \
  -H "Content-Type: application/json" \
  -d '{"apiKey":"<your_iqs_key>"}'
```

---

## 二、核心功能测试用例

### 模块 A：联网搜索工具

#### A-1: web_search 工具调用

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-A-1-001 |
| **测试目标** | LLM 通过 web_search 工具搜索互联网，返回带引用编号的结果 |
| **前置条件** | 助手 enable_web_search=1，IQS API Key 已配置 |
| **操作步骤** | 发送 "搜索最新的 React 19 新特性" |
| **预期结果** | 1. SSE 出现 `tool_call` 事件，toolName='web_search'<br>2. `tool_result` 返回格式：`{ raw: [...], formatted: "[citation:1]...\nURL:...\n摘要:...", citations: [{id, title, url, snippet}] }`<br>3. LLM 回复中引用搜索结果，使用 `[citation:N](URL)` 格式的 Markdown 链接 |
| **验证方式** | 检查 SSE 事件流和 LLM 回复中的可点击链接 |

#### A-2: web_fetch 工具调用

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-A-2-001 |
| **测试目标** | LLM 通过 web_fetch 读取搜索结果的网页全文 |
| **前置条件** | web_search 已返回 URL |
| **操作步骤** | 发送 "帮我打开 React 19 的官方博客看看具体更新了什么" |
| **预期结果** | 1. SSE 出现 `tool_call`（可能是 web_search → web_fetch 两步）<br>2. `tool_result` 返回 `{ url, title, content, fetchedAt }`<br>3. content 为清洗后的纯文本，最大 5000 字符 |
| **验证方式** | SSE 事件流 |

#### A-3: web_fetch 的 basic / scrape 模式

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-A-2-002 |
| **测试目标** | LLM 根据网页类型选择合适的抓取模式 |
| **前置条件** | 同上 |
| **操作步骤** | 发送 "帮我看看这个页面的完整内容 https://example.com/spa-page" |
| **预期结果** | LLM 可能选择 scrape 模式（JS 渲染页面），工具调用参数 `mode='scrape'` |
| **验证方式** | 检查 tool_call 的 args.mode |

---

### 模块 B：查询改写（深度思考联动）

#### B-1: 开深度思考时，思考链中体现检索决策

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-B-1-001 |
| **测试目标** | 深度思考模型在 reasoning 中自然完成查询改写，不做独立 LLM 调用 |
| **前置条件** | 助手使用支持深度思考的模型，深度思考按钮设为"自动"或"强制开启" |
| **操作步骤** | 发送 "我去年在成都出差时吃的那个火锅叫什么来着" |
| **预期结果** | 1. 前端显示 reasoning tokens（思考链）<br>2. 思考内容中包含检索决策（如"需要搜索记忆: 成都 火锅 出差"）<br>3. 思考链结束后发出 tool_call<br>4. 无独立的查询改写 API 调用 |
| **验证方式** | 前端 ThinkBlock 渲染 + SSE `reasoning` 事件 |

#### B-2: 不开深度思考时，直接搜索

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-B-2-001 |
| **测试目标** | 不开深度思考时不产生 reasoning tokens，模型直接用原始 query 搜索 |
| **前置条件** | 深度思考按钮设为"强制关闭"，或使用不支持 reasoning 的模型 |
| **操作步骤** | 发送 "今天成都有什么新闻" |
| **预期结果** | 1. 无 reasoning tokens<br>2. 直接发出 web_search tool_call，参数 query 为用户原始提问或简单提炼<br>3. 无额外延迟 |
| **验证方式** | SSE 事件流中无 `reasoning` 事件，直接出现 `tool_call` |

---

### 模块 C：引用标注格式

#### C-1: web_search 结果带 [citation:N] 编号

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-C-1-001 |
| **测试目标** | WebSearchService.formatCitations 将搜索结果格式化为带编号的引用文本 |
| **前置条件** | web_search 工具被调用 |
| **操作步骤** | 发送需要搜索的问题，观察 tool_result |
| **预期结果** | 1. tool_result.formatted 包含 `[citation:1]`、`[citation:2]` 等编号<br>2. 每条引用含 `URL:` 和 `摘要:` 行<br>3. tool_result.citations 数组中每项含 id、title、url、snippet |
| **验证方式** | 检查 SSE `tool_result` 事件的 data |

#### C-2: LLM 回复中的引用链接可点击

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-C-2-001 |
| **测试目标** | LLM 在回复中使用 `[citation:N](URL)` Markdown 链接，前端渲染为可点击链接 |
| **前置条件** | web_search 已完成，LLM 生成了最终回复 |
| **操作步骤** | 观察 LLM 回复中的引用格式 |
| **预期结果** | 1. 回复中包含带链接的引用标记<br>2. 点击链接可跳转到原始网页<br>3. 引用格式与知识库 `[citation]` 风格统一 |
| **验证方式** | ChatArea 中 Markdown 渲染结果 |

---

### 模块 D：工具调用前端展示

#### D-1: 五种工具的图标和颜色

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-D-1-001 |
| **测试目标** | ToolCallBlock 为每个工具显示独立的图标和颜色 |
| **前置条件** | 对话中有工具调用 |
| **操作步骤** | 触发不同的工具调用 |
| **预期结果** | search_memory=🧠紫色 / recall_context=💬蓝色 / recall_sources=📎靛蓝 / web_search=🌐绿色 / web_fetch=📄翠绿 |
| **验证方式** | 目视检查 |

#### D-2: 工具调用运行中 → 完成状态切换

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-D-2-001 |
| **测试目标** | 流式过程中显示"搜索中..."→ 完成后显示"搜索到 N 条结果" |
| **前置条件** | 触发 web_search 或 search_memory |
| **操作步骤** | 观察工具调用块的状态变化 |
| **预期结果** | 1. tool_call 事件后显示 `runningText`（如"搜索网页中..."）<br>2. tool_result 事件后显示 `doneText`（如"搜索到 5 条网页结果"）<br>3. 显示耗时（如"1.2s"） |
| **验证方式** | 目视检查 |

#### D-3: 嵌套树形展示

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-D-3-001 |
| **测试目标** | 有层级关系的工具调用以缩进 + 树形连线展示 |
| **前置条件** | 触发多层工具调用（如 web_search → web_fetch，或 search_memory → recall_context → recall_sources） |
| **操作步骤** | 发送需要深入查看网页内容或记忆上下文的问题 |
| **预期结果** | 1. web_fetch 相对于 web_search 缩进一层，前有 `├─` 连接线<br>2. recall_context 相对于 search_memory 缩进一层<br>3. recall_sources 相对于 recall_context 再缩进一层<br>4. 无关联的工具调用保持左对齐 |
| **验证方式** | 目视检查 ChatArea 中工具调用块的缩进和连接线 |

#### D-4: 历史消息工具调用持久化

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-D-4-001 |
| **测试目标** | 切换助手/会话后重新打开，工具调用块完整显示 |
| **前置条件** | 对话中有工具调用记录 |
| **操作步骤** | 1. 完成一轮含工具调用的对话<br>2. 切换到其他助手<br>3. 再切换回来 |
| **预期结果** | tool_calls 从 messages 表加载，ToolCallBlock 完整渲染（状态为 done） |
| **验证方式** | 检查 messages.tool_calls JSON 字段 + 前端渲染 |

---

### 模块 E：联网搜索开关与 UI 按钮

#### E-1: 联网搜索按钮 — 会话级开关

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-E-1-001 |
| **测试目标** | WebSearchToggleButton 在 ChatArea 输入框上方显示，控制会话级开关 |
| **前置条件** | 助手 enable_web_search=1 |
| **操作步骤** | 1. 默认状态：按钮状态继承助手 enable_web_search<br>2. 点击按钮关闭<br>3. 发送消息 |
| **预期结果** | 1. ON 状态绿色高亮<br>2. OFF 状态灰色<br>3. 关闭后 tools 中不含 web_search/web_fetch<br>4. 状态按会话独立，切换会话后重置 |
| **验证方式** | 检查 SSE 事件流中是否出现 tool_call |

#### E-2: 助手设置中的联网搜索默认开关

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-E-2-001 |
| **测试目标** | AssistantModal 中"默认启用联网搜索"开关控制 assistants.enable_web_search |
| **前置条件** | 打开助手编辑弹窗 |
| **操作步骤** | 1. 切换"默认启用联网搜索"开关<br>2. 保存<br>3. 新建会话 |
| **预期结果** | 1. assistants 表 enable_web_search 字段更新<br>2. 新建会话时 WebSearchToggleButton 默认状态跟随该字段 |
| **验证方式** | 数据库验证 + 前端按钮状态 |

#### E-3: 深度思考按钮三态切换

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-E-3-001 |
| **测试目标** | DeepThinkingButton 在 auto / enabled / disabled 三态间循环 |
| **前置条件** | 在 ChatArea 中可见 |
| **操作步骤** | 1. 默认状态：auto（跟随助手 thinking_mode）<br>2. 点击 → enabled（蓝色高亮）<br>3. 点击 → disabled（关闭思考）<br>4. 点击 → 回到 auto |
| **预期结果** | 1. 三态循环切换<br>2. 选中态蓝色高亮<br>3. 状态按对话独立保存 |
| **验证方式** | 目视检查 + providerOptions 传参 |

#### E-4: 隐私模式按钮（会话级）

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-E-4-001 |
| **测试目标** | ChatArea 中隐私模式按钮控制 conversations.privacy_mode |
| **前置条件** | 打开一个对话 |
| **操作步骤** | 1. 点击隐私模式按钮开启<br>2. 发送消息<br>3. 检查数据库 |
| **预期结果** | 1. conversations.privacy_mode=1<br>2. messages.privacy_mode=1（消息发送时快照）<br>3. 按钮显示激活状态 |
| **验证方式** | 数据库验证 |

---

### 模块 F：工具调用上限

#### F-1: 单工具独立上限（maxToolCallPerTool=3）

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-F-1-001 |
| **测试目标** | 每个工具最多调用 3 次，超出返回错误提示 |
| **前置条件** | 构造需要多次调用同一工具的场景 |
| **操作步骤** | 连续触发同一工具超过 3 次 |
| **预期结果** | 1. 第 4 次调用时返回 `{ error: "web_search 调用次数已达上限（3次），请基于现有结果回答" }`<br>2. 其他未达上限的工具不受影响 |
| **验证方式** | SSE tool_result 事件 |

#### F-2: maxSteps 由工具数动态计算

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-F-2-001 |
| **测试目标** | 不做固定全局轮次上限，maxSteps = 工具数 × 3 |
| **前置条件** | 五工具全部开启（memoryEnabled + webSearchEnabled） |
| **操作步骤** | 观察 chatStream 调用参数 |
| **预期结果** | maxSteps = 5 × 3 = 15，由 `Object.keys(tools).length * config.maxToolCallPerTool` 计算 |
| **验证方式** | 服务端日志或代码审查 |

---

### 模块 G：IQS Key 管理

#### G-1: IQS Key 配置状态查询（双来源检测）

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-G-1-001 |
| **测试目标** | GET /api/settings/iqs-key 同时检查 .env 和 user_profile 两个来源 |
| **前置条件** | .env 中已配置 IQS_API_KEY |
| **操作步骤** | 1. 调用 GET /api/settings/iqs-key<br>2. 通过前端 PUT 写入新 Key<br>3. 再次 GET 检查 |
| **预期结果** | 1. 首次返回 `source: "env"`，`configured: true`<br>2. PUT 后返回 `source: "user_profile"`，Key 不返回完整值，仅 masked |
| **验证方式** | API 调用 |

#### G-2: IQS Key 保存与移除

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-G-2-001 |
| **测试目标** | PUT /api/settings/iqs-key 保存 Key 并触发搜索引擎重建 |
| **前置条件** | 无 |
| **操作步骤** | 1. PUT 新 Key<br>2. 观察服务端日志<br>3. 移除 Key（PUT 空值） |
| **预期结果** | 1. 保存后日志输出 `[WebSearch] IQS 搜索引擎已初始化`<br>2. 移除后 `configured: false`，provider 置空 |
| **验证方式** | API 返回 + 服务端日志 |

#### G-3: 前端 SettingsArea IQS Key 配置

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-G-3-001 |
| **测试目标** | SettingsArea 模型 tab 中 IQS Key 配置区域完整可用 |
| **前置条件** | 打开设置 → 模型 tab |
| **操作步骤** | 1. 查看 "IQS 搜索 Key" 状态<br>2. 输入 Key 并保存<br>3. 移除 Key |
| **预期结果** | 1. 未配置时显示"未配置" + 输入框<br>2. 配置后显示 masked key（如 `-wxw***Yg`）+ "配置完成"<br>3. 移除后恢复未配置状态<br>4. 保存按钮在输入为空时 disabled |
| **验证方式** | 目视检查 |

---

### 模块 H：模型能力检测与错误处理

#### H-1: 不支持 FC 的模型报中文错误

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-H-1-001 |
| **测试目标** | 不支持 FC 的模型使用联网/记忆工具时，返回中文友好提示 |
| **前置条件** | 助手使用不支持 function calling 的模型，enable_web_search=1 |
| **操作步骤** | 发送消息 |
| **预期结果** | 返回中文提示："该模型不支持工具调用（记忆搜索/联网搜索），请关闭记忆/联网开关后重发，或切换到支持 Function Calling 的模型" |
| **验证方式** | SSE error 事件内容，匹配 translateError 正则 |

#### H-2: 不支持 FC 但 API 忽略 tools 正常产出文本

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-H-2-001 |
| **测试目标** | 部分兼容模型忽略 tools 参数但正常返回文本，透明通过 |
| **前置条件** | 使用此类模型（如某兼容模式），enable_web_search=1 |
| **操作步骤** | 发送消息 |
| **预期结果** | 正常返回文本回复，无 tool_call 事件，无报错 |
| **验证方式** | SSE 事件流 |

#### H-3: 不支持深度思考的模型

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-H-3-001 |
| **测试目标** | 模型不支持 reasoning 时，深度思考区域为空，按钮仍可点击 |
| **前置条件** | 使用不支持 reasoning 的模型，深度思考按钮设为 enabled |
| **操作步骤** | 发送消息 |
| **预期结果** | 1. 无 reasoning tokens<br>2. 深度思考区域为空<br>3. 无报错<br>4. 深度思考按钮不禁用 |
| **验证方式** | 前端渲染 |

#### H-4: 搜索引擎未配置时 web_search 报错

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-H-4-001 |
| **测试目标** | IQS Key 未配置时，web_search 返回明确错误 |
| **前置条件** | IQS Key 未配置，assistant.enable_web_search=1 |
| **操作步骤** | 发送需要搜索的问题 |
| **预期结果** | tool_result 返回 `{ error: "搜索引擎未配置，请在设置中填写 IQS API Key" }` |
| **验证方式** | SSE tool_result 事件 |

#### H-5: web_search 空结果

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-H-5-001 |
| **测试目标** | 搜索无结果时正常返回空数组 |
| **前置条件** | 使用极偏门的搜索词 |
| **操作步骤** | 发送 "搜索 xyzzy_nonexistent_keyword_123456" |
| **预期结果** | 1. tool_result 返回 `{ raw: [], formatted: "", citations: [] }`<br>2. LLM 回复"未找到相关结果" |
| **验证方式** | SSE 事件流 |

#### H-6: 客户端断连中断

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-H-6-001 |
| **测试目标** | 客户端断开时中止 LLM 调用和工具执行 |
| **前置条件** | 正在流式生成中 |
| **操作步骤** | 关闭浏览器 tab |
| **预期结果** | AbortController 中止所有进行中的操作，不崩溃 |
| **验证方式** | 服务端日志无 unhandled error |

#### H-7: IQS API 超时降级

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-H-7-001 |
| **测试目标** | IQS API 超时时返回友好错误 |
| **前置条件** | 模拟网络延迟或 IQS API 不可达 |
| **操作步骤** | 发送需要搜索的问题 |
| **预期结果** | 搜索超时后返回错误，不阻塞后续 LLM 回复（LLM 基于现有知识回答） |
| **验证方式** | SSE 事件流 |

---

## 三、端到端流程测试

### 流程 1：联网搜索完整链路

```
配置 IQS Key（.env 或 Settings 页面）
  → 创建/编辑助手开启 enable_web_search
  → 对话中发送需要实时信息的问题
  → LLM 调用 web_search 工具（带 query 参数）
  → tool_result 返回带 [citation:N] 编号的格式化结果
  → LLM 可选调 web_fetch 获取详细内容
  → 最终回复使用 [citation:N](URL) Markdown 链接引用来源
```

### 流程 2：记忆 + 网页并行搜索

```
用户提问涉及历史信息 + 最新事实
  → 深度思考模型在 reasoning 中规划检索策略
  → 并行调用 search_memory + web_search
  → 两路结果返回
  → LLM 融合两路信息生成回答
```

### 流程 3：深度思考三态切换

```
默认 auto 态 → 跟随助手 thinking_mode 配置
  → 点击按钮 → enabled → 传 thinking: { type: 'enabled' }
  → 再点击 → disabled → 传 thinking: { type: 'disabled' }
  → 再点击 → 回到 auto
  → 每个状态的 tool_call / reasoning 行为正确
```

### 流程 4：工具调用嵌套展示

```
用户发送需深入查看网页的问题
  → web_search 返回结果列表
  → LLM 选一条结果调 web_fetch(url)
  → 前端渲染：
      🌐 搜索网页中...  →  "搜索到 5 条网页结果"
        ├─ 📄 读取网页中...  →  "读取了 xxx 网页全文"
  → 嵌套缩进 + 树形连线正确
```

### 流程 5：联网搜索开关的会话隔离

```
会话 A：开启联网搜索 → 发送消息 → web_search 工具被注入
会话 B：关闭联网搜索 → 发送消息 → 无 web_search 工具
切换回会话 A → 联网搜索按钮仍为开启状态
```

---

## 四、性能基准

| 指标 | 目标值 | 测量方式 |
|------|--------|---------|
| IQS web_search 延迟 | < 1s | LiteAdvanced 极速版，实测 500-570ms |
| IQS web_fetch 延迟 | < 3s | readpage basic 静态解析 |
| SSE tool_call → tool_result 延迟 | < 3s（含网络） | 时间戳差值 |
| 前端 ToolCallBlock 渲染延迟 | < 50ms | React DevTools |

---

## 五、测试检查清单

### 接口覆盖

- [ ] POST/PUT /api/assistants — enable_web_search 字段 CRUD
- [ ] GET /api/settings/iqs-key — Key 配置状态（env / user_profile 双来源）
- [ ] PUT /api/settings/iqs-key — Key 写入 + provider 重建
- [ ] POST /api/chat/completions — SSE 含 tool_call / tool_result（web_search / web_fetch）
- [ ] POST /api/chat/regenerate — 重新生成 SSE 含工具调用

### SSE 事件覆盖

- [ ] `tool_call` — 工具调用发起（含 web_search、web_fetch）
- [ ] `tool_result` — 工具调用结果（web_search 含 formatted + citations）
- [ ] `reasoning` — 深度思考 tokens（查询改写过程可见）
- [ ] `token` — 内容 tokens
- [ ] `done` — 完成（含 metrics）
- [ ] `error` — 错误（含 FC 不支持中文提示）

### 前端组件覆盖

- [ ] WebSearchToggleButton — 会话级联网开关（绿/灰二态）
- [ ] DeepThinkingButton — 三态循环（auto/enabled/disabled）
- [ ] ToolCallBlock — 5 种工具 icon/颜色/动画/耗时
- [ ] ToolCallBlock — 嵌套树形缩进 + `├─` 连接线
- [ ] ChatArea — 历史消息 tool_calls 持久化渲染
- [ ] AssistantModal — "默认启用联网搜索"开关
- [ ] SettingsArea — IQS Key 配置（输入/保存/masked 显示/移除）

### 数据库表覆盖

- [ ] assistants（enable_web_search）
- [ ] messages（tool_calls JSON）
- [ ] user_profile（iqs_api_key）

---

## 六、已知合理偏离

| 序号 | 偏离项 | 原因 |
|------|--------|------|
| 1 | 查询改写不是独立模块 | 设计决策：深度思考模型在 reasoning 阶段自然完成检索决策，不额外调用 LLM。开深度思考时思考链可见，不开时直接用原始 query |
| 2 | 不做固定全局轮次上限 | maxSteps = 工具数 × maxToolCallPerTool 动态计算。5 工具各限 3 次 = 15 轮，足够覆盖所有合理场景 |
| 3 | 迁移 015 未创建 | 方案分析后判定：工具调用通过 messages.id 主键索引联查已足够，无需额外索引 |
| 4 | ChatArea 无独立记忆开关按钮 | 记忆开关是助手级别设置（enable_memory），不由会话级 toggle 控制。ChatArea 中有 DeepThinkingButton + WebSearchToggleButton + 隐私模式按钮 |
| 5 | formatCitations 返回结构化数据 | 返回 `{ raw, formatted, citations }` 而非纯文本——raw 用于 tool_calls JSON 存储，formatted 给 LLM 引用，citations 结构化元数据给前端 |
| 6 | 嵌套展示仅 ref 推断父子关系 | 按 tool_call 参数中的 memory_id / url 反查前序结果，非 LLM 显式标注层级。覆盖主流场景，边缘情况 flat 渲染 |
