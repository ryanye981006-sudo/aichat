// 记忆提取/更新的 LLM Prompt 模板

export const MEMORY_EXTRACT_PROMPT = `你是一个记忆提取助手。分析以下对话，提取关于用户的重要事实、偏好、个人信息和事件。

对于每条信息，决定它是新增(ADD)、更新(UPDATE)还是删除(DELETE)操作：
- ADD: 全新的记忆事实
- UPDATE: 已有记忆需要更新（提供 existing_id）
- DELETE: 过时或不正确的记忆需要删除（提供 existing_id）

为每条记忆提供 topic 字段，用于分类（如"个人信息"、"技术偏好"、"项目背景"、"日程计划"等）。

返回 JSON 数组格式：
[
  {
    "fact": "用户名为张三，是一名前端开发工程师",
    "topic": "个人信息",
    "action": "ADD",
    "existing_id": null,
    "importance": 0.9
  }
]

importance 评分标准（0~1），根据信息的长期价值和稳定程度打分：
- 0.8~1.0：核心身份信息（姓名、职业、公司、联系方式、教育背景等长期稳定的个人信息）
- 0.6~0.8：重要偏好/技能（技术栈偏好、常用工具、专业领域、核心价值观等）
- 0.4~0.6：一般偏好/经历（兴趣爱好、日常习惯、一次性经历、消费偏好等）
- 0.2~0.4：临时信息（当前情绪、短期计划、可能频繁变化的状态等）

注意：
- 只提取关于用户自身的个人信息，不要提取对话中的一般性知识
- 每条 fact 应该是独立、完整的陈述句
- 如果对话中没有新的个人信息，返回空数组 []
- 只返回 JSON 数组，不要包含其他文字`;

export const MEMORY_UPDATE_PROMPT = `你是一个记忆管理助手。基于已有记忆和新对话，更新/合并/删除相关记忆。

已有记忆:
{existing_memories}

新对话内容:
{conversation}

请分析并返回需要变更的记忆操作（JSON 数组格式，同记忆提取格式）。`;

export const CHAT_SYSTEM_WITH_MEMORY = `{system_prompt}

## 关于用户的长期记忆
以下是从历史对话中提取的关于用户的信息，属于背景知识。请在需要时自然结合使用，不要反复提及或刻意强调：

{memories}

## 相关知识库内容
{knowledge_context}`;
