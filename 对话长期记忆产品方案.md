# 对话长期记忆产品方案

> 产品方向文档，非研发实现方案。执行前再写研发 plan。

## 问题

当前项目的记忆提取是由用户手动触发或某种固定时机触发的，缺乏对"什么时候该提取记忆"的智能判断。提取之后记忆与原始消息之间也没有追溯关系。用户上传的文件、让 AI 检索的网页结果也与消息脱节。

## 核心思路

**不是所有对话都要提取记忆。近期对话保留原文上下文，远期对话在"主题切换点"触发提取。**

## 三层时间模型

```
┌────────────────────────────────────────────────────┐
│  L0 热层：限定轮次内（如最近 10 轮）              │
│  → 不做任何提取，原文保留在会话上下文中           │
│  → 超出轮次的消息自动滑入 L1                      │
├────────────────────────────────────────────────────┤
│  L1 温层：超出轮次但未提取的消息                   │
│  → 逐 turn 计算 embedding 余弦相似度              │
│  → turn[N] 与 turn[N+1] 相似度骤降处 = chunk 边界  │
│  → 连续高相似度的 turn 自然聚为一个 chunk          │
│  → chunk 长度不固定，由话题一致性决定             │
│  → chunk 闭合（遇到边界）后 → 触发记忆提取         │
├────────────────────────────────────────────────────┤
│  L2 冷层：已提取的长期记忆                         │
│  → 向量化存储，语义搜索召回                       │
│  → 保留与原始 chunk/消息的双向链接                │
│  → 时间衰减自然淘汰旧信息，不做主动去重/冲突解决    │
└────────────────────────────────────────────────────┘
```

**chunk 形成规则：**

```
turn_1 (Python 性能优化) ──sim 0.88──→ turn_2 (CPython vs PyPy) ──sim 0.82──→ turn_3 (JIT 编译器)
                                                       │
                                                       │ sim 骤降至 0.31 ← 话题切换！
                                                       ▼
                                              turn_4 (周末去爬山)
                                                       │
                                                       │ sim 0.79
                                                       ▼
                                              turn_5 (登山装备推荐)

结果：
  chunk_A = [turn_1, turn_2, turn_3]   ← 话题: Python 性能
  chunk_B = [turn_4, turn_5]            ← 话题: 周末户外
```

**与滑动窗口的本质区别：**
- 滑动窗口：固定 N turn 一切，可能在话题中间切断
- 语义切分：chunk 边界跟着话题走，每个 chunk 内容自成一体，提取的记忆更干净

## 触发时机（两种，均异步执行）

### 触发 A：话题切换触发

```
会话进行中，超出 L0 轮次的 turn 逐个滑入 L1
    │
    ▼
逐对计算 turn[N] 与 turn[N+1] 的 embedding 余弦相似度
    │
    ├── 相似度 ≥ 阈值（如 0.65）
    │     → 视为同一话题延续，turn[N+1] 并入当前 chunk
    │
    └── 相似度 < 阈值（骤降）
          → 视为话题切换，当前 chunk 闭合
          → 闭合的 chunk 触发记忆提取 Agent
          → turn[N+1] 成为新 chunk 的起点
          → Agent 按内容分为一块或几块记忆
          → 嵌入存储，记录关联的 chunk 和消息
```

**注意：** chunk 由 turn 间相似度自然形成，不由滑动窗口控制。一个 chunk 可能 2 个 turn（话题快速切换），也可能 20 个 turn（长话题讨论）。

### 触发 B：会话空闲超时触发

```
会话超过 1 小时无新消息
    │
    ▼
扫描该会话中所有"未提取"的 L1 chunk
    │
    └── 全部触发记忆提取（无论相似度）
          → 确保不会因为用户突然离开而丢失记忆
```

## 全局记忆开关与隐私模式

### 三层开关逻辑

```
全局记忆开关（按助手/会话级别配置）
  │
  ├── 关闭
  │     → 不提取记忆，不注入 search_memory 工具
  │     → 跟没开记忆功能一样
  │
  └── 开启
        │
        ├── 正常模式
        │     • 消息发送时快照 memory_enabled=1, privacy_mode=0
        │     • 可被提取 Agent 处理
        │     • LLM 可获得 search_memory 工具按需检索
        │
        └── 隐私模式（用户临时开启）
              • 消息发送时快照 memory_enabled=1, privacy_mode=1
              • 该时段消息不会被提取
              • LLM 仍可获得 search_memory 工具按需检索
```

### 关键约束

- **开启记忆不会回溯提取**：开启前的历史消息不补提取，只有开启后发送的消息才会被提取
- **隐私模式不阻断检索**：隐私模式下 LLM 仍能搜到之前的记忆，只是当前对话内容不会成为新记忆
- **消息级快照**：开关状态在消息发送时写入 `memory_enabled` / `privacy_mode` 字段。即使用户中途切换，每条消息也有独立标记，提取 Agent 只处理 `memory_enabled=1 AND privacy_mode=0` 的消息

### 用户交互

- 全局记忆开关：助手设置页，默认关闭，用户手动开启
- 隐私模式：会话顶部临时开关，关闭隐私模式后恢复正常记忆模式，对应消息可被提取

---

## L2 记忆检索：三层渐进工具调用

**L2 记忆不走固定注入，而是作为三个独立工具，让 LLM 按需逐级深入。**

### 为什么用工具调用而非固定注入

| 维度 | 固定注入 | 工具调用 |
|------|---------|---------|
| 触发方式 | 系统每次自动搜，无条件拼入 system prompt | LLM 自主决定何时查、查什么 |
| Token 效率 | 每次都消耗，无关记忆也注入 | 只在需要时消耗 |
| 缓存友好 | 每轮注入不同记忆，破坏 KV-cache | 无工具调用轮次缓存不受影响 |
| 自主性 | 系统决定什么相关 | LLM 自己写检索 query，更精准 |

**唯一前提：模型需支持 function calling。** 2024 年中以后发布的主流模型（GPT-4 全系、Claude 3+、Gemini 1.5+、DeepSeek V2+、Qwen-Max、GLM-4 等）全部支持。对于不支持的老旧模型，该助手不建议开启记忆功能。

### 设计原则：渐进式展开（Progressive Disclosure）

三层工具各有独立的成本门槛。LLM 按需逐级深入，不会一次拉回 50KB 的 PDF 炸掉上下文。

```
Layer 1: search_memory(query)         → 记忆块摘要        token 成本: 低（~200 token/条）
         LLM 需要"用户说过什么"
              │
              ▼  LLM 判断需要原始上下文
Layer 2: recall_context(memory_id)    → turn 原文 + 来源标注  token 成本: 中（可能几千）
         LLM 需要"当时具体聊了什么"
         · 附件只返回名称 + 类型（file/image/web）
         · 不返回附件全文
              │
              ▼  LLM 判断需要读文件内容
Layer 3: recall_sources(memory_id)    → 附件全文 + 网页检索全文  token 成本: 高（可能上万）
         LLM 需要"那个 PDF 里的 QPS 数据是多少"
```

### 工具定义

**工具 1：search_memory — 召回记忆块**
```
search_memory
  ├── 描述: "搜索用户的长期记忆，获取之前对话中记录的事实、偏好和上下文"
  ├── 参数:
  │     ├── query: string     ← LLM 自行组织检索词
  │     └── limit: number     ← 返回条数，默认 5
  └── 返回: [{
          memory_id, content, topic, type,
          created_at, importance,
          source_count        ← 关联的 turn/附件数量，供 LLM 判断是否深入
        }]
```

**工具 2：recall_context — 召回记忆关联的原始对话**
```
recall_context
  ├── 描述: "获取指定记忆的原始对话上下文，了解记忆的来源和背景"
  ├── 参数:
  │     └── memory_id: string
  └── 返回: {
          memory_id, topic,
          turns: [{
            turn_index, role, content, created_at
          }],
          sources: [             ← 只标名称和类型，不返回全文
            { source_id, name: "数据库选型方案.pdf", type: "file" },
            { source_id, name: "PostgreSQL vs MySQL 对比", type: "web" }
          ]
        }
```

**工具 3：recall_sources — 召回附件全文和网页检索内容**
```
recall_sources
  ├── 描述: "获取指定记忆关联的附件全文和网页检索内容"
  ├── 参数:
  │     ├── memory_id: string
  │     └── source_ids: string[]   ← 可选，指定具体 source，不传则返回全部
  └── 返回: {
          memory_id,
          attachments: [{
            source_id, name, type, content    ← 全文
          }],
          web_retrievals: [{
            source_id, url, title, snippet    ← 全文
          }]
        }
```

**为什么是三个独立工具而非合并参数？**
- Layer 1→2 之间成本差距中等（向量搜索 vs SQL join），但 Layer 2 的 turn 原文可能几千 token（项目讨论场景），合并为可选参数会让 LLM 轻率拉回
- Layer 2→3 之间成本跳跃大（附件全文），独立工具让 LLM 审慎调用
- 三个独立工具让 LLM 每步有明确的成本意识，形成自然的"要不要继续深入"的决策点

### search_memory 的检索管线

```
LLM 提供 query
    │
    ├── 通道 A：向量搜索 × time_decay（时间感知，取 top-15）
    └── 通道 B：纯向量搜索（语义兜底，取 top-15）
    │
    ▼
RRF 融合 → top-20
    │
    ▼
Reranker 精排 → top-10
    │
    ▼
时间修正 + 五信号加权 → 返回 top-5 给 LLM
```

### 检索后更新

命中反馈：`access_count += 1`，`last_accessed = now()`。

### 记忆生命周期状态

```
新记忆入库
    │
    ▼
┌──────────┐
│  active  │  ← 默认状态，可被检索
└────┬─────┘
     │
     │  当新记忆覆盖同一事实（如"技术栈 Go → Rust"）：
     ▼
┌──────────────┐
│ invalidated   │  ← 不被检索，但保留用于回溯查询
│ superseded_by │  ← 指向覆盖它的新记忆 ID
│ valid_until   │  ← 事实在现实中失效的时间
└──────────────┘
```

- **默认检索**：`WHERE status = 'active'`
- **回溯查询**：`WHERE valid_from <= '2026-02-01' AND (valid_until IS NULL OR valid_until > '2026-02-01')`
- 旧记忆不删除，保留完整的"我之前知道什么 → 什么时候改变了"链路

### AI 框架选择

**继续使用 Vercel AI SDK（`@ai-sdk/openai-compatible`），不引入额外 Agent 框架。**

| 框架 | 对 aichat 的判断 |
|------|-----------------|
| **Vercel AI SDK**（已在用） | ✅ `streamText` + `tools` + `maxSteps` 恰好够用 |
| LangChain/LangGraph | ❌ 3 个工具不需要 LCEL 链式抽象，过度设计 |
| AgentScope | ❌ Python/Java，语言不匹配 |
| Mastra | ⚠️ 可关注但现阶段过重 |

三个 memory 工具用 Zod schema 定义，总计不超过 50 行代码。换框架得不偿失。

---

## 记忆与消息/Chunk 的关联模型

```
记忆块 (memory_block)
  ├── id, content, embedding, type
  ├── created_at                   ← 记忆提取时间
  ├── source_conversation_id       ← 关联到会话
  ├── source_chunks: [             ← 关联到原始 chunk（chunk_id 记录在此）
  │       { chunk_id, turn_range: [3, 5] }
  │   ]
  └── metadata: {                  ← 记忆文本块元数据
          topic: "数据库选型",
          extracted_at: "2026-05-09T14:30:00Z",
          extraction_trigger: "topic_shift" | "session_idle",
          source_chunk_ids: ["chunk_012", "chunk_013", "chunk_014"]
      }

chunk
  ├── id, conversation_id
  ├── created_at                   ← chunk 创建时间
  ├── turn_range: [3, 5]           ← 覆盖的消息范围
  ├── status: "pending" | "extracted" | "extracting"
  ├── extracted_at                 ← 被提取的时间（NULL = 未提取）
  └── messages: [                  ← 关联到具体消息
        { message_id, role, turn_index }
      ]

消息 (message)
  ├── id, content, role, turn_index, created_at
  ├── memory_enabled: 1 | 0        ← 消息发送时的快照，该条消息是否允许被提取
  ├── privacy_mode: 1 | 0          ← 消息发送时的快照，是否处于隐私模式
  ├── attachments: [               ← 上传的附件
  │     { type: "image", name: "screenshot.png", path: "..." }
  │   ]
  └── web_retrievals: [            ← AI 网页检索结果
        { url: "...", title: "...", snippet: "..." }
      ]
```

**关键约束：**
- 上传内容（图片、文件等）归属于所属消息，记忆提取时一同处理（图片的描述文本、文件的解析内容参与 embedding）
- AI 网页检索结果同样归属于触发检索的那条消息，作为该消息的附属上下文
- 记忆检索命中时，可沿关联链一路追溯到：记忆 → chunk → 消息 → 附件/检索结果

## 记忆提取的内容分块

一次触发可以产出**一块或多块**记忆，由 Agent 根据内容自然分组：

```
原始 chunk 内容（讨论了两个话题）：
  "我们决定用 PostgreSQL...主键用 UUID...索引策略..."
  "对了，前端那边换成了 React 19，主要是为了 Server Components"

Agent 产出两块记忆：
  Block A: { topic: "数据库选型", content: "项目使用 PostgreSQL，主键策略为 UUID..." }
  Block B: { topic: "前端技术栈", content: "前端升级到 React 19，原因：Server Components..." }
```

每块独立 embedding、独立检索。

## 记忆提取时的元数据写入

提取时，Agent 同时输出以下元数据，随记忆一起存储：

```
记忆块 (memory_block)
  ├── id, content, embedding, type
  ├── created_at                   ← 记忆提取时间
  ├── source_conversation_id
  ├── source_chunks: [{ chunk_id, turn_range }]
  └── metadata: {
          topic: "数据库选型",
          extracted_at: "2026-05-09T14:30:00Z",
          extraction_trigger: "topic_shift" | "session_idle",
          source_chunk_ids: ["chunk_012", "chunk_013"],
          importance: 0.72,         ← 提取时计算（见下方）
          specificity: 0.72,        ← 专有名词 + 数字密度，与 importance 同值
          proper_nouns: ["PostgreSQL", "UUID"]
      }
  ├── access_count: 0              ← 初始化，后续检索命中后累加
  └── last_accessed: NULL          ← 初始化，后续检索命中后更新
```

### 重要度计算（提取时，CPU 启发式）

```
importance = clamp((专有名词数 + 数字数) / 总词数, 0, 1)

示例: "用户决定将 PostgreSQL 主键从 UUID 迁移到自增 ID，涉及 200 万行数据"
  专有名词: PostgreSQL, UUID, ID = 3
  数字: 200 万 = 1
  总词数: ~18
  importance = (3 + 1) / 18 = 0.22
  → 内容偏具体，重要度中等
```

**不做：** 检索时才算（延迟扛不住）、GPU 零样本分类（个人聊天用不上）。

---

## 记忆检索评分策略

### 总览：五信号加权融合

```
final_score = 0.35 × semantic + 0.20 × recency + 0.20 × time_decay + 0.10 × frequency + 0.15 × importance
                ↑              ↑              ↑               ↑               ↑
            语义相似度      近因性衰减     创建时间衰减     访问频率加权     重要度加权
```

五条信号线各自独立计算，最后加权求和。语义是主角（0.35），其余四个一起（0.65）保证时效和可靠性。

---

### 信号 1：语义相似度（semantic_score）

```
semantic_score = cosine_similarity(query_embedding, memory_embedding)
范围: [-1, 1]，实践中取 [0, 1]
权重: 0.35
```

最基础最核心的信号，不再赘述。

---

### 信号 2：创建时间衰减（time_decay）

```
time_decay = 0.5^(days_since_created / 60)
范围: 0 ~ 1
权重: 0.20
```

| 创建后经过 | 衰减系数 | 含义 |
|-----------|---------|------|
| 0 天 | 1.00 | 刚创建，满分 |
| 30 天 | 0.71 | 一个月前的信息，价值下降 |
| 60 天 | 0.50 | 半衰期，价值减半 |
| 120 天 | 0.25 | 四个月，显著衰减 |
| 180 天 | 0.12 | 半年，边缘化 |

**设计意图：** 旧信息不删除，但检索时自然靠后。用户换技术栈、变偏好，老记忆自动让位。60 天对应个人聊天中"一个季度前的讨论大概率已过时"的节奏。

---

### 信号 3：近因性衰减（recency_score）

```
recency_score = e^(-0.01 × hours_since_last_accessed)
范围: 0 ~ 1
权重: 0.20
```

| 上次被想起距现在 | 近因分 | 含义 |
|-----------------|--------|------|
| 1 小时前 | 0.99 | 刚刚被用到 |
| 1 天前 | 0.79 | 最近相关的 |
| 3 天前 | 0.49 | 有点远了 |
| 7 天前 | 0.19 | 一周没被想起 |
| 30 天前 | 0.00 | 从记忆中消失 |

**与 time_decay 的区别：**
- time_decay 问的是"这条记忆什么时候创建的"——创建时间越旧越衰减（静态）
- recency_score 问的是"这条记忆最近有没有被用到"——最近被用到说明跟当前话题有关（动态）

一条创建很久的记忆，如果最近频繁被检索到，recency_score 会很高，time_decay 可能较低，两者互补。

---

### 信号 4：访问频率加权（frequency_score）

```
frequency_score = 1 - e^(-0.1 × access_count)
范围: 0 ~ 1
权重: 0.10
```

| 被检索命中次数 | 频率分 | 含义 |
|--------------|--------|------|
| 0 次 | 0.00 | 从未被想起 |
| 1 次 | 0.10 | 第一次命中，大幅上涨 |
| 3 次 | 0.26 | 多次命中 |
| 5 次 | 0.39 | 常用记忆 |
| 8 次 | 0.55 | 高频记忆 |
| 20 次 | 0.86 | 趋近天花板 |

**设计意图：** 饱和曲线——第 0→1 次的增益最大（"被想起"本身就重要），第 50→51 次几乎没区别。

---

### 信号 5：重要度加权（importance_score）

```
importance_score = importance（提取时已写入 metadata，直接读出）
范围: 0 ~ 1
权重: 0.15
```

提取时算好，检索时零成本读出。

---

### 检索后更新（命中反馈）

```
每次检索返回 top-K 给用户后，对返回的每条记忆:
  access_count  += 1
  last_accessed  = now()
```

**效果：** 被命中的记忆下次检索时 frequency_score 和 recency_score 都会更高——形成"越想越重要"的正反馈。系统自动学会哪些记忆是真正有用的。

---

## 检索管线（两通道 + RRF + Reranker + 时间修正）

### 管线总览

```
用户查询
    │
    ├── 通道 A：向量搜索 × time_decay       (时间感知，取 top-15)
    │        语义 × 时间衰减
    │
    └── 通道 B：纯向量搜索                   (语义兜底，取 top-15)
             纯语义，不衰减
    │
    ▼
RRF 融合 → 两通道结果按排名合并 → top-20
    │
    ▼
Reranker 精排 → Cross-Encoder 精读 query+memory → 纯语义打分 → top-10
    │
    ▼
叠加时间修正 → final = CE_score × 0.5^(days/60)
    │
    ▼
五信号加权融合 → 叠加 frequency、recency、importance
    │
    ▼
返回 top-5
```

### 各步详解

**通道 A — 时间感知搜索：**
每条记忆的排序分 = 余弦相似度 × 0.5^(创建天数/60)。旧记忆自动靠后，但不删除。

**通道 B — 语义兜底：**
纯余弦相似度排序，不做时间衰减。防止时间衰减太粗暴——万一是旧但仍然准确的答案（用户问"两年前那个项目"）也能进入候选池。

**RRF 融合：**
公式：`RRF(m) = 1/(60 + 通道A排名) + 1/(60 + 通道B排名)`
用排名而非分数融合，两边量纲不同也能公平合并。60 是平滑因子，防止第 1 名碾压一切。

**Reranker 精排：**
Cross-Encoder 逐对精读 query + memory 的语义匹配，输出纯语义相关分。管语义精度，不管时间。

**叠加时间修正：**
`corrected = CE_score × 0.5^(days/60)`

**为什么不在 RRF 之后直接取结果，还要再叠时间修正？**
Reranker 只看语义、不管时间。一条 90 天前的记忆语义匹配 0.93，会比 2 天前匹配 0.91 的排得更前——但它已经过时了。在 Reranker 输出上再乘一次时间衰减，把旧信息拉回正确位置。

**五信号加权融合：**
在时间修正后的分数上，再叠加 frequency、recency、importance，输出最终排序。

**为什么前面通道 A 已经用了时间衰减，Reranker 之后还要叠？**
通道 A 的时间衰减管的是"能不能进候选池"——旧记忆可能根本进不来。Reranker 之后的修正管的是"进了候选池之后谁排前面"——候选中如果都是旧记忆（因为通道 B 放水进来了），Reranker 无法区分新旧，必须在这里再拉一次。两层时间信号不矛盾，各管各的。

---

## 关键参数（产品预设，后续可调）

| 参数 | 预设值 | 说明 |
|------|--------|------|
| L0 保留轮次 | 10 轮 | 以内的不做提取 |
| 话题切换阈值 | 余弦相似度 < 0.65 | 相邻 turn 低于此值视为话题切换，chunk 闭合 |
| chunk 形成方式 | 语义驱动 | 相邻 turn 高相似度→归入同一 chunk，低相似度→切分 |
| chunk 大小 | 不固定 | 由话题一致性自然决定，2~N 个 turn |
| 会话空闲超时 | 1 小时 | 超时强制提取所有未闭合 chunk |
| 异步执行 | 后台队列 | 不阻塞用户对话 |
| 时间衰减半衰期 | 60 天 | 创建后 60 天分数减半 |
| 近因衰减率 λ | 0.01/小时 | ~70 小时半衰，7 天后分数 ~0.19 |
| 频率增长率 μ | 0.1 | 饱和曲线增长速度 |
| 重要度算法 | CPU 启发式 | (专有名词数 + 数字数) / 总词数 |
| RRF 平滑因子 k | 60 | 防止第 1 名碾压 |
| 五信号权重 | sem:0.35, rec:0.20, time:0.20, freq:0.10, imp:0.15 | 语义为主，其余四个保证时效和可靠 |
| 通道 A/B 各自取 top-N | 15 | RRF 融合前各取 15 条，融合后取 20 |
| Reranker 精排取 top-N | 10 | 精排后取 10 条，时间修正 + 五信号后最终返回 5 条 |
| 全局记忆开关 | 默认关闭 | 按助手配置，仅支持 function calling 的模型建议开启 |
| 隐私模式 | 会话级别临时开关 | 开启时段消息不提取但可检索，关闭隐私模式后恢复正常记忆模式 |
| 消息快照字段 | memory_enabled, privacy_mode | 消息发送时写入，提取 Agent 按条件过滤 |
| search_memory 返回条数 | 5 | 第一层工具，返回记忆块摘要 |
| recall_context | — | 第二层工具，返回 turn 原文 + 来源标注（名称+类型） |
| recall_sources | — | 第三层工具，返回附件全文 + 网页检索全文，支持指定 source_ids |
| 记忆默认状态 | active | 可被检索；被新记忆覆盖后变为 invalidated |
| AI 框架 | Vercel AI SDK | 已在用，不引入 LangChain/AgentScope 等额外框架 |

## 用户体验

1. 用户对话中，一切都无感知——记忆提取在后台异步跑
2. 后续新对话中，系统检索到相关记忆时，回复底部可附带"参考来源"：点击可跳转到原始对话的对应位置
3. 用户可在记忆管理界面查看、编辑、删除自己的记忆，并能看到每条记忆来自哪次对话

## 用户个人信息（固定上下文）

### 与记忆系统的区别

| 维度 | L2 冷层记忆 | 用户个人信息 |
|------|-----------|------------|
| 来源 | Agent 自动从对话提取 | 用户手动维护 |
| 内容 | 从聊天中挖掘的事实/偏好 | 用户主动声明的身份/背景 |
| 发送方式 | LLM 通过 search_memory 工具按需调用 | **每次请求固定拼接**，不检索 |
| 变化频率 | 自动衰减 + 新记忆覆盖 | 用户手动增删改 |
| 示例 | "用户正在用 PostgreSQL 做电商项目" | "我叫张三，住在北京，职业是后端开发" |

### 存储

```
user_profile
  ├── user_id
  ├── fields: {                ← 结构化键值对
  │     "昵称": "张三",
  │     "位置": "北京",
  │     "职业": "后端开发",
  │     "技术栈": "Go, Python, PostgreSQL",
  │     "当前项目": "电商平台重构"
  │   }
  └── updated_at
```

### 发送机制

```
每次 LLM 请求构造 system prompt 时：

  system_prompt = 基础系统提示词
                + "## 用户信息\n" + user_profile.fields 序列化
                + "## 对话历史\n" + L0 热层消息（固定轮次）

  tools（如果全局记忆开关开启）:
                + search_memory 工具  ← LLM 按需调用，不走固定注入
```

**关键：** 用户信息始终跟在 system prompt 里，不走检索、不走向量。L2 记忆不做固定注入，LLM 通过 `search_memory` 工具自主决定何时检索。

### 维护方式

用户在前端个人信息设置页手动填写/修改，不需要 Agent 参与。跟改个人资料一样简单。

## 暂不纳入本期范围

- 跨用户记忆共享
- 记忆的自动 Dream/合并（待记忆量上来后再做）
- 图数据库（首期用 JSONB entities 字段做轻量关联）
- 多模态记忆的直接 embedding（图片用描述文本替代）
